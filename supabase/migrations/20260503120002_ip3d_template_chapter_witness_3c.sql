-- IP-3d chapter_witness decisão: Opção 3c (obrigatório com grace period 60 dias).
-- Applied 19/04/2026 via MCP apply_migration.
-- Legal-counsel audit p34 peça 3 — resolve RF-3 inconsistência template
-- (facultativo) vs workflow IP-3c (obrigatório threshold=5).
-- RF-F residual: migration IP-3c precisa grace period em _can_sign_gate.
SELECT 'No-op stub. UPDATE via MCP apply_migration.'::text;
