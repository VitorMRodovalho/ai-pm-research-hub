# Decision — #592 + #591a gamification cockpit polish (build + ship)

**Date:** 2026-06-08
**Type:** PM-decision (build) + council-on-draft
**Outcome:** Shipped. PR #597 → `main`. Frontend-only (no migration/RPC/deploy). 0 `--admin`. #592 CLOSED; #591 stays open (persistence half deferred).

## Options presented (session "go")
Head of the PM-agreed follow-up sequence (from the #579 handoff). Grounded BEFORE presenting (live: main `d0129497`, 59 open, /mcp 308 +/semantic 4, invariants 0):
1. **#592 + #591a (cockpit polish)** — both in `TribeGamificationTab.tsx`, same #425/#577 arc; one PR, one council pass. #591b (toggle persistence) gated on a live PostHog check.
2. #592 only (minimal a11y close).
3. #587 batch link N+1 (issue says "not urgent").

**PM chose option 1.**

## Grounding that shaped scope
- The `showBreakdown` toggle has **zero explicit instrumentation**; `$autocapture` is flowing (10,855/90d) but shows **0 clicks on the toggle in 30d** (it shipped hours ago, leaders-only). → **#591b persistence cannot be gated on a cohort that does not exist.** Decision: ship the *enabler* (a `gamification_breakdown_toggled` capture) so a future session can evaluate the cohort; **defer persistence**. #591 stays open.
- The 7 pillar `*_points` values are already in the `Member` payload; the 7 pie-chart pillar labels already exist in i18n → #591a needs only one new key (`xpByPillar`) + a drill-down section.

## Build
- **#592 a11y**: `forced-colors:focus:outline (+offset-2)` on the per-row expand button AND the sortable `<Th>` headers (both keyboard-focusable; HC strips box-shadow). `aria-controls` made conditional on `isOpen` (panel only in DOM while open).
- **#591a**: new "XP por pilar" section in `MemberDrillDown` rendering all 7 pillar values (was: only `champions_points`, in Recognition) → future full column-collapse is lossless. Reuses pie-chart labels + `StatCard`.
- **#591b enabler**: `toggleBreakdown` captures `gamification_breakdown_toggled {expanded, tribe_id, initiative_id}` (best-effort); orphan-sort reset moved out of the setState updater (fires once, not twice under StrictMode). No persistence.

## Council-on-draft (pre-commit) — Workflow `wf_0e6285eb-a0a`
5 reviewers (code-reviewer, senior-software-engineer, ux-leader, stakeholder-persona=gp-leader, a11y/i18n verifier): **GO ×1, GO_W_FIXES ×4, 0 NO_GO, 0 blocker.** Folded:
- **TS2322** (senior-eng, MEDIUM): a bare `key={…}` on the plain-function `StatCard` is a type error in this project's React-types setup (verified `tsc --noEmit`). The reviewer's `key={i}` suggestion would *also* error → wrapped each pillar card in a keyed `<Fragment>` (transparent in DOM, typed for key). Confirmed gone.
- **champions dedup** (4/5 reviewers, MEDIUM): champions was rendered twice (XP-por-pilar + Recognition). Removed the Recognition Champions card; champions now lives only in XP-por-pilar (its canonical XP-source home). Recognition = Credly + CPMAI.
- **placement** (ux, MEDIUM): XP-por-pilar moved to *after* the trail/coaching narrative (keep behavioural + learning adjacent).
- **grid** (ux, MEDIUM): capped at `md:grid-cols-4` (was `lg:grid-cols-7`, cramped in the panel).
- **outline-offset** (a11y, LOW) on all 3 rings + **tribe/initiative** telemetry context (gp-leader, NIT).
- **test de-brittle** (senior-eng, NIT): the contract test now locks the Fragment pattern + structural facts, not exact callback source.

**Documented-not-changed** (deferred to #591/#592): section-title total (sum-vs-table-total mismatch risk), pillar ordering by coaching impact (kept mirroring table column order for cross-reference), semantic `<h4>` section headers (pre-existing across all 4 drill-down sections — broader a11y follow-up). ASCII JSX fallbacks (`Presenca`/`Certificacoes`) are *correct* — they match the existing pie-chart fallbacks for the same keys (reviewer "inconsistent" flag was a false positive).

## Verification (live)
- `tsc --noEmit`: no new TS error (TS2322 eliminated).
- `astro build` exit 0 · full DB-aware suite **3696/0/0**.
- 0 NUL bytes; i18n parity in all 3 dicts; new test (`592-591-gamification-cockpit-polish`) registered in BOTH `test` + `test:contracts`.
- Section order verified: signals → trail → XP-por-pilar → recognition; `member.champions_points` renders exactly once; 3 forced-colors rings + 3 offsets.

## Refs
Issue #592 · #591 (a checked / b deferred) · PR #597 · #425 (cockpit) · #577 (toggle) · `TribeGamificationTab.tsx`.
