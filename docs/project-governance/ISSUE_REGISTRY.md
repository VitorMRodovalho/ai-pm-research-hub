# Issue Registry

**Last updated:** 2026-05-26 (p262.W4c — **#315 Wave 1b dispatch 3/7 #379 SHIPPED + CLOSED — list_governance_library default-status exclusion live; GAP-259.A closed; biblioteca membro agora oculta `draft`/`pending_proposer_consent`/`withdrawn`/`revoked` por default**): PR #389 squash-merged `eb72a926`. Migration `20260805000042` extends `list_governance_library` body with ONE new WHERE clause line: `AND (v_filter_status IS NOT NULL OR gd.status IN ('active','approved','under_review','superseded'))`. Effect: member library default call (no filter) shows only 4-status include set (active + approved + under_review + superseded); explicit `p_filters.status='draft'` (or any of the 4 excluded statuses) still works for admin/audit override (existing `v_filter_status IS NULL OR gd.status = v_filter_status` line preserved). All 5 visibility-class branches preserved byte-identical (public + active_members + legal_scoped with member_document_signatures join + admin_only with v_is_admin + audit_restricted with v_is_platform_admin). Payload P0-Q8 forward-defense preserved (12 canonical fields; no file_id/drive_url/content_html/pdf_url leak). Live verification: 16 total docs / 13 visible by default (4-status include set) / 3 hidden by default (Frontiers `18ec4690-…` in `draft` post-#377 + 2 manual drafts). Smoke 1 (Vitor impersonation + default filter): Frontiers excluded ✓. Smoke 2 (Vitor impersonation + explicit status='draft'): Frontiers appears ✓ (admin override preserved). **Invariants 21/21 violation_count=0 live** post-deploy. **PM ratification chain**: p259 GAP-259.A PM Option (a) ratified at p259 close → p262 PM #379 prompt confirmed → p262 PM AskUserQuestion ratified 4-status default include set (active + approved + under_review + superseded) matching p259 v2 + STATUS_FILTER_OPTIONS dropdown in GovernanceLibrary.tsx. SEDIMENT-254.A applied: shadow row `20260525193317` deleted with exact `WHERE version = '20260525193317'` discipline; canonical `20260805000042` preserved + registered via `supabase migration repair --status applied 20260805000042 --linked`. Contract test `p262-312-w4c-gap-259-a-default-status-exclusion.test.mjs` 20 assertions (17 static + 3 DB-gated). **Test baseline ratchet**: 2483/2392/0/91 → 2503/2409/0/94 offline (+20). **#380 W4d reader hardening** is now next dispatch in PM-ratified sequence (4/7 — unblocked) — extends `/governance/document/[id]` to use new SECDEF RPC `get_member_governance_doc` instead of table-direct SELECT. **Prior context (p262 — dispatch 2/7 #378 gate templates SHIPPED)**: PR #387 squash-merged `15c7b3f0`. Migration `20260805000041` extends `resolve_default_gates(p_doc_type text)` CASE with branches for `editorial_guide` (3 gates: curator/leader_awareness/submitter_acceptance) · `governance_guideline` (3 gates same shape) · `manual` (5 gates: curator/leader_awareness/submitter_acceptance/president_go/president_others — mirrors policy) · `executive_summary` (2 gates: curator/submitter_acceptance — minimal consumer) · `framework_reference` (3 gates: same as editorial_guide) · `project_charter` (2 gates: curator/leader_awareness — initiative-internal TAPs). 5 pre-existing branches preserved byte-identical (cooperation_agreement + cooperation_addendum 6 gates each with chapter_witness; volunteer_term_template + volunteer_addendum 5 gates each with volunteers_in_role_active; policy 5 gates). Sanity DO RAISES if any of 11 doc_types still returns NULL post-deploy (migration-time forward-defense). Live verification: all 11 doc_types return non-NULL via `WITH dt(name) AS (VALUES ...) SELECT resolve_default_gates(name)` (count: 6+6+5+5+5+3+3+5+2+3+2 = 45 gates total). DocumentVersionEditor unchanged — already handles non-NULL arrays correctly. Frontiers Editorial Guide (currently in `draft` per p261 #377) can now advance `draft → under_review` via editor lock for the first time. SEDIMENT-254.A applied: shadow row `20260525191127` deleted with exact `WHERE version = '20260525191127'` discipline; canonical `20260805000041` preserved + registered via `supabase migration repair --status applied 20260805000041 --linked`. Phase C body drift gate clean (function is `LANGUAGE sql` — no inline `--` comments inside `AS $$ ... $$` to be stripped by MCP). Follow-up commit fixed `preview-gate-eligibles-cache-equivalence.test.mjs:135` which used `executive_summary` as the canonical NULL example; switched to fictional `nonexistent_doc_type_for_test` to decouple from gate-template coverage. **Invariants 21/21 violation_count=0 live** post-merge. **Test baseline ratchet**: 2442/2354/0/88 → 2483/2392/0/91 offline (+41 from new contract test, +3 DB-gated skips). **#379 W4c GAP-259.A** is now next dispatch in PM-ratified sequence (3/7 — unblocked). Implementation scope per p259 GAP-259.A PM-ratified Option (a) + PM #379 prompt: `list_governance_library` body extension excluding `draft`/`pending_proposer_consent`/`withdrawn`/`revoked` from default member-context call; explicit `p_filters.status` override allowed only for admin/audit. **Prior context (p261 — Wave 1b first dispatch #377 sign_proposer_consent SHIPPED)**: PR #385 squash-merged `e8d7fe92`. Migration `20260805000040` deployed live via `mcp__supabase__apply_migration` then registered via `supabase migration repair --status applied 20260805000040 --linked`. 4 sections: ALTER TABLE `governance_documents.proposer_member_id uuid REFERENCES members(id) ON DELETE RESTRICT` (nullable for historical compat); Frontiers fixture (`18ec4690-…`) backfilled with Fabrício Costa member.id (`92d26057-…`) from p259 evidence; `create_governance_document_intake` extended (minimum-diff) to persist `proposer_member_id` from payload; new `sign_proposer_consent(p_document_id uuid, p_evidence jsonb DEFAULT NULL)` SECDEF RPC. Gates: caller MUST equal `governance_documents.proposer_member_id` (GP/admin cannot sign on behalf — matches p256 M3 self-attest guard). Idempotent on already-draft (returns `already_signed=true`). Audit log captures `governance.proposer_consent_signed` with 6 metadata fields (rpc_version=`p261_312_w4b`). Live smoke 3/3 PASS: (1) Fabrício impersonation via SEDIMENT-226.C JWT-claim DO block → status `pending_proposer_consent → draft`, audit row anchored at evidence channel `p261_live_smoke`; (2) re-call returns `already_signed=true`, audit count stays at 1 (no duplicate INSERT); (3) wrong member (Vitor admin) rejected with `Unauthorized: only the named proposer can sign`. **Invariants 21/21 violation_count=0 live** (V'_prime no longer applies to Frontiers since status=draft; V_status_chain_coherence does not yet apply because draft is not in approved/active). **2 hotfixes during CI**: (a) Phase C body drift — apply_migration MCP stripped inline `--` comments from function bodies (live prosrc_len=4249/2885 vs file 5030/3369); fixed per documented pattern (CLAUDE.md/database.md GC-097 sediment note: minimum-diff fix = strip from file to match live); (b) **SEDIMENT-254.A** — shadow row `20260525182624` auto-created by apply_migration MCP alongside canonical `20260805000040` registration; flagged by Phase C orphan-detection gate as 'NEW missing-file drift'; deleted with exact `WHERE version = '20260525182624'` discipline. **#378 W4a gate templates** is now next dispatch in PM-ratified sequence (2/7). **Test baseline ratchet**: 2414/2326/0/86 → 2442/2354/0/88 offline (+28 from new contract test). Contract test 26 static pass + 2 DB-gated skip (offline). CI in-DB-mode 2481/2474 pass + 5 skip + ALL invariants green post-merge. **Prior context (p260 — Wave 4 #312 audit jornada + PM D1-D5 ratified + 7 children spawned)**: Wave 4 audit doc shipped at `docs/audit/p260_wave_4_312_journey_audit.md` grounded in code+RPCs+live corpus (16 docs, 21 invariants 0 violations, 12 engagement_kind_permissions for `participate_in_governance_review`, 6 chains under review, 43 document_comments). **Key Wave 4 findings**: (A) **GATE TEMPLATE COVERAGE GAP** — 6 of 11 doc_types return NULL from `resolve_default_gates(p_doc_type)` (editorial_guide [Frontiers fixture!], governance_guideline, project_charter [TAPs!], manual, executive_summary, framework_reference) → `DocumentVersionEditor.tsx` sets `gatesUnsupported=true` blocking lock; (B) **`sign_proposer_consent` RPC MISSING** — Wave 1b deferred per p256 M3 header note "Real consent flow ships Wave 1b"; Fabrício cannot self-attest in-app yet, Frontiers Guide stuck at `pending_proposer_consent`; (C) **BLIND-REVIEW PRIMITIVES ENTIRELY ABSENT** — no `review_mode` column on `governance_documents`/`document_versions`/`document_comments`/`approval_chains`; no `submitted_initial_parecer_at` filter on comment listing; SPEC §19.5 explicitly deferred to Wave 1b ("`document_comments` blind-review columns for Wave 4 enforcement of invariant 20"); SPEC §11 row 4 Curador independente persona BLOCKED on this primitive; (D) **COMMENT VISIBILITY DRIFT** — drawer (`ClauseCommentDrawer.tsx`) declares 4 visibilities (`curator_only/submitter_only/change_notes/public`) but RPC `create_document_comment` (adr_0041) REJECTS `public`; 5 legacy rows with `visibility='public'` predate adr_0041 (2026-04-27); (E) **REVIEW-MODE × INSTRUMENT × TARGET ARCHITECTURE UNBUILT** — no `target_instrument`/`target_language_policy`/`target_length_policy`/`derived_product_group_id`/`source_artifact_id`/`content_products` table or column; SPEC §6.1 introduces these as NEW concepts; conflation risk between research-output domain (`board_items` + `peer_review_log` + `leader_review_log`) and governance domain (`governance_documents` + `approval_chains`) needs ADR. **Persona smoke verdict (SPEC §11 8 personas)**: 1 GP/admin ✓; 2 Author/proponente ⚠ (sign_proposer_consent missing); 3 Curador ⚠ (#161 cross-cut + draft-read regression Roberto/Sarah); 4 Curador independente ✗ BLOCKER (no review_mode primitives); 5 Revisor externo (Angelina) ✓ (p220 BUG-219.A correctly wired); 6 Líder de tribo ⚠ (Ana Carla #171 specific RC TBD); 7 Membro ativo ✓ + GAP-259.A pending; 8 Auditor privilegiado ✓ (audit_restricted predicate untested live). **5 PM decisions required before Wave 4 closes** (D1-D5 in audit §1.3): D1 wave for missing gate templates · D2 wave for blind-review primitives · D3 ADR for derived products surface · D4 wave for reader hardening · D5 disposition of 5 legacy `public` rows. **Proposed child-issue split (audit §11)**: v1-must-have leaves `#312-W4a` gate templates / `#312-W4b` sign_proposer_consent RPC / `#312-W4c` GAP-259.A fix / `#312-W4d` reader hardening / `#312-W4e` curator draft-read mitigation; v1-follow-up `#312-W4f` blind-review primitives / `#312-W4g` derived products (PM D3 ADR first). #312 STAYS OPEN as audit umbrella until PM ratifies D1-D5 + child issues dispatched. Wave 4 acceptance per SPEC §16.5 is documentation+matrix — does NOT require all gaps fixed in one PR. Carries from p259 unchanged (curator-draft-access, reader hardening, list_governance_library payload extension, GAP-259.A — all now mapped to `#312-W4*` leaves). **Prior context (p259 — Frontiers fixture #96 validated end-to-end via live M3 intake)**: Live smoke of Wave 2 #310 admin intake via the underlying RPC `create_governance_document_intake` (Wave 1a M3 path) using SEDIMENT-226.C JWT-injection pattern (`PERFORM set_config('request.jwt.claims', …, true)` simulating Vitor inside a `WITH jwt AS (...)` CTE so the main `SELECT` evaluates `auth.uid()` to Vitor's `auth.users.id` and passes the RPC's `members.auth_id = auth.uid() AND is_active = true` gate + `can_by_member(member_id, 'manage_event')` ladder). PM-ratified payload (3/3 Recommended in scope-lock AskUserQuestion): `title='Guia Editorial Frontiers in AI & Project Mgmt'` (SPEC §9 literal) + `doc_type='editorial_guide'` (P0-Q1 ratified) + `author_label='Fabricio Costa'` + `visibility_class='active_members'` (SPEC §9 post-approval target) + 701-char description (under 1000 wizard limit; references CR-050/R3-C3/ADR-0021 deps + #96 anchor + tracks A/B/C + 9-stage editorial flow + declarações obrigatórias) + `proposer_ack_offline=false` (canonical A2 path — Fabrício hasn't signed in-app yet) + `proposer_member_id=92d26057-5550-4f15-a3bf-b00eed5f32f9` (Fabrício Costa member.id, distinct from caller per RPC self-attest guard). RPC returned `{ok:true, document_id:18ec4690-4f5a-4cab-904d-451e2c7245bf, status:pending_proposer_consent, acknowledgement_mode:informational, note:'Doc awaiting proposer in-app consent (...). Wave 1b will ship sign_proposer_consent RPC.'}`. Wizard JSX NOT exercised (PM may walk browser modal separately for visual UX evidence; same row would land either way). **Validations all PASS**: (1) row landed in `governance_documents` with all expected fields — `organization_id` Núcleo IA `2b4f58ab-7c45-4170-8718-b77ee69ff906` (derived from caller), `acknowledgement_mode=informational` (A1 default for editorial_guide, override allowed=No), `current_version_id`/`current_ratified_version_id`/`approved_at`/`closing_gate_signoff_id`/`effective_from`/`effective_until`/`required_action` all NULL — expected for fresh intake; (2) `list_governance_library('{}'::jsonb)` called as Vitor (authenticated CTE injection) returned `total_returned=16, total_field=16` (was 15 pre-fixture, +1 = our doc), Frontiers entry surfaces with all 12 expected library fields including the full 701-char description untruncated server-side + **ZERO PII leak** (no `file_id`/`drive_url`/`content_html`/`pdf_url` in payload per P0-Q8 forward-defense; CI lock in `p258-314-wave3-member-library.test.mjs`); (3) `check_schema_invariants()` returned 21/21 invariants all `violation_count=0` — specifically `V_prime_pending_proposer_consent_no_open_chain` = 0 (our doc has NO row in `approval_chains` per A2 design) + `V_status_chain_coherence` = 0 (our doc outside `IN ('approved','active')` so doesn't apply; 7 legacy pre-chain docs remain compliant via #367 synthetic chains) + A1/A2/A3 member role invariants = 0 (no member-side drift); (4) `admin_audit_log` has 0 rows for this doc — correct because `proposer_ack_offline=false` skipped the `governance.proposer_attestation_offline` audit branch (the canonical A2 path leaves the doc parked until Wave 1b ships the real proposer_consent flow; the `proposer_member_id` Fabrício UUID was validated for distinctness from caller but otherwise NOT persisted anywhere — by design). Redirect URL `/admin/governance/documents/18ec4690-4f5a-4cab-904d-451e2c7245bf/versions/new` is well-formed; the page mounts `DocumentVersionEditor` (manage_member RPC-gated) — Vitor has manage_member so editor opens; non-admin members blocked by RLS swap (pre-existing curator-draft-access carry deferred to separate Wave 1b leaf). **NEW GAP-259.A surfaced + PM-ratified at session close** (low severity, not blocker for Wave 4 dispatch): `list_governance_library` default unfiltered payload returns ALL documents with valid `visibility_class` regardless of status — so `pending_proposer_consent`/`draft`/`withdrawn`/`revoked` ALL surface to active members in `/governance/documents` default view. The `STATUS_FILTER_OPTIONS` in `GovernanceLibrary.tsx:69` deliberately only exposes 4 "operational" statuses in the dropdown (`active`, `approved`, `under_review`, `superseded`), but the default unfiltered view shows all 8. **PM ratified Option (a) at session close (2026-05-25)**: `list_governance_library` MUST exclude `draft`/`pending_proposer_consent`/`withdrawn`/`revoked` from the default member-facing library view; allow explicit override via `p_filters.status` ONLY for admin/audit contexts (admin pages OR explicit MCP calls), NOT for member library default. Rationale: member library represents "ready-to-engage" governance — not intake/revision queue. Implementation tracked as separate Wave 1b leaf (NOT this PR; minimum change = server-side WHERE clause adding `(v_filter_status IS NOT NULL OR gd.status IN ('active','approved','under_review','superseded'))` + forward-defense contract test asserting default exclusion). Out of scope for #312 Wave 4 audit. **Carries (deferred, unchanged from p258 close)**: (a) Wave 1a known regression — curator-draft-access mitigation Roberto Macêdo + Sarah Faria (separate Wave 1b leaf, NOT this PR); (b) `/governance/document/[id].astro` reader hardening (table-direct SELECT bypass, OPP-153.1 follow-up); (c) `list_governance_library` payload extension for `depends_on`/templates (Wave 1b expansion deferred). **NEXT dispatch (PM call)**: Wave 4 #312 audit jornada review/comment/approval persona smoke — next-in-line for v1 close per SPEC §16.5 + §17 Wave 4 footprint. **#315 stays OPEN** (umbrella closes after Wave 4 #312 + PM v1-vs-post-v1 cut); **#96 stays OPEN** (umbrella covers full Frontiers launch end-to-end including 5 Gate-0 legal blockers from Claude A analysis — fixture cadastro is one operational milestone, not the full launch). PR scope: docs-only (registry ratchet + evidence doc `docs/audit/p259_frontiers_fixture_96_live_smoke.md` + #96 comment with cadastro evidence); 0 migrations, 0 code, 0 contract test changes; no auto-close keywords for #315/#96/#312. PM A/B/C/D streak +1 (3/3 Recommended in scope-lock AskUserQuestion) → **21 consecutive Recommended**. 0 NEW sediments (SEDIMENT-226.C reused as designed; SEDIMENT-235.A respected — `#315`/`#96`/`#312` never use close keywords in PR/commit/description text). **Previous header (p258 — #315 Wave 2 #310 + Wave 3 #314 SHIPPED end-to-end via 2 parallel PRs)**: **PR #371 squash `da83327f`** (`feat(governance): #315 Wave 2 #310 admin intake wizard`) ships the GP-facing intake at `/admin/governance/documents`: new React island `DocumentIntakeWizard.tsx` (modal triggered from the page header) collects Tier-1 5 fields per P1-Q6 (title · doc_type · author_label · visibility_class · description) + optional `proposer_ack_offline` (A2) + optional `proposer_member_id` UUID via Advanced section; calls Wave 1a M3 RPC `create_governance_document_intake(p_payload jsonb)` (live since p256); on success redirects to existing `/admin/governance/documents/{docId}/versions/new` editor de versão. Read-only preview of `acknowledgement_mode` (A1 per-doc_type default) + initial `status` (A2) surfaces to GP so they know what will land before submit. 54 new i18n keys in `governance.docs.intake.*` × 3 langs (parity-tested). 32 contract assertions in `tests/contracts/p258-310-wave2-admin-intake-wizard.test.mjs` with **5 forward-defense regressions**: F1 client MUST NOT send `acknowledgement_mode` (RPC computes per A1); F2 client MUST NOT send `status` (RPC computes per A2); F3 `doc_type` comes from typed enum only — no free-text path; F4 `visibility_class` comes from typed enum only; F5 success path MUST redirect to editor de versão route. **PR #372 squash `ecb111c7`** (`feat(governance): #315 Wave 3 #314 member library + /documents alias + retire legacy tab`) ships the member-facing biblioteca + 4 retirement actions: (1) new `/governance/documents/index.astro` (PT canonical) + `/en/governance/documents/index.astro` + `/es/governance/documents/index.astro` redirects mount `GovernanceLibrary.tsx` which consumes Wave 1a M3 RPC `list_governance_library(p_filters jsonb)` (live since p256) — filters doc_type+status persist in URL (`?type=…&status=…`) for shareable links; cards expose title, status badge, doc_type chip, acknowledgement_mode chip, visibility_class chip, description (truncated), effective_from + approved_at, link to existing `/governance/document/{id}` reader; (2) new `/documents.astro` + `/en/documents.astro` + `/es/documents.astro` 301 aliases per P2-Q3 ratification (`/documents` → `/governance/documents`); (3) `GovernancePage.tsx` retired the `documents` tab — `type View` narrowed from 4→3 values (`'document'|'approvals'|'changes'`); legacy `?view=documents` URL redirects via `window.location.replace` to `/governance/documents` (preserves bookmarks; locale-aware); legacy `get_governance_documents` RPC call removed; new cross-link card under the Manual view points to `/governance/documents` for discoverability; (4) `src/components/governance/DocumentsList.tsx` DELETED (no other consumer — grep verified). 45 new i18n keys in `governance.library.*` × 3 langs (parity-tested). 34 contract assertions in `tests/contracts/p258-314-wave3-member-library.test.mjs` with **7 forward-defense regressions**: no PII leak (P0-Q8) — component code MUST NOT reference `file_id`/`drive_url`/`content_html`/`pdf_url`; no table-direct SELECT on `governance_documents`/`document_versions`; `DocumentsList.tsx` MUST stay deleted; legacy `get_governance_documents` RPC call MUST NOT return; `type View` MUST NOT include `'documents'`; legacy `?view=documents` MUST redirect; cross-link `data-testid="governance-library-crosslink"` MUST exist. **Test baseline cumulative**: `2348/2262/0/86 → 2414/2328/0/86 offline` (+66/+66/0/0 — matches PR descriptions exactly: 32 #310 + 34 #314). `npx astro build` ✓ 0 new errors. **Conflict resolution recipe locked for future paired-wave PRs**: when 2 PRs both insert into i18n dicts (3 langs) + package.json whitelists at the same anchor, the rebase of the second PR against post-first-merge main yields conflict in en-US.ts + es-LATAM.ts + package.json; pt-BR.ts auto-merges (different anchor). Resolution is pure additive: keep BOTH namespace blocks in i18n, keep BOTH test file refs in package.json — no semantic conflict. Validated via local rebase test pre-PR + applied identically post-#371-merge. **Out of scope (deferred per PM directive p256 + Wave 0 ratification)**: (1) Curator-draft-access regression carry (Roberto Macêdo + Sarah Faria) — Wave 1a M2 RLS swap removed `curate_content`/`manager`/`deputy_manager` operational_role bypass; only `manage_member` is admin bypass; affected 2 curators lose direct draft SELECT via DocumentVersionEditor. Separate Wave 1b leaf will ship dedicated curator-draft-access policy or RPC. (2) `/governance/document/[id].astro` reader hardening — currently does table-direct SELECT on `governance_documents` + `document_versions` (pre-Wave-1a OPP-153.1); deferred to follow-up leaf per PM Recommended (post-Wave-1a RLS swap may have changed behavior; investigation needed). (3) `list_governance_library` payload extension for `depends_on` / templates — Wave 1b expansion deferred. (4) Frontiers fixture (#96) operational smoke — pending post-merge. (5) Wave 4 #312 audit jornada review/comment/approval persona smoke — next-in-line for v1. Waves 5/6/7 (MCP/Drive grants, evidence bundles, semantic/backfill) deferred post-v1. **`#315` umbrella STAYS OPEN** until Wave 4 #312 ships + Frontiers fixture validated + PM decides on v1-vs-post-v1 cut. **PM A/B/C/D streak +1** (4 Recommended picked: parallel #310+#314 / ship `/documents` alias / remove legacy tab+301 redirect / defer reader hardening) → **19 consecutive Recommended**. Carries unchanged from p251: (a) Cron jobid 47 first natural fire Mon 2026-05-25 13:00 UTC observability (selection reliability program, orthogonal to Governance Documents); (b) #356 Child #3 Admin UI + #357 Child #4 Cycle4 reseed (SPEC #348 v1 — orthogonal); (c) #243 E2 5-question PM decision; (d) #254 D-children 4-question PM decision; (e) #333/#334/#335 LGPD W4-5 (#333 blocked on PM sequencing call; #334 external Angeline; #335 spec ADR-0094). 0 NEW sediments. SEDIMENT-186.C respected (both new tests added to BOTH `"test"` + `"test:contracts"` whitelists pre-`npm test`). SEDIMENT-235.A respected (each PR's body uses `Closes #310.` / `Closes #314.` legitimately since these issues genuinely close; **#315** is NEVER associated with a close keyword in either PR body — the parent umbrella stays OPEN). Live smoke pending PM browser walkthrough post-this-ratchet: GP creates Frontiers via wizard → redirect to editor → opens cleanly; active member opens `/governance/documents` → sees 12 corpus docs → filter by manual → click Abrir documento → reader renders; `/documents` 301 → `/governance/documents`; legacy `/governance?view=documents` bookmark redirects to `/governance/documents`. **Previous header (p251 — **SPEC #348 Child #2 RPC body SHIPPED — track-aware booking URL routing via LRD with audit log**): Migration `20260805000030_p251_355_spec_348_v1_rpc_routing.sql` applied live + local file + repair done + shadow row cleaned + body trimmed to match live (SEDIMENT-246.B inline-comment strip). `CREATE OR REPLACE FUNCTION public.notify_selection_cutoff_approved(p_application_id uuid)` — same 1-arg signature preserved verbatim per SEDIMENT-238.C (audited `pg_get_function_arguments` + `proconfig=["search_path=public"]` + ACL pre-migration; CREATE OR REPLACE preserves grants). Extends body with track-aware `interview_booking_url` routing: leader → `cycle.interview_booking_url` (group/dual, never queries committee); researcher → LRD round-robin via `LEFT JOIN LATERAL` `MAX(dispatched_at)` lookback on `selection_dispatch_url_log` filtered to `(cycle_id, track='researcher', resolved_evaluator_id)` + filter `sc.role IN ('evaluator','lead') AND can_interview=true AND COALESCE(committee_override, member_global) IS NOT NULL` + ORDER BY `lrd.last_dispatched NULLS FIRST, sc.member_id LIMIT 1`; sub-fallback inside researcher branch when committee empty → cycle URL; defensive ELSE for unknown role_applied → cycle URL. INSERT INTO `selection_dispatch_url_log` BEFORE `PERFORM campaign_send_one_off` (audit-before-send semantic). `campaign_send_one_off` `interview_booking_url` variable now receives `v_resolved_url` (was `v_cycle.interview_booking_url` unconditionally). `admin_audit_log.metadata` gains 3 keys (`resolution_path`, `resolved_evaluator_id`, `role_applied`) + `rpc_version='p251_355'`. Return envelope gains 2 keys (`resolution_path`, `resolved_evaluator_id`). CUTOFF_NO_BOOKING_URL errcode P0020 preserved (fires only after fallback chain exhausts both per-evaluator + cycle URLs). **SPEC drift mid-session resolved via PM Option A (2026-05-24)**: SPEC §5.1 draft filtered `sc.role='researcher'` but live `selection_committee_role_check` allows only `IN ('evaluator','lead','observer')` (committee POSITION, not candidate TRACK). PM Q3 ratification (p249) also said `role='researcher'` for Child #4 seed — would violate the existing CHECK. Audit of 12 RPCs using `selection_committee.role` showed ALL filter `role='lead'` for committee-lead authority gates (no RPC filters by `evaluator`/`observer`). PM picked Option A (filter by position, no schema change): researcher branch filters `role IN ('evaluator','lead') AND can_interview=true`; Child #4 will seed Vitor+Fabricio as `role='evaluator'`. Spec §5.1 query + §8 Q3 note amended in same commit to lock the live-schema interpretation. 32 new contract assertions in `tests/contracts/p251-355-spec-348-v1-rpc-routing.test.mjs` (28 static + 4 DB-gated) including **3 forward-defense regressions locking the regression class permanently in CI**: **F1** migration body MUST NOT contain literal `sc.role='researcher'` OR `sc.role='leader'` (SPEC drift class); **F2** leader branch MUST NOT reference `selection_committee` OR `selection_dispatch_url_log` (PM directive: leader → cycle URL only, no LRD for leader v1 per SPEC §5.3); **F3** `INSERT INTO selection_dispatch_url_log` MUST appear BEFORE `PERFORM public.campaign_send_one_off` (audit-before-send). DB-gated lane: live RPC signature single overload + `is_secdef=true` + live body has v1 branches via `_audit_list_public_function_bodies()` helper + `selection_dispatch_url_log` PostgREST surface + migration version registered. Test added to BOTH `"test"` and `"test:contracts"` whitelists (SEDIMENT-186.C respected). Test baseline 2081/2009/0/72 → **2113/2037/0/76 offline** (+32/+28/0/+4 skip — matches the test exactly: 28 static pass + 4 DB-gated skip offline). With-DB env 32/32 pass 0 skip. **Live smoke** via SEDIMENT-226.C JWT-injection (`PERFORM set_config('request.jwt.claims', ...)` simulating Vitor) on one of the 5 already-dispatched cycle4 apps: RPC returned `{success:true, email_sent:false, reason:'already_sent', previously_sent_at:'...'}`; `selection_dispatch_url_log` count remained 0 (idempotency short-circuits before INSERT); zero side effects. `check_schema_invariants()` 19/19=0 throughout. `astro build` ✓ 0 new errors. **0 NEW escalation-eligible sediments**; **NEW lesson SEDIMENT-250.B documented** (live-query `pg_constraint` / distinct values BEFORE assuming spec literal SQL strings are valid against current schema; mirrors SEDIMENT-244.A handoff-narrative pattern in the schema-literal domain). **SEDIMENT-238.A recurred** (apply_migration MCP shadow row `20260524212026` cleaned via DELETE — three-step apply ritual reinforced: write file → repair canonical → DELETE wall-clock shadow). **SEDIMENT-246.B recurred** (apply_migration MCP silently stripped trailing inline `-- CHECK aligned: ('researcher','leader')` comment after `v_app.role_applied,` line; minimum-diff fix per p246 sediment: stripped the comment from local file to match live byte-for-byte at 8583 chars / 7809 normalized chars). PM A/B/C/D streak +1 (Option A live-schema filter) → **18 consecutive Recommended**. P162 entry #218 RESOLVED-#355 anchors. **NEXT dispatch (PM call) post-p251**: (1) **#356 Child #3 Admin UI** — unblocked by #354 + #355; can ship in parallel (members.interview_booking_url editable on `/admin/members/[id]` + 6 i18n keys × 3 langs). (2) **#357 Child #4 Cycle4-2026 committee reseed** — unblocked by #354+#355; seed Vitor+Fabricio with `role='evaluator'` (NOT `'researcher'` per PM Option A correction); `members.interview_booking_url` stays NULL (PM populates later via #356 admin UI). Zero immediate behavior change — committee exists but routing still falls back to cycle URL until URLs populated. (3) Cron jobid 47 first natural fire **Mon 2026-05-25 13:00 UTC** observability. (4) #243 E2 5-question PM decision; #254 D-children 4-question PM decision. (5) #333/#334/#335 LGPD W4-5 still blocked. (6) LOW carries unchanged: OPP-248.A Fabiano / OPP-247.A colspan / OPP-245.A Playwright / OPP-244.A administrative onboarding ADR. **Previous header (p250 — SPEC #348 Child #1 Foundation SHIPPED via PR #358 — booking URL per evaluator DDL)**: Migration `20260805000029_p250_354_spec_348_v1_foundation_booking_url_per_evaluator.sql` applied live + local file + repair done. DDL ships 3 schema artifacts: (1) `ALTER TABLE public.members ADD COLUMN interview_booking_url text` (nullable; personal evaluator calendar/booking pool URL — user-set via admin form in Child #3, validation app-level per Q2); (2) `ALTER TABLE public.selection_committee ADD COLUMN interview_booking_url text` (nullable; optional cycle-scoped override of the member-global URL); (3) `CREATE TABLE public.selection_dispatch_url_log` with 9 columns (id, application_id, cycle_id, track CHECK('researcher','leader'), resolved_url, resolution_path CHECK('committee_override','member_global','cycle_fallback'), resolved_evaluator_id nullable, dispatched_at, organization_id) + 4 FKs + 2 indexes (app_idx for app lookups + partial cycle_round_robin_idx WHERE track='researcher' for LRD picker in Child #2) + V4 canonical RLS pair (rpc_only_deny_all + selection_dispatch_url_log_v4_org_scope) — the V4 pair replaces spec §4.1 draft policy `rls_can('view_selection') OR rls_can('manage_member')` after mid-session audit revealed `view_selection` is NOT in `engagement_kind_permissions` (would silently deny all reads except `manage_member` holders); PM ratified Option A V4 canonical pair (matches selection_applications/committee/cycles/evaluations/interviews/ai_suggestions/diversity_snapshots; all reads via SECDEF RPC); spec doc amended in same commit with explanatory note documenting the rejected `view_selection` path. Sanity DO block raises EXCEPTION on missing artifacts (7 EXISTS checks: 2 columns + 1 table + 2 indexes + 2 policies). NOTIFY pgrst trailer. ZERO behavior change — no RPC bodies touched (Child #2 / #355 scope). 27 new contract assertions in `tests/contracts/p250-354-spec-348-v1-foundation.test.mjs` (23 static + 4 DB-gated): file existence + header + spec doc anchor + 2 ALTER + 2 COMMENT + CREATE TABLE IF NOT EXISTS + 9-column declaration + 2 CHECK enum exactness + resolved_evaluator_id nullable + table COMMENT + app_idx exact + cycle_round_robin_idx exact partial + RLS ENABLE + 2 RLS policy declarations + sanity DO 5 RAISE strings + NOTIFY trailer + **4 forward-defense regressions locking the regression class permanently in CI**: (a) active SQL must NOT contain CREATE OR REPLACE FUNCTION or CREATE FUNCTION (Child #2 scope guard); (b) active SQL must NOT reference `rls_can('view_selection')` (locks the spec-divergence regression — strips line comments so header RLS NOTE can document the rejected path without tripping); (c) members.interview_booking_url + selection_committee.interview_booking_url must NOT carry DB CHECK (Q2 ratification: validation app-level); (d) migration must NOT touch `selection_cycles.interview_booking_url` (cycle URL stays intact for fallback + leader default). DB-gated lane (4): PostgREST column-probe for members + selection_committee + table (with all 9 columns named in select) + `supabase_migrations.schema_migrations` row presence via `_audit_list_schema_migrations` helper. ADR-0012 allowlist updated: `members.interview_booking_url` added to `STORAGE_ONLY_ALLOWLIST` with rationale "Personal evaluator calendar/booking pool URL (SPEC #348); user-set via admin form, no derivation source" — caught by full `npm test` per ADR-0012 contract enforcing every new column on members/engagements/initiatives ships either with a maintenance trigger OR with an explicit storage-only declaration. Test baseline **2054/1986/0/68 → 2081/2009/0/72 offline** (+27/+23/0/+4 skip). Invariants 19/19=0 throughout. astro build ✓ 0 new errors. Cron jobid 47 pre-fire arm reverified at boot: schedule `0 13 * * 1` + active=true + 0 prior runs in `cron.job_run_details` + first natural fire pending Mon 2026-05-25 13:00 UTC (~16.5h post-this-session at boot 20:33 UTC; ~13h post-PR-merge). p249 uncommitted docs (SPEC + P162 + this header + handoff p249 + MEMORY) bundled into the same PR per PM boot directive ("Commitar os docs p249 junto com o primeiro PR docs/governance apropriado ou em PR docs-only, conforme PM decidir"). **NEXT dispatch (PM call) post-p250**: (1) **#355 Child #2 RPC body** — unblocked by #354; same 1-arg `notify_selection_cutoff_approved` signature must be preserved per SEDIMENT-238.C (probe `pg_get_function_arguments` BEFORE migration); schema is ready for `INSERT INTO selection_dispatch_url_log` + LRD `LEFT JOIN LATERAL` pick. (2) **#356 Child #3 Admin UI** — unblocked by #354; can ship in parallel with #355. (3) **#357 Child #4 Cycle4 reseed** — still blocked by #354 + #355 (defensive ordering). (4) Cron jobid 47 first natural fire Mon 2026-05-25 13:00 UTC observability — verify cycle4 researcher final régua remains target=258.12 (or shifts if Eduardo/Henrique cohorts approved in the meantime). All other carries unchanged from p249 (#333/#334/#335 LGPD W4-5; OPP-248.A Fabiano LOW; OPP-247.A colspan LOW; OPP-245.A Playwright LOW; OPP-244.A administrative onboarding ADR LOW; OPP-246.A still closed p248). SEDIMENT-186.C respected (test added to BOTH `"test"` + `"test:contracts"` whitelists pre-`npm test`). SEDIMENT-235.A respected (PR title uses neutral wording — #354 child of #348; parent stays open until #355/#356/#357 also ship). SEDIMENT-238.C respected (no CREATE OR REPLACE FUNCTION in this migration; lesson applies to #355 next). SEDIMENT-244.A respected (live-queried `engagement_kind_permissions` + existing `selection_*` RLS policies BEFORE planning RLS policy text — caught the `view_selection` non-existence at SCOPE-LOCK phase, not at planning phase). 0 NEW sediments (escalation-eligible) this session. **SEDIMENT-238.A recurred** (caught by CI on first push of PR #358 — `apply_migration` shadow row `20260524203814` was not deleted after `supabase migration repair`; cleanup applied via `DELETE FROM supabase_migrations.schema_migrations WHERE version = '20260524203814'`; CI re-run passed; same known pattern from p238/p240/p241/p246, no new letter assignment, but the three-step apply_migration ritual now adds an explicit "Step 3: DELETE shadow row at wall-clock timestamp"). PM A/B/C/D streak +1 (Option A V4 canonical RLS pair picked on mid-session spec divergence) → **17 consecutive Recommended**. P162 entry #217 RESOLVED-#354 anchors (Sediment subsection documents the recurrence + cleanup recipe). **Previous header (p249 — SPEC #348 v1 DISPATCH — Booking URL per Evaluator / Committee + 4 ready-leaf children spawned)**: Per PM boot directive ("desenhar v1 com booking_url por avaliador/comitê; researcher -> Vitor/Fabricio individual; leader -> Núcleo/dupla; deixar reschedule/cancel fora do v1"), SPEC #348 v1 drafted at `docs/specs/SPEC_348_BOOKING_URL_PER_EVALUATOR.md` (~310 lines, 10 sections covering v0 baseline + current state audit + v1 IN/OUT scope + schema proposal Option B Híbrido + routing decision tree + test plan + 3 PM-ratified decisions + migration plan + cross-ref). 3 open decisions surfaced via AskUserQuestion and all 3 ratified as Recommended defaults: Q1 (routing policy) → **LRD round-robin** via `selection_dispatch_url_log` `LEFT JOIN LATERAL ... MAX(dispatched_at)` lookback; Q2 (URL field) → **`members.interview_booking_url text`** nullable, app-level regex `^https?://`, no DB CHECK (preserves grep parity with `selection_cycles.interview_booking_url`); Q3 (cycle4 reseed) → **Seed committee Vitor+Fabricio researcher with URLs NULL** as Child #4 DML migration. 4 GitHub child issues spawned with concrete acceptance criteria + execution dependencies: **#354** Foundation DDL (ALTER members + ALTER selection_committee + CREATE selection_dispatch_url_log table with 7 columns + 2 indexes + RLS + SELECT policy gated on view_selection/manage_member) — independently shippable; **#355** RPC body extension (`CREATE OR REPLACE FUNCTION notify_selection_cutoff_approved` with track-aware routing per §5.1 decision tree, blocked by #354, forward-defense regressions lock LRD policy + "leader → cycle-only" PM rule); **#356** Admin UI (editable form field on `/admin/members/[id]` + 6 i18n keys × 3 langs, blocked by #354); **#357** Cycle4-2026 committee reseed (one-shot DML INSERT for Vitor + Fabricio researcher with URLs NULL + `NOT EXISTS` idempotency + audit row + sanity DO, blocked by #354 + #355 defensive ordering so new routing logic exists before committee transitions from empty to non-empty). Substrate audit verified live: `selection_committee` for cycle4-2026 is EMPTY (p226 seed deprioritized; PM operates via SEDIMENT-226.C JWT injection); de facto evaluators are Vitor (51 evals) + Fabricio (40 evals); `members` has no booking URL column today; `notify_selection_cutoff_approved` raises `CUTOFF_NO_BOOKING_URL` errcode P0020 when cycle URL is empty. Short-term v0 decision (Step 1 from #348 4-step roadmap) preserved: `cycle4-2026.interview_booking_url = 'https://calendar.app.google/XPiGWLh9JaLVFKJc6'` stays as leader-track URL AND researcher-track fallback; v1 is purely additive. Out of v1 (deferred to Steps 3+ children — not spawned this session): Step 4 Hub reschedule/cancel; Google Calendar API integration; per-evaluator availability matrix; round-robin alternatives (least-loaded, weighted, sticky); auto-trigger from cron jobid 47 → cutoff dispatch (p230 fast-follow governance call); `cycle.interview_booking_url` deprecation. WATCH-240.C cron observability state pre-fire verified this session: cron jobid 47 schedule=`0 13 * * 1` (Mon 13:00 UTC; boot directive said 13:00; handoff p243 said 14:00 — cron schedule confirms 13:00) + active=true + 0 prior runs in `cron.job_run_details` + first fire Mon 2026-05-25 13:00 UTC ≈ 17h post-this-session. No code/migration changes this session (spec-only delivery + GH issue spawn + doc updates). Test baseline UNCHANGED 2054/1986/0/68 offline. Invariants 19/19=0 throughout. P162 entry #216 DISPATCH-SPEC-348 anchors the disposition + 4-child execution plan + verify-on-next-boot checklist. SEDIMENT-235.A respected (child issues #354-#357 reference parent #348 as "Parent" NOT "closes" so parent #348 stays open until ALL 4 children ship; gh issue comment uses "ready for implementation" framing, no auto-close keywords). 0 NEW sediments this session. PM A/B/C/D streak +1 (all 3 Recommended defaults ratified in single AskUserQuestion turn) → **16 consecutive Recommended**. **Previous header (p248 — OPP-246.A CLOSED — submit_evaluation canonical final_score (drops naïve sum race) + Francisleila cleanup**): PM Option C+B picked + executed end-to-end on same UTC day as p247 ship. Audit doc `docs/audit/OPP_246_A_FRANCISLEILA_FINAL_SCORE_DRIFT.md` (440+ lines) found drift root cause: `submit_evaluation` (leader_extra + interview branches) wrote `final_score = obj + interview + leader_extra` (naïve sum) in its own UPDATE, OVERRIDING the trigger-driven canonical `compute_application_scores` call (CR-047 weighted `leader_score = research × 0.7 + leader_extra × 0.3`). Drift was isolated to **1 row** (Francisleila cycle4 leader, screening, `final_score: 309 → canonical 158.30`); all 8 approved leaders all-time were clean (self-healed via `submit_interview_scores` modern path that already calls compute_application_scores). Migration `20260805000028_p248_opp_246_a_submit_evaluation_canonical_final_score.sql` (313 lines): (Step C) CREATE OR REPLACE FUNCTION submit_evaluation — same 6-arg signature with 3 DEFAULTs preserved, SECDEF+search_path=public preserved per minimum-diff, interview + leader_extra branches removed naïve-sum `final_score = ...` inline UPDATE + added explicit `PERFORM public.compute_application_scores(p_application_id)` defense-in-depth; (Step B) DO block `PERFORM compute_application_scores(Francisleila.id)` + `INSERT INTO admin_audit_log` with `action='selection.final_score_canonical_reconciliation'` + full metadata envelope (before_final=309, after_final=158.30, canonical_expected=158.30, rpc_version=p248, audit_doc path), idempotent on re-apply; Sanity DO RAISES if any leader row drift > 0.5pt survives (atomic rollback guarantee); NOTIFY pgrst. Live smoke post-apply: Francisleila `final_score 309 → 158.30` ✓ matches canonical leader_score exactly; audit row created at 19:43:59 UTC; 0 leader drift across all cycles (1 researcher Fabiano partial-eval edge case documented as OPP-248.A LOW, NOT in any cohort). **20 contract assertions** in `tests/contracts/p248-opp-246-a-submit-evaluation-canonical-final-score.test.mjs` (14 static + **3 forward-defense regressions locking the regression class permanently in CI**: no `UPDATE...SET interview_score=..., final_score=...` regex; same for leader_extra; submit_evaluation body code-only must NOT contain ANY `final_score = ` assignment); + 3 DB-gated (Francisleila canonical / audit row present / 0 leader drift goal). Legitimate test contract update in `selection-blind-eval.test.mjs:206-211` to assert canonical delegation pattern (`PERFORM compute_application_scores`) instead of inline naïve-sum formula. Test baseline 2034/1967/0/67 → **2054/1986/0/68 offline** (+20/+19/+1 skip). Invariants 19/19=0. astro build ✓ 0 new errors. **Cohort contamination risk NEUTRALIZED**: structural bug fixed + Francisleila reconciled + future submit_evaluation calls cannot re-introduce drift; leader final régua stays clean as cohort grows toward dynamic activation at n≥10. PM stated risk vector ("isso pode contaminar uma futura régua dinâmica de líderes se for drift antigo") fully closed. **0 NEW sediments** (238.C respected via pg_get_function_arguments + prosecdef + proconfig probe BEFORE migration write — no atomic-rollback this session; 238.A no shadow rows; 186.C whitelist pre-test; 235.A internal P162 tag, neutral PR title). P162 entry #215 RESOLVED-OPP-246.A. **NEW OPP-248.A** (deferred LOW): Fabiano Bressiani (cycle3-2026 researcher, rejected, partial evals) has final_score=35 with research_score=NULL (canonical formula returns NULL); NOT in any cohort, no operational impact. PM A/B/C/D streak +1 → **15 consecutive Recommended**. **Previous header (p247 — #229b Frontend SHIPPED — per-candidate Final + Interview score visibility on /admin/selection (PR2 of 2-PR split per PM Option A)**: Frontend leaf consumes p246 Foundation payload to surface what was already computed server-side. `src/pages/admin/selection.astro` gains new helper `finalScoreChip(row)` (~25 lines) consuming the track-resolved `r.final_score_pert_*` per-app fields populated by p246 backend (`_compute_pert_cutoff_core` UPDATE scoped via `AND role_applied = p_role`); the chip reuses p245's `bandClassify` + `pertFullTooltip` + delegates classification (no parallel classifier) + handles `method === 'disabled'` branch (mirrors `leaderExtraChip` pattern, surfacing "Régua final: n=N<10" when cohort_n<10 for that track). 2 new TH headers (`Nota Entrevista` sortable + `Final` sortable) inserted between `data-sort="leader_score"` (Líder) and `data-sort="rank"` (Rank) — scores cluster visually; existing `Entrevista` column (next-scheduled date/time + HOJE badge) stays untouched and operationally distinct. 2 new TDs in row render: interview_score plain number (PM rule: "Não criar régua para entrevista" — `interview_score` is just a number to display), final_score + finalScoreChip(r) below. `colspan="13"` → `colspan="15"` bumped in both error + empty state placeholders. 6 new i18n keys × 3 langs (18 inserts: `colInterviewScore` + `colInterviewScoreHint` + `colFinal` + `colFinalHint` + `finalScoreCutoffTooltip` + `finalScoreDisabledChip`); reuses `pertBandAbove/Within/Below` labels already shipped p245. T object (frontmatter + fallback inside script) wires 2 helper-consumed keys. **35 contract assertions** in `tests/contracts/p247-229b-frontend-per-candidate-final-score.test.mjs` (28 static + 6 i18n parity + **3 forward-defense regressions locking PM "do-not" rules**: (1) `interview_score` TD must NOT call any chip helper / `bandClassify` / régua classifier — locks "no régua for interview"; (2) `finalScoreChip` body must NOT reference `currentCycle?.pert_cutoff` (PERT Objetiva = gate to interview, separate dimension) NOR `leader_extra_cutoff` (separate dimension); (3) no `interview_score_pert_*` columns or `interviewScoreChip` helper anywhere on the page — locks against future drift conflating dimensions). Test baseline **1999/1932/0/67 → 2034/1967/0/67 offline** (+35 pass). Invariants 19/19=0 throughout. astro build ✓ 0 new errors. Frontend-only PR, 0 migrations, 0 schema changes, 0 RPC changes. P162 entry #214 RESOLVED-#229b-FRONTEND anchors. NEW OPP-247.A (low priority): pre-existing 1-off in colspan preserved (was "13" for 14 actual TDs; now "15" for 16 actual TDs; out of scope to fix this PR). SEDIMENT-186.C respected (test added to BOTH whitelists in package.json BEFORE running npm test). SEDIMENT-235.A respected (#229b is internal P162 tag — no GitHub issue; PR title uses neutral phrasing). 0 NEW sediments. PM A/B/C/D streak +1 (PM Option A picked for column layout, executed end-to-end with 3 forward-defense regressions making PM rules permanently enforced by CI) → **14 consecutive Recommended**. **Previous header (p246 — #229b Foundation SHIPPED — final-score PERT régua by track (PR1 of 2-PR split per PM Option A)**: Migration `20260805000027_p246_229b_final_score_pert_by_track.sql` adds 6 new `final_score_pert_*` columns on `selection_applications` (track-resolved per app at recompute time); `_compute_pert_cutoff_core` extended with `v_is_final_score` branch (UPDATE track-scoped via `AND role_applied = p_role` — separates researcher cohort ≈180-290 scale from leader cohort ≈220-310 scale; fallback target lookup also track-aware); `recompute_all_active_pert_cutoffs` now orchestrates 4 dimensions (objective + leader_extra + final_score×researcher + final_score×leader); `get_selection_dashboard.cycle` gains `final_score_cutoff_researcher` + `final_score_cutoff_leader` blocks; `get_selection_dashboard.applications[]` chunk-2 gains `interview_score` + 6 `final_score_pert_*` per-app fields (chunk-2 placement preserves PG 100-arg cap). Live smoke post-recompute: cycle4 researcher final régua **target=258.12 band=[232.31, 283.93] cohort_n=19 method=dynamic** (rows_updated=34); leader cohort_n=6<10 method=disabled (rows_updated=4, expected per p219 Phase 1 threshold); cycle3-b2 mirrors. Per-app verification: Flavio researcher final=288.50 → target=258.12; Francisleila leader final=309 → target=NULL disabled (track separation works). 16 contract tests in `tests/contracts/p246-229b-final-score-pert-by-track.test.mjs` (15 static + 1 DB-gated suite skipping offline) cover schema columns + 5-arg signature with all 4 DEFAULTs preserved + v_is_final_score discriminator + track-scoped UPDATE regression catch + track-aware fallback + recompute 4-dimension orchestration + 2 cycle blocks scoped per track + 7 per-app fields + chunk-2 placement + SECDEF/search_path + NOTIFY pgrst + 3 forward-defense regressions honoring PM "do-not" constraints (no `interview_score_pert_*` columns; exactly 1 `ELSIF v_is_final_score`; no `UPDATE selection_applications SET status =` honoring no-auto-decisions rule). Test baseline **1983/1917/0/66 → 1999/1932/0/67 offline** (+16/+15/+1 skip). Invariants 19/19=0. SEDIMENT-238.A applied (3-shadow consolidation via CTE merge into canonical `20260805000027`). **NEW SEDIMENT-238.C escalation candidate** (rolled-back first apply_migration revealed): `CREATE OR REPLACE FUNCTION` MUST preserve every parameter DEFAULT clause from original signature — Postgres rejects with "cannot remove parameter defaults from existing function"; run `pg_get_function_arguments(oid)` BEFORE writing migration. **NEW OPP-246.A** (deferred): audit Francisleila `final_score=309` drift vs declared composite formula (returns 158.3 = research×0.7 + le×0.3 expected, NOT 309 = naive sum). P162 entry #213 RESOLVED-#229b-FOUNDATION anchors. **PR2 p247 Frontend next session** — consumes new payload to render per-candidate Interview score + Final score + Final régua classification chips. PM A/B/C/D streak +1 → 13 consecutive Recommended. **Previous header (p245 — #229a per-candidate PERT classification + leader_extra dimension visibility SHIPPED on /admin/selection)**: PM-dispatched read-surface follow-on to p232 #229 Phase 2 (RPC/data foundation already shipped; per-candidate UI visibility was the gap). `src/pages/admin/selection.astro` gains 4 new helpers (`bandClassify` returning structured `{color, label:'above'|'within'|'below'|'none', delta}` + `formatDelta` + `pertBandChip` + `pertFullTooltip` enriched with 6 placeholders + `leaderExtraChip` handling disabled vs classified branches). 2 per-app TDs refactored: research_score TD keeps band color + appends explicit band-position chip with delta + enriched tooltip; **leader_score TD strips `bandColorClass(r.leader_score, currentCycle?.pert_cutoff)` (PM rule: "Não comparar líder contra a régua objetiva")** + appends leader_extra dimension chip wired to `leader_extra_cutoff` (with disabled-reason `Régua líder: n=6<10` when cycle cutoff method=disabled per cohort_n<10 threshold per p219 Phase 1 design). 5 new i18n keys in 3 dicts (pt-BR + en-US + es-LATAM = 15 total). 30 new contract assertions in `tests/contracts/per-candidate-pert-classification.test.mjs` (helper signatures + return shapes + delta logic + tooltip placeholders + disabled-branch behavior + LE prefix marker + 2 TD wiring assertions + 2 forward-defense regressions against the PM-rule-violating `bandColorClass(r.leader_score, currentCycle?.pert_cutoff)` AND `bandClassify(r.leader_score, currentCycle?.pert_cutoff)` patterns + cycle chip presence baseline + i18n parity across 3 langs + placeholder presence in all 3 + T object surface + fallback). Test baseline **1953/1887/0/66 → 1983/1917/0/66 offline** (+30 tests / +30 pass). Live smoke 30/30 PASS isolated + npm test full pass + astro build 0 errors + invariants 19/19=0. Frontend-only PR, 0 migrations, 0 RPC changes (data already populated p242 recompute). Cycle4-2026 state at audit: 38/38 apps have full objective PERT (target=155.42, band=139.88-170.96, cohort_n=19, method=dynamic), 3 apps have leader_extra_pert_score populated, cycle leader_extra_cutoff=disabled (cohort_n=6<10 — expected). P162 entry #212 RESOLVED-#229a-READ-SURFACE anchors. **NEW OPP-245.A** (deferred): browser-driven test for /admin/selection visual rendering would catch CSS regressions (currently static contract test only). PM A/B/C/D streak +1 → 12 consecutive Recommended. **Previous header (p244 — WATCH-240.B closed as audit-only no remediation)**: `docs/audit/CYCLE3_B2_LATE_EVAL_P244_WATCH_240_B.md` ships finding (1) the p240 backfill touched 0 cycle3-2026-b2 rows live (all 14 backfill audit entries target cycle4-2026 exclusively; P162 #208 line 2466 "10 cycle4 + 4 cycle3-b2" was an attribution error that propagated through p241/p242/p243 handoffs without live verification — the CTE scope in migration `20260805000025` does include both cycles, but the idempotency guard `f.target_status <> a.status` filtered cycle3-b2 to 0 candidates because both apps were already past `interview_done` via terminal `status='approved'`); (2) cycle3-2026-b2 has exactly 2 apps both already `approved` via `application.status_manual_advance` on 2026-05-09 — Herlon Alves de Sousa (`d1d72a91…`, leader, obj=252.00) + João Uzejka dos Santos (`a06b9a26…`, researcher, obj=137.00) — administrative pre-cycle4 onboarding cases per PM directive "extend formal acceptance pre-Cycle 4"; (3) the "4 late-evaluation" reframed as Herlon's 4 non-interview eval rows submitted 2.4-3.2 days post-interview (1 leader_extra Vitor synchronous + 1 leader_extra Fabricio +2.4d + 1 objective Fabricio +2.4d + 1 objective Vitor +3.2d), all 4 legitimate PM/curator post-hoc submissions in the administrative onboarding window; (4) no structural remediation needed because cycle3-b2's standard funnel ordering does not apply to apps that bypassed via manual_advance. Mid-session scope redirect from PM Option A "audit + dispatch packet" to Option A1 "audit-only" after live data revealed dispatch moot. Docs-only PR, 0 migrations, 0 schema changes, 0 status writes. Test baseline UNCHANGED 1953/1887/0/66. Invariants 19/19=0. P162 entry #211 RESOLVED-WATCH-240.B anchors the disposition + correcting footnote on #208 attribution. **1 NEW SEDIMENT-244.A** (handoff narrative propagation drift — when handoff carry text claims "N rows in cycle/table X", live-query source-of-truth table BEFORE planning dispatch work around it; mirrors SEDIMENT-242.A in handoff-text domain). PM A/B/C/D streak +1 → 11 consecutive Recommended. **Previous header (p242 — WATCH-240.C closed as informational via docs-only audit)**: `docs/audit/CYCLE4_PERT_CUTOFF_P242_WATCH_240_C.md` ships with status × band crosstab + 8-app detail of stuck-in-screening (5 above-target Henrique/João/Francisleila/Cristiano/Edinan + 1 in-band borderline Hector + 2 below-band Alexandre/Carla) + cron `recompute-pert-cutoffs-weekly` posture table + verify-after-first-fire SQL recipe + per-app `notify_selection_cutoff_approved(app_id)` dispatch workflow (audit-corrected — NOT `finalize_decisions`; that's for FINAL committee decisions AFTER interviews; the cutoff→interview_scheduled advance happens naturally via candidate-booking webhook chain). Manual `recompute_all_active_pert_cutoffs()` invocation 2026-05-24 15:40:38 UTC populated all 38 cycle4 + 2 cycle3-b2 apps with `pert_target_score`/`pert_band_lower`/`pert_band_upper`/`pert_calc_at` (cycle4 researcher target=155.42, band=[139.88, 170.96], cohort_n=19; leader_extra method=disabled cohort_n=6<10). Cron jobid 47 first fire Mon 2026-05-25 13:00 UTC ≈ 22h post-audit — 0 runs in 30d is *not a bug*, just brand-new (installed p228 Sat 2026-05-23). 11 above-target = 6 already in interview_* (progressing via p240 trigger + p241 hoist) + 5 in screening (PM committee action: `notify_selection_cutoff_approved(app_id)` per-app from authenticated session). 0/38 emails sent — orthogonal carry, will fire naturally once PM dispatches per-app. P162 entry #210 RESOLVED-WATCH-240.C anchors. SEDIMENT-235.A respected (PR title `Closes WATCH-240.C` is internal tag). **Previous header (p241 — WATCH-240.A defense-in-depth shipped via PR #344)**: migration `20260805000026_p241_watch_240_a_submit_interview_scores_relax_status_gate.sql` hoists `UPDATE selection_interviews SET conducted_at = now()` out of the `IF v_all_interviewers_submitted` branch of `submit_interview_scores()` — with idempotency guard `IF v_interview.conducted_at IS NULL`. The p240 trigger `trg_sync_interview_to_app_status` now reliably fires on partial-submission paths (1-of-N interviewers) and advances the parent app from `interview_pending` → `interview_done` going forward. Same-signature `CREATE OR REPLACE` (no consumer break). 15 contract tests (11 static + 2 forward-defense + 2 DB-gated; baseline `1938/1874/0/64 → 1953/1887/0/66` offline +15/+13/+2). Live smoke: body-hash drift check passes (md5 normalized=`f9cf4bde13a3145f6702d5543661cf79` matches local file byte-for-byte); hoist position 3525 < all-submitted check 3722 < all-submitted branch 4470 < `status='completed'` 4554 < `status='final_eval'` 5437 (chronological order confirmed); forward-defense regex confirms no `conducted_at = now()` substring inside all-submitted branch; single overload; direct invocation reaches the auth gate at line 24 with signature `(uuid,jsonb,text,text,jsonb)` intact. Invariants 19/19=0. SEDIMENT-238.A applied cleanly (canonical INSERT WITH statements copied FROM shadow + DELETE shadow); SEDIMENT-238.B respected (live prosrc matches file body); SEDIMENT-186.C respected (new test added to BOTH whitelist scripts before npm test). P162 entry #209 RESOLVED-WATCH-240.A anchors the disposition. **Previous header (p240 — #251 selection interview status transition gap CLOSED)**: PM reproduced Luíse Quintana + William Junio (researcher) labelled `Aguardando Entrevista` in `/admin/selection` despite completed interviews + submitted interview evals. Migration `20260805000025_p240_251_interview_status_transition_trigger.sql` adds AFTER INSERT OR UPDATE OF (status, conducted_at) trigger on `selection_interviews` + 14-row backfill (10 cycle4-2026 + 4 cycle3-2026-b2; audit reason `p240_251_interview_status_transition_backfill`) + sanity DO + terminal-status no-op guard per PM directive (trigger nunca toca terminal). 13 contract tests (11 static + 2 DB-gated; baseline `1925/1863/0/62 → 1938/1874/0/64` offline). Live smoke 3/3 PASS (Luíse + William researcher → `interview_done`; Rafael Bellotti → `interview_scheduled`); William *leader* preserved as `rejected` (terminal guard fires). Invariants 19/19=0. Selection reliability cluster narrative updated below. P162 entry #208 RESOLVED-#251 anchors the disposition. SEDIMENT-186.C reminder: package.json `npm test` whitelist requires NEW test files to be added to both `"test"` + `"test:contracts"` scripts — caught this session (initial npm test ran 1925/1863 instead of 1938/1874 because the new file wasn't in whitelist). **Previous header (p239b — #332 W3 retroactive Art. 18 §IV remediation CLOSED end-to-end via PR #342 3-commit squash)**: operator surface ship + accessor_id FK hotfix + actual PM dispatch). Wave 3 closes with the audit chain anchored: PM dispatched the interim_v1 notification email to Eduardo Luz (`eduardoluz.pm@gmail.com`) at 2026-05-24T00:29:00-03:00 via `nucleoia@pmigo.org.br`, then invoked `lgpd_record_retroactive_notification` MCP tool from authenticated MCP-Claude session — returned `pii_access_log.id = ee6a1541-bef6-4f7c-be69-c1e7efbc6568` (context=`lgpd_art_18_retroactive_notification`, accessor_id=`880f736c` resolved to Vitor's members row, target_member_id NULL because applicant-only, accessed_at=`2026-05-24 03:29:00+00` matches the real send moment). PR #342 shipped 3 commits: (1) operator surface — 2 MCP tool wrappers + version bump (count 299→301, ef_version 2.79.1→2.80.0); (2) hotfix migration `20260805000024` — `accessor_id := auth.uid()` → `v_caller_member_id` (p238b RPCs violated FK `pii_access_log_accessor_id_fkey` → `members(id)` on first PM-authenticated dispatch; service-role smoke at p238b missed it because gate ladder fired before INSERT); (3) ratchet — this update + GH issue close. 9 forward-defense contract assertions in `tests/contracts/p239b-332-hotfix-accessor-id-fk.test.mjs` lock the FK-column-source regression class. Audited the other 4 `pii_access_log` writers (`_log_application_pii_access`, `log_pii_access`, `log_pii_access_batch`, `list_initiative_engagements_by_kind`) — all canonical (use members.id). 30-day Eduardo response window opens 2026-05-24; tacit retention applies after; if deletion requested, PM invokes `lgpd_execute_retroactive_deletion` MCP tool (preview default → confirm:true) → tracked under #333 sequencing call. PM saved Gmail "Sent" PDF evidence to org offline records (PII not committed to repo). Test baseline 1879→1925/1820→1863/0/62 offline (+46 across p238 #331 + p238b #332 infra + p239b operator+hotfix). Invariants 19/19=0 throughout. SEDIMENT-235.A respected (PR description #342 used `Closes #332` only after the GENUINE close criteria were satisfied — dispatch + RPC log both done; this is the intended use, not the auto-close-regression class). SEDIMENT-238.A applied cleanly on hotfix migration. SEDIMENT-239b.A NEW LEARNING: contract tests for SECDEF RPCs that INSERT into FK-constrained tables MUST assert the source of every FK column, not just gate-ladder presence + context literal. P162 entry #207 RESOLVED-#332-OPERATOR carries the full operator runbook + close-evidence. **Previous header (p238b — Wave 3 infrastructure shipped)**: Migration `20260805000023_p238_332_lgpd_art18_retroactive_deletion_log.sql` ALTERs `pii_access_log` ADD COLUMN `deletion_artifacts jsonb` (nullable; only set on Art. 18 deletion events); CREATEs `lgpd_record_retroactive_notification(p_application_id, p_template_version, p_lang, p_notification_method DEFAULT 'email', p_dispatched_at DEFAULT NULL)` SECDEF gated on `can_by_member('manage_member')` — writes pii_access_log row with `context='lgpd_art_18_retroactive_notification'`; CREATEs `lgpd_execute_retroactive_deletion(p_application_id, p_video_id, p_deletion_reason, p_drive_deletion_ref DEFAULT NULL)` SECDEF gated on `manage_member` — atomically clears `pmi_video_screenings.transcription` + writes pii_access_log row `context='lgpd_art_18_deletion_executed'` with full `deletion_artifacts` jsonb (video_id, application_id, pillar, question_index, old_transcription_len, drive_file_id, drive_file_name, drive_deletion_ref, deleted_at, deletion_reason, reversible=false, lgpd_basis='Art. 18 IV'). Idempotent no-op rejected (transcription IS NULL guard) + cross-app deletion guard + `p_deletion_reason` minimum length (8 chars). Sanity DO + single-overload defense + NOTIFY pgrst. PM-approved interim notification template `docs/audit/lgpd-art11-remediation/notification_eduardo_luz_p238_interim.md` (pt-BR primary 2050+ chars + en-US fallback 1900+ chars; cites Art. 11 §I + Art. 18 §IV + Art. 9; metadata header includes application_id `e780d8a9-55e0-4a6c-9370-4acc24a9619d` + video_id `6afb7e26` + drive_file_id `14bA9rCe` + 30-day window + tacit-retention rule + operational checklist of 7 steps PM follows to dispatch + log + close). 20 new contract assertions in `tests/contracts/lgpd-art-18-retroactive-deletion.test.mjs` (11 static migration + 4 static docs + 2 forward-defense + 3 DB-gated). Test baseline **1879/1820/0/59 → 1899/1837/0/62 offline** (+20 / +17 pass / +3 skip). Live smoke 2/2 PASS via direct DO-block (both RPCs gate correctly — service-role context returns "Unauthorized: no member record"). Invariants 19/19=0 throughout. SEDIMENT-238.A APPLIED CLEANLY this time (shadow row `20260524025931` cleaned via canonical INSERT WITH `statements` column copied BEFORE delete — the recipe that emerged from p238 #331 close-review). NO video/audio reprocessing performed. NO live notification recorded (PM dispatches email manually + calls RPC themselves to anchor the audit chain at the actual send moment — avoids fabricating a "sent" timestamp). **Previous header (p238 close — #331 SHIPPED)**: Migration `20260805000022_p238_331_voice_biometric_consent_rpcs.sql` DROP+CREATEs `give_consent_via_token(text, text, jsonb)` accepting `p_consent_type='voice_biometric'` + required `p_evidence` jsonb (version + lang + label_text_hash SHA-256 of displayed destacado label); DROP+CREATEs `revoke_consent_via_token(text, text)` accepting voice_biometric; extends `consume_onboarding_token` payload with `has_voice_biometric_consent` + `has_voice_biometric_revoked`. Frontend `PMIOnboardingPortal.tsx` (`/pmi-onboarding/[token]`) gains: amber-styled destacado section (separate from AI analysis consent per LGPD Art. 8 destaque rule + Art. 11 §I citation) + `sha256Hex` evidence helper + `VOICE_CONSENT_VERSION='v1'` constant + `handleVoiceConsentToggle` handler + video upload pillar list gated on `hasVoiceConsent` (alternative interview path stays available, application accepted). i18n adds `privacy.s4.openaiWhisper` + 7 `pmi.onboarding.voiceConsent*` keys in pt-BR/en-US/es-LATAM (21 total). `privacy.astro` lists OpenAI Whisper subprocessor. 23 new contract assertions in `tests/contracts/consent-voice-biometric-ui.test.mjs` (17 static + 3 forward-defense + 3 DB-gated). Test baseline **1856/1800/0/56 → 1879/1820/0/59 offline** (+20 pass + 3 skip). Live smoke 3/3 PASS (signature is 3-arg; give+revoke dispatch voice_biometric and reach token check; consume payload includes both new keys). Invariants 19/19=0 throughout. SEDIMENT-232.B applied: apply_migration created shadow row `20260524021619` that was DELETEd + canonical `20260805000022` registered manually. Migration `20260805000022_p238_331_voice_biometric_consent_rpcs.sql` DROP+CREATEs `give_consent_via_token(text, text, jsonb)` accepting `p_consent_type='voice_biometric'` + required `p_evidence` jsonb (version + lang + label_text_hash SHA-256 of displayed destacado label); DROP+CREATEs `revoke_consent_via_token(text, text)` accepting voice_biometric; extends `consume_onboarding_token` payload with `has_voice_biometric_consent` + `has_voice_biometric_revoked`. Frontend `PMIOnboardingPortal.tsx` (`/pmi-onboarding/[token]`) gains: amber-styled destacado section (separate from AI analysis consent per LGPD Art. 8 destaque rule + Art. 11 §I citation) + `sha256Hex` evidence helper + `VOICE_CONSENT_VERSION='v1'` constant + `handleVoiceConsentToggle` handler + video upload pillar list gated on `hasVoiceConsent` (alternative interview path stays available, application accepted). i18n adds `privacy.s4.openaiWhisper` + 7 `pmi.onboarding.voiceConsent*` keys in pt-BR/en-US/es-LATAM (21 total). `privacy.astro` lists OpenAI Whisper subprocessor. 23 new contract assertions in `tests/contracts/consent-voice-biometric-ui.test.mjs` (17 static + 3 forward-defense + 3 DB-gated). Test baseline **1856/1800/0/56 → 1879/1820/0/59 offline** (+20 pass + 3 skip). Live smoke 3/3 PASS (signature is 3-arg; give+revoke dispatch voice_biometric and reach token check; consume payload includes both new keys). Invariants 19/19=0 throughout. SEDIMENT-232.B applied: apply_migration created shadow row `20260524021619` that was DELETEd + canonical `20260805000022` registered manually. **Previous header (p237 close)**: orphan agent branches `agent/issue-218-whisper-art11` (`58a9051b`) + `agent/issue-221` (`a48ebc90`) DELETED post-merge after explicit ADR-0097 amnesty disposition per PM Option A (Recommended). `agent/issue-221` was purely stale (3 files byte-identical to main). `agent/issue-218-whisper-art11` carried 1 file whose canonical row `20260520231254:issue_218_whisper_art11_emergency_block` was already in `MIGRATION_FILE_DRIFT_BASELINE_P224.txt` line 1 (694-entry amnesty baseline); DDL operationally confirmed live (trigger + function + 2 consent columns). No DDL, no code, no live DB writes this PR. Audit doc `docs/audit/2026-05-23_p237_orphan_branches_equivalence.md` + P162 #204 anchor the disposition. Side-finding: 4 historical `apply_migration` shadow rows (SEDIMENT-227.A / SEDIMENT-232.B class) noted but NOT remediated this session (deferred Path C carry). **Previous header (p236 dispatch)**: #221 + #218 Whisper Art. 11 voice biometric umbrella DECOMPOSED into 5 narrow ready-leaf children per PM Option A (Recommended). Wave 1 engineering moat already shipped p207 (drop trigger + voice biometric consent columns + helper gate via migs `20260801000000-002`). Remaining workstreams now tracked as: **#331** (W2 UI consent capture + `privacy.s4.openaiWhisper` i18n in 3 langs, ready-leaf), **#332** (W3 retroactive notification of 1 affected candidate + Art. 18 §IV deletion offer + per-subject deletion log schema, ready-leaf), **#333** (W4 invariant U enforcing voice biometric consent on transcribed rows, blocked on PM sequencing decision), **#334** (W5 Angeline legal-ops chain — ANPD Art. 48 + notification template + DPO + Adendo Privacidade + Termo de Speaker; 5 docs under `docs/audit/lgpd-art11-remediation/`; blocked on Angeline async), **#335** (ADR-0094 Initiative Collaboration Hub draft + Tier 3 ratification; blocked on C1/C2/C3 closes). Both umbrella issues closed at this PR's window (#221 as IMPLEMENTED, #218 as REFRAMED) with evidence comments posted pre-merge per SEDIMENT-235.A. Registry "consent blockers resolved or explicitly decomposed" clause for #243 + #254 satisfied — both unblocked for spec/audit work. Tier 3 council 5/5 verdict on #221 honored by this disposition. **Previous header (p235)**: #323 Gap C closed (PR #328 squash `841da143`) AND #230 umbrella auto-closed via the same merge — migration `20260805000021` applied PM per-kind decision on `engagement_kinds` catalog (study_group_owner placeholder slug + study_group_participant flip); sanity DO enforces catalog invariant; 12 contract tests; live smoke 0 offenders + invariants 19/19=0; Fernando's participant row is_authoritative=true with zero new capabilities; owner row still pending termo.)
**Purpose:** dispatch board for parallel agents. This registry complements GitHub labels by adding execution status, lane, blockers, and close rules.

**Operating protocol:** `docs/project-governance/ISSUE_REGISTRY_OPERATING_PROTOCOL.md`

Status values:

- `active` — work may proceed now.
- `qa-window` — implementation appears complete; keep open for stabilization smoke only.
- `blocked` — do not implement until blocker clears.
- `spec-only` — planning/ADR/sub-issues only; no app behavior changes.
- `ready-leaf` — narrow implementation issue ready for one agent/worktree.
- `defer` — valid but not current-wave work.
- `close-candidate` — likely closable after curator comment/signoff.

---

## Stop-The-Line

| Issue | Registry status | Lane | Blocks | Blocked by | Acceptance evidence | Close rule |
|---|---|---|---|---|---|---|
| #221(closed) | closed | Foundation / Integration / Governance | none | none | Closed p236 as IMPLEMENTED — Wave 1 engineering moat shipped p207 (drop trigger `trg_video_ai_analysis_on_upload` via mig `20260801000000` + voice biometric consent columns `consent_voice_biometric_at + revoked_at + evidence` via mig `20260801000001` + `analyze_application_video_async` helper gate via mig `20260801000002`). Remaining audit chain docs #3-5 + ADR-0094 explicitly decomposed into siblings #332/#334/#335. Tier 3 council 5/5 verdict honored by this disposition. | Closed 2026-05-23 (p236); no re-open unless new live LGPD exposure surfaces |
| #218(closed) | closed | Foundation / Governance | none | none | Closed p236 as REFRAMED — Wave 1 emergency block shipped p206/p207 (BEFORE INSERT/UPDATE trigger `trg_pmi_video_screening_voice_consent` via mig `20260731000000_issue_218_whisper_art11_emergency_block.sql` + the p207 trio). Waves 2-5 explicitly decomposed into 5 ready-leaf children #331/#332/#333/#334/#335 per PM Option A. | Closed 2026-05-23 (p236); re-open only if a remaining sibling cannot be cleanly assigned to one of #331-#335 |
| #148 | active | Infra/Security / QA | all merge confidence | #220 | CI heartbeat green on `main` | Close only after CI Validate recovers on main |
| #220 | active | Infra/Security / QA | #148 | none | Local test:browser:guards + build pass with resolvable mock Supabase URL; remote CI pending | Close after CI PR passes and main heartbeat confirms |
| #260 | qa-window | Foundation / Integration / QA / Governance | selection funnel communications, Resend quota safety | production smoke 7d; auto-trigger design for selection_cutoff_approved | All 7 W2 leaves shipped p228 (PRs #305 + #307), Leaf 5 hotfix shipped (PR #313), replay executed live (`updated_count=2`; 15 manual_close), P162 hotfix addendum landed (PR #316). Catalog parity locked. Phase C drift gate enforced. Health signal HEALTHY. | Close after production smoke confirms no recurrence over 7 days |
| #230(closed) | closed | Foundation / Governance / QA | none | none | p230 audit refuted original Herlon-specific premise (his engagements don't require agreement; A3 fixed by #318/#320). Reframed into 3 narrow children covering actual gaps. All 3 shipped: #321(closed p233) phantom onboarding rows + AFTER INSERT trigger + 30-row backfill; #322(closed p234, 2 migrations) 8-row classification-leftover backfill + forward guard in `approve_selection_application` + offboarding extension in `admin_offboard_member` + `get_my_onboarding` skipped≡completed harmonization + close-review patch mirroring guard in `get_my_onboarding` auto-seed; #323(closed p235) catalog config — `study_group_owner` placeholder slug + `study_group_participant` flip; sanity DO enforces invariant; 12 tests + 2 forward-defense ratchets; live smoke 0 offenders. Auto-closed by PR #328 merge (registry text "close #230" matched GitHub auto-close regex); evidence summary posted retroactively at comment 4526837462 | Closed 2026-05-23 (p235); no re-open unless new vol_term lifecycle drift surfaces |

---

## QA Window

| Issue | Registry status | Lane | Blocks | Blocked by | Acceptance evidence | Close rule |
|---|---|---|---|---|---|---|
| #159 | qa-window | Governance | p201 child dispatch | PM stabilization confirmation | Template renders; first p201 child uses template | Close after PM confirms stabilized |
| #216 | qa-window | Frontend | `/profile` trust | production smoke | `/profile` no `ReferenceError`, spec diagnosis corrected | Close after production smoke and doc/comment cleanup |
| #179(closed) | closed | Foundation / Governance | none | none | 5/5 ACs met live p230 — canonical `approve_selection_application` exists; `admin_update_application` + `finalize_decisions` delegate (RAISE on failure); invariants R/S/A3 green; contract test `canonical-approval-orchestration.test.mjs`. Implementation continues under #230 children (#321/#322/#323) + existing lifecycle issues #180/#181/#177/#182/#183 → registry close rule satisfied | Closed 2026-05-23 (p230); no re-open unless concrete canonical RPC failure proves the partial AC-4 split is incorrect |
| #229(closed) | closed | Foundation / MCP-AI / Frontend | none | none | Phase 2 shipped p232 — migration `20260805000017` extends 3 read RPCs: `get_pert_cutoff_summary` accepts `leader_extra_pert_score` + dual-track math, `get_application_score_breakdown` returns `leader_extra_cutoff` block, `get_selection_dashboard.cycle` exposes sibling `leader_extra_cutoff`. MCP v2.79.1 surfaces `leader_extra_pert_score` in score_column z.enum + descriptions clarify dual-dim. Frontend `/admin/selection` has 2-band chip (objective + leader cutoff). Contract tests +12 (`p232-229-phase2-leader-extra-visibility.test.mjs`). Live smoke 3/3 — get_pert_cutoff_summary leader_extra returns method=disabled (cohort_n=6 < 10) expected, get_application_score_breakdown returns leader_extra_cutoff + top-level leader_extra_pert_score, get_selection_dashboard.cycle has both blocks. Backfill not needed (cron `recompute-pert-cutoffs-weekly` already calls both dimensions per p219 Phase 1) | Closed 2026-05-23 (p232); no re-open unless concrete read-surface gap surfaces in production |

---

## Spec Trackers

| Issue | Registry status | Lane | Blocks | Blocked by | Acceptance evidence | Close rule |
|---|---|---|---|---|---|---|
| #292 | spec-only | Governance / QA | #260, #251, #116(closed), #179(closed), #229(closed), #230(closed) + #321/#322/#323 sequencing | PM/dev adoption of plan | `SELECTION_RELIABILITY_PRIORITIZATION_PLAN.md` accepted and child handoffs dispatched in order | Close after sprint child issues are dispatched or tracker intentionally retained |
| #300 | spec-only | Governance / Foundation / MCP-AI | external_reviewer onboarding refactor | PM triage; do not preempt #292 unless escalated as active P0 | 10-gap umbrella decomposed into child issues with lanes and blockers | Keep deferred during Selection reliability sprint unless PM explicitly escalates a narrow stop-the-line child |
| #212 | spec-only | Governance / Architecture | #211, #209, Drive/external-member work | #221 and PM signoff | ADR-0094, architecture doc, sub-issue set accepted | Close only after child issues spawned or tracker intentionally retained |
| #254 | spec-only | QA / Foundation / Integration / Frontend / MCP-AI / Governance | Cycle 4 video screening trust, AI-assisted video review workflow | production read-only audit, #221/#218 consent stance | Drive folder files reconciled to `pmi_video_screenings`, AI processing/suggestion state, and human validation path; child issues split by lane | Close only after audit evidence and child implementation issues are accepted or tracker intentionally retained |
| #243 | spec-only | Governance / MCP-AI / Frontend / QA | selection AI-assist calibration children | calibration profile contract and child split | Spec/ADR defines versioned calibration profile, context completeness warnings, evidence guardrails, AI lineage, and LGPD/HITL stance | Close only after child issues are accepted or tracker intentionally retained |
| #166 | spec-only | Foundation / Governance | semantic-layer implementation | ADR decisions | roadmap/ADR accepted | Close after roadmap decomposes into leaves |
| #204 | spec-only | Governance / Integration | Gemini/Drive/Calendar work | #212/#209 boundaries | integration governance direction accepted | Close after split into vendor eval, Drive lifecycle, calendar governance leaves |
| #97 | spec-only | Governance / Foundation | speaker lifecycle | consent/legal pattern from #221/#212 | accepted external speaker lifecycle spec | Close after child issues exist |
| #92 | spec-only | Integration / Foundation | calendar sync implementation | #205/#210 identity/email contract | accepted calendar integration architecture | Close after child issues exist |
| #335 | spec-only | Governance / Architecture | Tier 3 council ratification path for #212; subsequent ICH implementation tickets | Siblings C1 #331 + C2 #332 + C3 #333 closed (C4 #334 NOT required for Accepted) | `docs/adr/ADR-0094-initiative-collaboration-hub.md` authored + Tier 3 council `/council-review topic=adr-0094-initiative-collaboration-hub` returns 5/5 GO + status flips Draft → Proposed → Accepted + linked from `docs/adr/README.md` | Close after Tier 3 ratification + ADR linked from index |
| #312 | audit-shipped-p260 / **PM D1-D5 ratified + 7 children spawned (2026-05-25)** / **dispatch 1/7 #377 SHIPPED p261 via PR #385 `e8d7fe92`** / **dispatch 2/7 #378 SHIPPED p262 via PR #387 `15c7b3f0`** / **dispatch 3/7 #379 SHIPPED p262.W4c via PR #389 `eb72a926` — GAP-259.A closed; biblioteca membro oculta draft/pending_proposer_consent/withdrawn/revoked** | Governance / QA / Foundation / Frontend | **#377(closed p261)** (W4b sign_proposer_consent v1-must seq 1/7 ✓) · **#378(closed p262)** (W4a gate templates v1-must seq 2/7 ✓) · **#379(closed p262.W4c)** (W4c GAP-259.A v1-must seq 3/7 ✓) · **#380** (W4d reader hardening v1-must seq 4/7 — NEXT DISPATCH, unblocked) · #381 (W4e curator draft-read v1-must seq 5/7) · #382 (W4f blind-review primitives v1-follow-up seq 6/7) · #383 (W4g content_products ADR v1-follow-up seq 7/7, PM D3 ratified `content_products` canonical) | sequence-blocked: #381 blocked by #380, #382-#383 blocked by 5 v1-must leaves closing | Audit doc `docs/audit/p260_wave_4_312_journey_audit.md` with persona × shell matrix (SPEC §11), instrument × review_mode matrix (SPEC §6.1), gate template coverage (5/11 pre-p262 → 11/11 post-p262), comment visibility drift, **PM ratification appendix §15.5 (D1-D5 verbatim + sequence + 7 child issue IDs)**. #377 evidence: PR #385 + migration `20260805000040` + contract test `p261-312-w4b-sign-proposer-consent.test.mjs` + live smoke 3/3 PASS. #378 evidence: PR #387 + migration `20260805000041` + contract test `p262-312-w4a-gate-templates-6-doc-types.test.mjs` + 6 new templates live + 5 preserved + Frontiers ready for editor lock. #379 evidence: PR #389 + migration `20260805000042` + contract test `p262-312-w4c-gap-259-a-default-status-exclusion.test.mjs` (17 static + 3 DB-gated) + live smoke 2/2 PASS (default hides Frontiers + explicit draft filter shows Frontiers) + corpus 13/3 split. Wave 4 acceptance per SPEC §16.5 = documentation+matrix (✓ shipped). | Close after #380+#381 ship (2 remaining v1-must children) AND #382+#383 ratchet to in-flight per dispatch sequence AND Frontiers Guide completes intake → consent (✓ p261) → draft (✓ p261) → lock-ready (✓ p262) → review → approval → active end-to-end |
| #315 | Wave 1a Foundation shipped p256 (PR #366 `f728a89a`); Wave 1b first leaf #367 shipped p257 (PR #369 merge `7354c75d` — V invariant ACTIVATED, 21st invariant live with 0 violations); **Wave 2 #310 + Wave 3 #314 SHIPPED p258 (PR #371 `da83327f` + PR #372 `ecb111c7`) — admin intake wizard + member library + `/documents` alias + retired legacy tab in GovernancePage** | Governance / Foundation / Frontend / Integration | #310(closed Wave 2 — shipped p258), #314(closed Wave 3 — shipped p258), #312 audit jornada (Wave 4 QA — next-in-line for v1), #96 Frontiers launch (Wave 2+4 — Wave 2 infra fulfilled; operational fixture pending), #311 evidence bundles (Wave 6), #308 curator evidence (Wave 6), #181 certificate counter-sig (Wave 6), #301 Drive grants (Wave 5) | Wave 4 #312 audit jornada + Frontiers fixture #96 operational smoke ready next; Waves 5/6/7 deferred post-v1 per PM directive p256 | Wave 0 ratified (Tier P0 10/10 + Tier P1 7/7 + amendments A1/A2/A3) per [#315 comment-4530613476](https://github.com/VitorMRodovalho/ai-pm-research-hub/issues/315#issuecomment-4530613476). **Wave 1a Foundation shipped p256 PR #366 merge `f728a89a`** (3 migrations: 20260805000035 M1 org_id backfill + 20260805000036 M2 taxonomy/visibility/status/atomic-RLS-swap/V' invariant + 20260805000037 M3 create_governance_document_intake + list_governance_library; 72 new contract assertions; 6 PM corrections applied incl. FK RESTRICT + no fake proposer_consent + locked_at hard-gate). **Wave 1b first leaf #367 shipped p257** (2 migrations: 20260805000038 synthetic-chain backfill for 7 legacy pre-chain docs + 20260805000039 V invariant activation; 46 contract assertions in p257-315-w1b-legacy-chain-backfill.test.mjs + 1 ratchet bump in volunteer-authority-invariants.test.mjs 20→21; PM-ratified 3-class signer-of-record convention: Classe 1 docusign 5 docs + Classe 2 gov_br_template 1 doc + Classe 3 internal_attestation 1 doc; signoff_type=acknowledge + content_snapshot.role=migration_attestation + content_snapshot.legacy_migration=true preserves semantic distinction "migration attester" vs "content approver"; placeholder document_version created for Termo Voluntariado Ciclo 3 to satisfy approval_chains.version_id NOT NULL FK while preserving invariant J; 3 triggers disabled+re-enabled during backfill — approval_signoff_xp + trg_approval_signoff_notify + trg_artia_sync_on_govdoc_ratified — to suppress XP/notification/Artia side-effects from synthetic rows). **V invariant activated** as 21st in check_schema_invariants() with NO carve-out (PM Q1=C — rejected metadata.legacy_pre_chain shortcut); forward-defense locks regression class permanently in CI (no metadata.legacy_pre_chain + no metadata.legacy_migration in V body). **Live**: 21 invariants all violation_count=0; 7 governance_documents all have current_ratified_chain_id NOT NULL post-backfill; 7 admin_audit_log rows with action=governance.legacy_chain_synthesized. **KNOWN REGRESSION CARRY** (intentional per PM #3 from Wave 1a): document_versions_read_published swap removes `curate_content` and `manager`/`deputy_manager` operational_role bypass; only `manage_member` is admin bypass. Effect: 2 curators (Roberto Macêdo + Sarah Faria) without `manage_member` lose direct SELECT on UNLOCKED drafts via DocumentVersionEditor. Chain review unaffected (uses get_chain_workflow_detail SECDEF). Wave 1b separate leaf (NOT #367) will ship dedicated curator-draft-access policy or RPC. **Wave 2 #310 shipped p258 PR #371 `da83327f`**: admin intake wizard (Tier-1 5 fields + 2 optional advanced + acknowledgement_mode A1 preview + initial_status A2 preview) calling `create_governance_document_intake`; 5 forward-defense regressions (F1-F5) locked in CI. **Wave 3 #314 shipped p258 PR #372 `ecb111c7`**: member-facing biblioteca at `/governance/documents` (PT canonical + en/es redirects) + `/documents` alias 301 per P2-Q3 + retired legacy `?view=documents` tab in GovernancePage (`type View` narrowed 4→3) + deleted orphan `DocumentsList.tsx` + crosslink card from Manual view; calls `list_governance_library` with URL-persisted filters; 7 forward-defense regressions locked in CI (no PII leak / no table-direct SELECT / single canonical surface / legacy RPC retired / view enum locked / bookmark redirect / crosslink testid). Test baseline cumulative `2348 → 2414/2328/0/86` offline (+66 pass). Wave 2/3 known carries deferred to follow-up leaves: (a) curator-draft-access mitigation Roberto/Sarah (Wave 1b separate); (b) `/governance/document/[id].astro` reader hardening (table-direct SELECT bypass); (c) `list_governance_library` payload extension for `depends_on`/templates (Wave 1b expansion); (d) Frontiers fixture #96 operational smoke pending post-merge. **p259 Frontiers fixture #96 live-smoked end-to-end** via SEDIMENT-226.C JWT-injection — `create_governance_document_intake` returned `document_id=18ec4690-4f5a-4cab-904d-451e2c7245bf` with `status=pending_proposer_consent` + `acknowledgement_mode=informational` (A1 default for editorial_guide); `list_governance_library('{}'::jsonb)` returns 16/16 docs with Frontiers entry surfacing all 12 library fields + ZERO PII leak; invariants 21/21=0 (V' verified); 0 `admin_audit_log` side-effect (canonical A2 path skips audit row by design). Evidence in `docs/audit/p259_frontiers_fixture_96_live_smoke.md`. NEW GAP-259.A surfaced + **PM-ratified Option (a) at session close 2026-05-25**: `list_governance_library` MUST exclude `draft`/`pending_proposer_consent`/`withdrawn`/`revoked` from default member library view; explicit `p_filters.status` override allowed only for admin/audit; member-facing biblioteca surfaces "ready-to-engage" docs only. Separate Wave 1b leaf will ship the RPC body update + forward-defense test (NOT this PR). **p260 Wave 4 #312 audit jornada shipped (this session)**: read-only audit doc `docs/audit/p260_wave_4_312_journey_audit.md` grounded in SPEC + code + live corpus (16 docs, 21/21 invariants 0, 12 perms for `participate_in_governance_review`); 5 PM decisions D1-D5 surface 3 blockers (gate templates missing for 6 doc_types; `sign_proposer_consent` deferred Wave 1b; blind-review primitives absent); proposed child-issue split `#312-W4a/b/c/d/e/f/g`; #312 stays OPEN as audit umbrella until PM ratifies D1-D5 + dispatches child leaves. | Close after Wave 4 #312 audit ships (✓ p260) + child leaves dispatched + Frontiers #96 fixture advances through full chain (✓ p259 intake; blocked at sign_proposer_consent + gate template); OR tracker intentionally retained for Wave 4/5/6/7 sequencing |
| #367(closed) | closed | Foundation / Governance / Audit-trail | none | none | p257 — 2 migrations + 46 contract assertions ratchet baseline 2302/2219/0/83 → 2348/2262/0/86 offline. **M1 (20260805000038)**: 1 placeholder document_versions row for Termo Voluntariado Ciclo 3 (Classe 2 Sub-decision A — preserves invariant J via `locked_at=now()`); 7 INSERT approval_chains with status='active' + gates=[{kind='member_ratification', order=1, threshold=1, status='completed'}] (trg_sync_ratification_cache auto-populates governance_documents cache fields); 7 INSERT approval_signoffs with signoff_type='acknowledge' (NOT 'approval' per PM convention — preserves "migration attester" vs "content approver" semantic) + content_snapshot.legacy_migration=true + content_snapshot.role=migration_attestation + content_snapshot.p257_migration_marker=true + content_snapshot.invariant_anchor='V_status_chain_coherence'; 7 UPDATE governance_documents SET closing_gate_signoff_id + approved_at; 7 INSERT admin_audit_log (action='governance.legacy_chain_synthesized', metadata.signer_of_record_convention='pm_vitor_as_migration_attester'). **3 triggers disabled+re-enabled** during backfill (approval_signoff_xp + trg_approval_signoff_notify + trg_artia_sync_on_govdoc_ratified) to suppress XP pollution + notifications to involved parties + Artia external sync 7x. Sanity DO RAISES if orphan_count > 0 OR chain_count != 7 OR signoff_count != 7 OR audit_count != 7. **M2 (20260805000039)**: CREATE OR REPLACE FUNCTION check_schema_invariants() preserving 20 prior invariants byte-identical (Phase C body-hash drift gate respected) + appending V_status_chain_coherence as 21st (drift CTE = governance_documents WHERE status IN (approved, active) AND current_ratified_chain_id IS NULL); COMMENT bumped 20 → 21 citing V activation + Wave 1b + #367 + #315 + migration 20260805000038. Sanity DO RAISES if V violation_count != 0 OR V absent. **Forward-defense**: V body MUST NOT contain `metadata.legacy_pre_chain` (PM Q1=C rejected this shortcut permanently) NOR `metadata.legacy_migration` (legacy_migration lives on signoffs/chains, NOT on gd — V scopes only on gd.status + gd.current_ratified_chain_id). Drift CTE WHERE clause asserted to have EXACTLY 2 conditions joined by AND. **Live smoke**: 21 invariants all 0 violations; check_schema_invariants() returns V_status_chain_coherence with violation_count=0; 7 canonical legacy doc IDs all have current_ratified_chain_id NOT NULL; COMMENT live = "21 schema invariants — V activated p257 Wave 1b first leaf (#367 Step 5, #315 P0-Q6)"; Phase C body-hash byte-identical to migration file capture. PM-ratified signer-of-record convention from comment-4530953843 honored end-to-end. | Closed 2026-05-25 (p257) via PR #369 merge `7354c75d`; no re-open unless V violation drifts above 0 (CI ratchet catches via DB-gated assertions in p257-315-w1b-legacy-chain-backfill.test.mjs) |

---

## Ready Or Near-Ready Leaves

| Issue | Registry status | Lane | Blocks | Blocked by | Acceptance evidence | Close rule |
|---|---|---|---|---|---|---|
| #211 | blocked | Frontend | #212 G3 | #212 PM signoff if broader scope applies | metadata UI smoke | Do not start until scope is confirmed standalone vs #212 child |
| #194 | ready-leaf | QA | curatorship p197 confidence | p197 test fixtures | contract tests for review flow pass | Close after tests land |
| #193 | ready-leaf | Foundation | curatorship status consistency | none | migration/audit confirms phantom states removed | Close after migration + rollback docs |
| #192 | ready-leaf | Foundation | curatorship review integrity | none | DB constraint/RPC test: one curator review per round | Close after test + migration evidence |
| #321(closed) | closed | Foundation / Governance / Data-integrity | none | none | p233 — migration `20260805000018` installs AFTER INSERT trigger on certificates with `WHEN (NEW.type='volunteer_agreement')` that atomically flips matching `onboarding_progress.volunteer_term` row to `status='completed', completed_at=NEW.issued_at` (idempotent via `status != 'completed'` guard on unique `(member_id, step_key)` index). In-tx backfill of 30 phantom rows via DISTINCT ON latest-cert-per-member CTE, using each member's latest `issued_at` for historical accuracy. Sanity DO block RAISE EXCEPTION on remaining phantom (fails apply at write time, not runtime). 12 contract test assertions (11 static + 1 DB-gated). Live smoke: 0 phantom rows post-apply; 30 backfill audit log rows; invariants 19/19=0. | Closed 2026-05-23 (p233); no re-open unless concrete future phantom row regresses (trigger is the forward-defense going forward) |
| #322(closed) | closed | Foundation / Governance / Data-integrity | none | none | p234 — **2 migrations (20260805000019 initial + 20260805000020 close-review patch)**. Initial: (1) backfills 8 phantom-pending vol_term rows (4 active + 4 inactive — none with cert, none with active requires_agreement engagement) to `status='skipped'` with metadata `reason='no_requires_agreement_engagement'`, `completed_via='p234_322_backfill_no_agreement_path'`, `migration='20260805000019'` (PM directive 2026-05-23 — NEVER `completed` unless cert exists or real signing event); (2) forward guard in `approve_selection_application` Path A SELECT adds `AND NOT (s.id = 'volunteer_term' AND NOT COALESCE(v_requires_agreement, FALSE))`; (3) offboarding extension in `admin_offboard_member` auto-skips open vol_term steps with metadata `reason='offboarded_pre_signing'`; (4) `get_my_onboarding` harmonizes `skipped`≡`completed`. **Close-review patch (20260805000020)**: PM curator review of PR #327 caught that `get_my_onboarding()` universally auto-seeded all `onboarding_steps` for the member on first call — reintroducing Gap B for any future member whose first hit lands there. Patch mirrors `approve_selection_application` forward guard in `get_my_onboarding`: computes `v_has_req_agreement_engagement` via EXISTS query on engagements+engagement_kinds (status=active + requires_agreement=true), then adds `AND NOT (s.id = 'volunteer_term' AND NOT v_has_req_agreement_engagement)` to the auto-seed INSERT. 20 contract test assertions (19 static + 1 DB-gated). Live smoke: goal metric = 0 active members with pending vol_term and no active requires_agreement engagement; 8 backfill audit rows present; invariants 19/19=0; live function body has all 4 expected fragments (declares var + EXISTS query + auto-seed guard + skipped≡completed preserved). | Closed 2026-05-23 (p234); no re-open unless forward guard fails to prevent new Gap B occurrences (audit goal metric ratchet against 0 going forward) |
| #323(closed) | closed | Foundation / Governance / Data-integrity | none | none | p235 — migration `20260805000021` applies PM per-kind decision (2026-05-23 ABCD): (1) `study_group_owner` keeps `requires_agreement=true` and gains placeholder `agreement_template='study_group_owner_agreement_v1'` — preserves ADR-0006/0008 termo intent; mirrors ADR-0078 D5 external_reviewer slug placeholder precedent; template body deferred to follow-up legal-counsel issue. (2) `study_group_participant` flips `requires_agreement=false` (legal_basis stays `contract`); course enrollee model per ADR-0008 "termo de uso" read as platform-wide TOS. Sanity DO block RAISES on invariant violation. 12 contract tests (11 static + 1 DB-gated): file existence + header cross-refs (#323/ADR-0006/ADR-0008/ADR-0078/ROLLBACK/Herlon directive) + owner UPDATE with idempotency guard + participant UPDATE with idempotency guard + audit log INSERT with canonical action + sanity DO RAISE + sanity allowlist (`'volunteer'`) + NOTIFY pgrst + BEGIN/COMMIT wrapper + 2 forward-defense (no future migration re-flips owner template back to NULL nor participant requires_agreement back to TRUE) + 1 DB-gated live invariant. Live smoke: 0 catalog offenders + Fernando participant row → `is_authoritative=true` (no role=leader perms granted; role=participant write_board prospective seed unchanged) + owner row stays pending termo + invariants 19/19=0. | Closed 2026-05-23 (p235); no re-open unless legal-counsel ships the body template at which point the slug binding becomes load-bearing |
| #331(closed) | closed | Frontend / Governance / UX | none | none | p238 — migration `20260805000022` DROP+CREATEs `give_consent_via_token(text, text, jsonb)` accepting `p_consent_type='voice_biometric'` + required `p_evidence` jsonb (version + lang + label_text_hash SHA-256); DROP+CREATEs `revoke_consent_via_token` accepting voice_biometric; extends `consume_onboarding_token` payload with `has_voice_biometric_consent` + `has_voice_biometric_revoked`. Sanity DO defends against dual-overload regression (SEDIMENT-232.A). `PMIOnboardingPortal.tsx` adds amber destacado section (separate from AI analysis consent per LGPD Art. 8) + `sha256Hex` helper + `handleVoiceConsentToggle` handler + pillar list gate `!isInterviewMode && hasVoiceConsent` (interview alternative path stays visible, app accepted). i18n adds `privacy.s4.openaiWhisper` + 7 voiceConsent* keys in pt-BR/en-US/es-LATAM. `privacy.astro` lists Whisper subprocessor. 23 contract assertions (17 static + 3 forward-defense ratchets + 3 DB-gated). Test baseline 1856→1879/1800→1820/0/56→59 offline. Live smoke 3/3 PASS (give+revoke dispatch voice_biometric + reach token check; consume payload contains both new keys). Invariants 19/19=0. Goal metric — UI path exists to populate `selection_applications.consent_voice_biometric_at` (0/107 → grows by 1 per candidate explicit grant) + `consent_voice_biometric_evidence` stores immutable proof (version+lang+SHA-256-of-shown-label) per Art. 11 §I. | Closed 2026-05-23 (p238); no re-open unless a future migration regresses voice_biometric dispatch from either RPC (forward-defense ratchet would catch this in CI) |
| #332(closed) | closed | Foundation / Governance / Audit-trail | none | none | p239b — Wave 3 retroactive Art. 18 §IV remediation fully ratcheted to closed via 3-commit PR #342: (1) infrastructure already shipped p238b via migration `20260805000023` (`pii_access_log.deletion_artifacts jsonb` + 2 SECDEF RPCs gated on `manage_member`); (2) operator surface +2 MCP tools wrapping the RPCs (tool count 299→301; ef_version 2.79.1→2.80.0); (3) hotfix migration `20260805000024` fixed `accessor_id := auth.uid()` → `v_caller_member_id` bug discovered when PM ran the actual dispatch (FK target is `members(id)`, not `auth.users(id)`; gate ladder fired before INSERT during service-role smoke so p238b smoke missed it). Live dispatch executed 2026-05-24T00:29:00-03:00 by Vitor Maia Rodovalho (accessor member.id `880f736c`) using the PM-approved interim_v1 template (`docs/audit/lgpd-art11-remediation/notification_eduardo_luz_p238_interim.md`) — pt-BR text dispatched to `eduardoluz.pm@gmail.com` via `nucleoia@pmigo.org.br` (PM minor body edit: "um vídeo" → "os vídeos (Background + demais perguntas)" — factual clarification, preserves `interim_v1` semantics). **Chain anchor:** `pii_access_log.id = ee6a1541-bef6-4f7c-be69-c1e7efbc6568` (context=`lgpd_art_18_retroactive_notification`, fields_accessed=['email'], accessor_id resolved to Vitor's member row, target_member_id NULL because Eduardo is applicant-only). PM saved PDF evidence of Gmail "Sent" to org offline records (Drive `LGPD/Art_18_remediation/eduardo_luz_p238/` — PII not committed to repo). 30-day window for Eduardo response opens 2026-05-24; tacit retention applies after; right to deletion remains exercisable indefinitely. 9 new forward-defense contract assertions in `tests/contracts/p239b-332-hotfix-accessor-id-fk.test.mjs` (locks the regression class going forward) + 17 in `mcp-lgpd-retroactive-operator-tools.test.mjs` + 2 ratchet updates. Test baseline 1899/1837/0/62 → 1925/1863/0/62 offline (+26 total across p239b PR #342 + hotfix). Invariants 19/19=0 throughout. SEDIMENT-235.A respected (no auto-close keywords until this ratchet commit, which intentionally uses `Closes #332` because the issue genuinely closes here). SEDIMENT-238.A applied cleanly on hotfix migration. SEDIMENT-239b.A LEARNING (new): contract tests for SECDEF RPCs that INSERT into FK-constrained tables MUST assert the source of every FK column, not just the gate ladder presence — would have caught the auth.uid() vs members.id mismatch at p238b CI. | Closed 2026-05-24 (p239b) via PR #342 squash-merge; re-open only if Eduardo requests deletion within 30 days (triggers `lgpd_execute_retroactive_deletion` MCP tool invocation + Drive file removal — that operational follow-up tracked under #333 sequencing call) |
| #310(closed) | closed | Frontend / Governance / UX | none | none | p258 — Wave 2 admin intake wizard shipped via PR #371 squash `da83327f`. `src/components/governance/DocumentIntakeWizard.tsx` (React island, modal triggered from header CTA) collects Tier-1 5 fields per P1-Q6 (title · doc_type · author_label · visibility_class · description) + optional `proposer_ack_offline` boolean (A2) + optional `proposer_member_id` UUID via Advanced section. Read-only preview of `acknowledgement_mode` (A1 default-per-doc_type table) + initial `status` (A2 — `pending_proposer_consent` if no offline ack, `draft` if attested offline). Calls Wave 1a M3 RPC `create_governance_document_intake(p_payload jsonb)` (live since p256); on success redirects to existing `/admin/governance/documents/{document_id}/versions/new` editor de versão. 11 doc_type options (mirrors CHECK exactly) + 5 visibility_class options (mirrors CHECK) localized × 3 langs. 54 new i18n keys in `governance.docs.intake.*` (pt-BR canonical + en-US + es-LATAM, parity-tested). 32 contract assertions in `tests/contracts/p258-310-wave2-admin-intake-wizard.test.mjs` (9 nested describes) with **5 forward-defense regressions locking the regression class permanently in CI**: F1 client MUST NOT send `acknowledgement_mode` (RPC computes per A1); F2 client MUST NOT send `status` (RPC computes per A2 from `proposer_ack_offline`); F3 `doc_type` comes from typed enum only — no free-text path; F4 `visibility_class` comes from typed enum only; F5 success path MUST redirect to editor de versão route. Test added to BOTH `"test"` + `"test:contracts"` whitelists (SEDIMENT-186.C). Test baseline ratchet (isolated): `2348/2262/0/86 → 2380/2294/0/86 offline` (+32 pass). `npx astro build` ✓ 0 new errors. RPC was already live since p256 (Wave 1a M3); no migration in this PR. | Closed 2026-05-25 (p258) via PR #371 squash-merge; re-open only if a future migration drifts the wizard payload shape from RPC contract (5 forward-defense regressions would catch it in CI) |
| #314(closed) | closed | Frontend / Governance / UX | none | none | p258 — Wave 3 member library + `/documents` alias + retire legacy tab shipped via PR #372 squash `ecb111c7`. **6 new files**: `src/components/governance/GovernanceLibrary.tsx` (React island consuming Wave 1a M3 RPC `list_governance_library(p_filters jsonb)` — filters `doc_type`+`status` persist in URL `?type=…&status=…` for shareable links; cards expose title, status badge, doc_type chip, acknowledgement_mode chip, visibility_class chip, description truncated, effective_from + approved_at dates, link to existing `/governance/document/{id}` reader); `src/pages/governance/documents/index.astro` (PT canonical); `src/pages/en/governance/documents/index.astro` + `src/pages/es/governance/documents/index.astro` (302 redirects with locale query param); `src/pages/documents.astro` + `src/pages/en/documents.astro` + `src/pages/es/documents.astro` (301 aliases per P2-Q3 ratification — `/documents` → `/governance/documents`). **GovernancePage.tsx retired**: `type View` narrowed 4→3 values (`'document'|'approvals'|'changes'` — `'documents'` removed); legacy `?view=documents` URL redirects via `window.location.replace` to `/governance/documents` (preserves bookmarks; locale-aware); legacy `get_governance_documents` RPC call removed from boot() promise array; new cross-link card under the Manual view points to `/governance/documents` with `data-testid="governance-library-crosslink"`. **`src/components/governance/DocumentsList.tsx` DELETED** (no other consumer — grep verified; biblioteca is now the single canonical member-facing surface). 45 new i18n keys in `governance.library.*` × 3 langs (parity-tested) — pageTitle/intro + 5 filter labels + loading/error/empty states + 4 card labels + 11 doc_type labels + 8 status labels (matches CHECK) + 5 visibility labels + 3 acknowledgement_mode labels + 2 cross-link strings. 34 contract assertions in `tests/contracts/p258-314-wave3-member-library.test.mjs` (9 nested describes) with **7 forward-defense regressions**: no PII leak (P0-Q8) — component code MUST NOT reference `file_id` / `drive_url` / `content_html` / `pdf_url`; no table-direct SELECT on `governance_documents`/`document_versions`; `DocumentsList.tsx` MUST stay deleted; legacy `get_governance_documents` RPC call MUST NOT return; `type View` MUST NOT include `'documents'`; legacy `?view=documents` MUST redirect to `/governance/documents`; cross-link `data-testid="governance-library-crosslink"` MUST exist on Manual view. Test added to BOTH `"test"` + `"test:contracts"` whitelists (SEDIMENT-186.C). Test baseline ratchet (isolated): `2348/2262/0/86 → 2382/2296/0/86 offline` (+34 pass); cumulative with PR #371 = `2414/2328/0/86`. `npx astro build` ✓ 0 new errors. RPC was already live since p256 (Wave 1a M3); no migration in this PR. **Mid-session conflict resolution validated**: when PR #371 merged first, rebase of #372 against new main yielded conflict in en-US.ts + es-LATAM.ts + package.json (pt-BR auto-merged); resolution is pure additive — keep BOTH namespace blocks in i18n + keep BOTH test file refs in package.json. Recipe locked for future paired-wave PR scenarios. | Closed 2026-05-25 (p258) via PR #372 squash-merge (rebased post-#371 merge with --force-with-lease push, CI re-validated green pre-merge); re-open only if a future regression breaks one of the 7 forward-defense classes (PII leak / table-direct / deleted file revives / legacy RPC reappears / view enum drifts / bookmark redirect breaks / cross-link disappears) |
| #377(closed) | closed | Foundation / Governance | none | none | p261 — Wave 1b first dispatch (#312-W4b sequence 1/7 in PM-ratified order) shipped via PR #385 squash `e8d7fe92`. Migration `20260805000040` (4 sections): ALTER TABLE `governance_documents.proposer_member_id uuid REFERENCES members(id) ON DELETE RESTRICT` (nullable for historical compat) + COMMENT explaining proposer convention; Frontiers fixture (`18ec4690-…`) backfilled to Fabrício Costa member.id (`92d26057-…`) from p259 evidence (p256 M3 validated but never persisted the value) with sanity DO RAISES on backfill miss; `create_governance_document_intake` extended via minimum-diff CREATE OR REPLACE to persist `proposer_member_id` from payload (preserves all prior behavior — 5-field validation, A1 ack_mode, A2 status, offline audit row, GP-cannot-self-attest guard); new SECDEF `sign_proposer_consent(p_document_id uuid, p_evidence jsonb DEFAULT NULL) RETURNS jsonb` RPC with pinned `search_path TO public, pg_temp`. Gates: caller resolved via `members WHERE auth_id = auth.uid() AND is_active = true` (SEDIMENT-239b.A — FK source members.id NOT auth.users.id); caller MUST equal `governance_documents.proposer_member_id` (GP/admin cannot sign on behalf — matches p256 M3 self-attest guard); doc MUST be in `pending_proposer_consent` status; idempotent return on already-draft (returns `already_signed=true` without double-write). Atomic UPDATE `status=draft` + INSERT `admin_audit_log` action=`governance.proposer_consent_signed` with metadata (document_id + document_title + proposer_member_id + signed_at + evidence + rpc_version=`p261_312_w4b`). Pre-chain capture (approval_chain created later when GP opens workflow via editor lock — distinct from #378 W4a gate templates). Live smoke 3/3 PASS: (1) Fabrício impersonation via SEDIMENT-226.C JWT-claim DO block injection (`PERFORM set_config('request.jwt.claims', json_build_object('sub','5103d397-…','role','authenticated')::text, true)`) → status pending_proposer_consent→draft, audit row anchored at evidence channel `p261_live_smoke`; (2) re-call → returns `already_signed=true`, audit count stays at 1 (no duplicate INSERT); (3) wrong member (Vitor admin auth_id `58675a94-…`) rejected with `Unauthorized: only the named proposer can sign proposer_consent for this document`. **Invariants 21/21 violation_count=0 live** (V_prime_pending_proposer_consent_no_open_chain no longer applies to Frontiers since status=draft; V_status_chain_coherence does not yet apply because draft is not in approved/active). Contract test `tests/contracts/p261-312-w4b-sign-proposer-consent.test.mjs` — 28 assertions (26 static + 2 DB-gated). Static covers: migration file existence + 7-field header (WHAT/WHY/SPEC/SCOPE LOCK/ROLLBACK/INVARIANTS/CROSS-REF) + 7 cross-refs (#312/#315/#96/#377/p256/p259/p260/SEDIMENT-239b.A) + ALTER TABLE with REFERENCES + ON DELETE RESTRICT + nullable forward-defense + COMMENT + backfill exact UUID match + idempotency guards (status + proposer_member_id IS NULL) + sanity DO RAISES + create_governance_document_intake INSERT includes proposer_member_id + SECURITY DEFINER + pinned search_path + GRANT/REVOKE + sign_proposer_consent signature (uuid + jsonb DEFAULT NULL) + SEDIMENT-239b.A caller resolution pattern + caller != proposer rejection + NULL proposer rejection + idempotent already-draft branch + status guard + atomic status flip + audit log action + 6 metadata fields + forward-defense NEVER INSERTs approval_signoffs (pre-chain capture only) + GRANT/REVOKE + NOTIFY pgrst + forward-defense SEDIMENT-235.A no close-keyword + #312/#315/#96 in migration body. DB-gated: Frontiers proposer_member_id matches Fabrício + RPC registered in PostgREST. Test added to BOTH `test` + `test:contracts` whitelists in package.json (SEDIMENT-186.C). Test baseline ratchet: 2414/2326/0/86 → 2442/2354/0/88 offline (+28). CI in-DB mode 2481/2474 pass + 5 skip. **2 hotfixes during CI**: (a) Phase C body drift — apply_migration MCP stripped inline `--` comments from function bodies (live prosrc_len=4249/2885 vs file 5030/3369); fixed per documented pattern (CLAUDE.md/database.md GC-097 sediment: minimum-diff fix = strip from file to match live); (b) SEDIMENT-254.A — shadow row `20260525182624` auto-created by apply_migration MCP alongside canonical `20260805000040`; flagged by Phase C orphan-detection gate; deleted with exact `WHERE version = '20260525182624'` discipline (canonical 20260805000040 preserved). `npx astro build` ✓ 0 new errors. ADR-0097 honored: file synced locally + applied via `supabase migration repair --status applied 20260805000040 --linked`. SEDIMENT-235.A respected: PR title + body audited against close-keyword regex pre-merge; only `Closes #377` appears (#312/#315/#96/#378-#383 references remain neutral). | Closed 2026-05-26 (p261) via PR #385 squash-merge `e8d7fe92`; re-open only if Frontiers Guide regresses from draft to pending_proposer_consent without explicit PM rollback dispatch OR if a future migration drops `governance_documents.proposer_member_id` column |
| #378(closed) | closed | Foundation / Governance | none | none | p262 — Wave 1b dispatch 2/7 #312-W4a shipped via PR #387 squash `15c7b3f0`. Migration `20260805000041` extends `resolve_default_gates(p_doc_type text)` CASE with 6 new branches matching PM-ratified templates from p260 audit §11: editorial_guide (3 gates curator/leader_awareness/submitter_acceptance — informational, no chapter signoff); governance_guideline (3 gates same shape); manual (5 gates mirrors policy template — chapter-wide ratification); executive_summary (2 gates minimal — consumer of other approved docs); framework_reference (3 gates informational); project_charter (2 gates initiative-internal — TAPs). 5 pre-existing branches preserved byte-identical (cooperation_agreement + cooperation_addendum 6 gates each with chapter_witness; volunteer_term_template + volunteer_addendum 5 gates each with volunteers_in_role_active; policy 5 gates). Sanity DO RAISES at migration-time if any of 11 doc_types still returns NULL (forward-defense at apply, not runtime). NOTIFY pgrst end. Function is `LANGUAGE sql` (not plpgsql) — no inline `--` comments inside `AS $$ ... $$` to be stripped by apply_migration MCP (Phase C body drift gate clean). Live verification: all 11 doc_types return non-NULL gates with curator(order=1, threshold=`all`) as first gate in every template; total 45 gates across 11 templates. Frontiers Editorial Guide (in `draft` status post-#377 from p261) now has 3 gates ready for editor lock — can advance `draft → under_review` for the first time. Contract test `tests/contracts/p262-312-w4a-gate-templates-6-doc-types.test.mjs` — 41 assertions (38 static + 3 DB-gated). Static covers: migration file existence + 7-field header + cross-refs (#312/#315/#96/#378/#377/p260); resolve_default_gates signature CREATE OR REPLACE FUNCTION (p_doc_type text) RETURNS jsonb + LANGUAGE sql + pinned search_path TO public, pg_temp; all 11 doc_types in CASE expression; 6 new templates expected gate shapes (kinds + count); curator-first invariant for every new doc_type; 5 pre-existing templates byte-identical preserved (chapter_witness in cooperation_agreement, volunteers_in_role_active in volunteer_term_template, policy 5 gates); sanity DO RAISES on residual NULL; NOTIFY pgrst at end; forward-defense SEDIMENT-235.A migration body MUST NOT contain close-keyword + stay-open issue refs. DB-gated: all 11 return non-NULL live + editorial_guide 3 gates with curator first + ELSE NULL branch preserved (unknown doc_type still returns NULL). Test added to BOTH `test` and `test:contracts` whitelists in package.json (SEDIMENT-186.C). Test baseline ratchet: 2442/2354/0/88 → 2483/2392/0/91 offline (+41 from new contract test + 3 DB-gated skips). Follow-up commit `11f083a5`: `tests/contracts/preview-gate-eligibles-cache-equivalence.test.mjs:135` used `executive_summary` as canonical NULL example — switched to `nonexistent_doc_type_for_test` (clearly fictional) to decouple from gate-template coverage going forward; ELSE NULL branch in resolve_default_gates CASE preserves the assertion intent. SEDIMENT-254.A applied: shadow row `20260525191127` auto-created by apply_migration MCP alongside canonical `20260805000041` registration; deleted with exact `WHERE version = '20260525191127'` discipline. ADR-0097 honored: local file synced + applied via `supabase migration repair --status applied 20260805000041 --linked`. **Invariants 21/21 violation_count=0 live** post-deploy. Frontend `DocumentVersionEditor.tsx` unchanged (already handles non-NULL gate arrays correctly). `npx astro build` ✓ 0 new errors. SEDIMENT-235.A respected: PR title + body audited against close-keyword regex pre-merge; only `Closes #378` appears (#312/#315/#96/#379-#383 references remain neutral). | Closed 2026-05-26 (p262) via PR #387 squash-merge `15c7b3f0`; re-open only if a future migration drops one of the 11 doc_type branches OR if curator(order=1, threshold=`all`) invariant is violated in any branch (forward-defense locks regression class in CI permanently) |
| #379(closed) | closed | Foundation / Governance | none | none | p262.W4c — Wave 1b dispatch 3/7 #312-W4c (GAP-259.A close) shipped via PR #389 squash `eb72a926`. Migration `20260805000042` extends `list_governance_library` body with ONE new WHERE clause line `AND (v_filter_status IS NOT NULL OR gd.status IN ('active','approved','under_review','superseded'))`. Effect: member library default call (no `p_filters.status`) excludes 4 non-consultable statuses (`draft` + `pending_proposer_consent` + `withdrawn` + `revoked`); explicit `p_filters.status='draft'` (or any of the 4) still returns matching docs (admin/audit override preserved via existing `v_filter_status IS NULL OR gd.status = v_filter_status` line). All 5 visibility-class branches preserved byte-identical (public + active_members + legal_scoped with member_document_signatures join + admin_only with v_is_admin + audit_restricted with v_is_platform_admin). Function header (CREATE OR REPLACE + LANGUAGE plpgsql + STABLE + SECURITY DEFINER + pinned search_path TO public, pg_temp) preserved. GRANT EXECUTE TO authenticated + REVOKE FROM PUBLIC preserved. Payload P0-Q8 forward-defense preserved (12 canonical fields in jsonb_build_object SELECT; no file_id/drive_url/content_html/pdf_url leak). Function comments inside AS $$ ... $$ preserved byte-identical (apply_migration MCP did NOT strip this time — comments use straight ASCII patterns matching live state). Live verification post-deploy: (1) `WITH 16 govdocs SELECT count WHERE status IN (4-include)` = 13 visible by default; (2) `WHERE status IN (4-exclude)` = 3 hidden (Frontiers `18ec4690-…` in `draft` post-#377 + 2 manual drafts); (3) Smoke 1 (Vitor impersonation via SEDIMENT-226.C JWT-claim DO block + default filter): Frontiers excluded ✓; (4) Smoke 2 (Vitor impersonation + explicit `status='draft'` filter): Frontiers appears ✓ (admin override preserved). **Invariants 21/21 violation_count=0 live** post-deploy (V_prime_pending_proposer_consent_no_open_chain + V_status_chain_coherence + 19 others all green). **PM ratification chain**: p259 close PM Option (a) ratified for GAP-259.A → p262 PM #379 prompt confirmed (3 statuses listed: approved/active/superseded conditional) → p262 PM AskUserQuestion ratified Option A (4-status default include set: active + approved + under_review + superseded, matching p259 v2 + STATUS_FILTER_OPTIONS dropdown). Contract test `tests/contracts/p262-312-w4c-gap-259-a-default-status-exclusion.test.mjs` — 20 assertions (17 static + 3 DB-gated). Static covers: migration file existence + 7-field header + 8 cross-refs (#312/#315/#96/#379/#378/#377/GAP-259.A/p259); function signature CREATE OR REPLACE + LANGUAGE plpgsql + STABLE + SECURITY DEFINER + pinned search_path; GRANT/REVOKE preserved; new default-exclusion WHERE line present; existing explicit-filter line preserved; **FORWARD-DEFENSE 4-status include set is EXACTLY active/approved/under_review/superseded** (excluded statuses MUST NOT appear in the include set — regex locks regression class permanently); all 5 visibility-class branches preserved (legal_scoped with member_document_signatures + admin_only with v_is_admin + audit_restricted with v_is_platform_admin); payload P0-Q8 forward-defense (12 canonical fields + 4 forbidden field names absent from jsonb_build_object); SEDIMENT-235.A migration body MUST NOT contain close-keyword + stay-open issue refs. DB-gated: Frontiers excluded from default include set + appears under explicit draft filter + corpus 13/3 split confirmed via direct SELECT. Test added to BOTH `test` and `test:contracts` whitelists in package.json (SEDIMENT-186.C). Test baseline ratchet: 2483/2392/0/91 → 2503/2409/0/94 offline (+20 total / +17 pass / +3 skip). SEDIMENT-254.A applied: shadow row `20260525193317` auto-created by apply_migration MCP alongside canonical `20260805000042` registration; deleted with exact `WHERE version = '20260525193317'` discipline. ADR-0097 honored: local file synced + applied via `supabase migration repair --status applied 20260805000042 --linked`. `npx astro build` ✓ 0 new errors (STATUS_FILTER_OPTIONS in GovernanceLibrary.tsx already lists 4 statuses matching new default — no FE change needed). SEDIMENT-235.A respected: PR title + body audited against close-keyword regex pre-merge; only `Closes #379` matches the intended close target. | Closed 2026-05-26 (p262.W4c) via PR #389 squash-merge `eb72a926`; re-open only if a future migration drops the default-exclusion WHERE line OR if STATUS_FILTER_OPTIONS dropdown drifts from the 4-status default include set (forward-defense locks regression class in CI permanently) |
| #333 | blocked | Foundation / QA / Audit | C5 #335 ADR Accepted status | (a) C2 #332 closed OR (b) PM accepts 1-row allowlist | `check_schema_invariants()` invariant U registered + post-cleanup `violation_count=0` + CI ratchet blocks regressions | Close after invariant U live + `violation_count=0` |
| #334 | blocked | Governance / Legal-ops | C2 #332 final shipping (notification template); C5 #335 ADR Accepted (audit chain) | Angeline async response (3-5 business days typical) | All 5 docs under `docs/audit/lgpd-art11-remediation/` (Art. 48 opinion + notification template + DPO stance + Adendo Privacidade + Termo de Speaker) + Adendo Privacidade published | Close after all 5 docs landed + Angeline written opinion attached |


## Close Candidates / QA Evidence Needed

| Issue | Registry status | Lane | Blocks | Blocked by | Acceptance evidence | Close rule |
|---|---|---|---|---|---|---|
| #217 | close-candidate | Foundation / QA | none | final curator/PM close comment | Migration `20260802000007` + contract test show `/initiative/` and not `/iniciativas/` | Close after posting evidence summary to issue |
| #227 | close-candidate | Foundation / QA | none | PM browser smoke for CV button | Storage policy uses `rls_can('view_pii')`; forward-defense test exists | Close after `/admin/selection` CV signed URL smoke confirms no 400 |


---

## Program Clusters

| Cluster | Issues | Registry stance | Dispatch rule |
|---|---|---|---|
| LGPD Art. 11 voice biometric remediation | #221(closed), #218(closed) + #331/#332/#333/#334/#335 children | Engineering moat shipped p207 (drop trigger + voice biometric consent columns + helper gate); remaining UI + retroactive notification + invariant + legal-ops + ADR pending in 5 narrow children | Dispatch C1 #331 + C2 #332 in parallel (different lanes; C2 can ship with interim PM-approved text until C4 #334 Angeline template). C3 #333 after PM sequencing call on (a) C2-first vs (b) allowlist-first. C4 #334 blocked on Angeline async response (3-5 business days typical). C5 #335 blocked on C1/C2/C3 closes. C4 NOT required for C5 Accepted (legal-ops parallel to architectural decision). |
| Selection reliability Cycle 4 | #292, #251(closed), #298(closed), #260(qa-window), #116(closed), #318(closed), #179(closed), #229(closed), #230(closed) + #321(closed)/#322(closed)/#323(closed) children, #243, #254 | P0 sequencing program | #251 audit + #298 fix + #260 W2 ALL 7 LEAVES SHIPPED + replay live (p228) — qa-window for 7d production smoke only. #116 calendar webhook smoke PASS + closed p229. #318 A3 invariant fixed p230 (PR #320). **p230 close #179 + reframe #230**: #179 closed as IMPLEMENTED (5/5 ACs met live; canonical RPC + delegating legacy paths + invariants R/S/A3 + contract test). #230 reframed into umbrella with 3 ready-leaf children — #321 phantom-row sync trigger, #322 classification leftovers + forward guard, #323 catalog config. **p232 #229 Phase 2 closed**: leader_extra cohort visibility in 3 read RPCs (get_pert_cutoff_summary dual-track CHECK + math; get_application_score_breakdown adds leader_extra_cutoff block; get_selection_dashboard.cycle exposes sibling block); MCP v2.79.1 with `leader_extra_pert_score` in z.enum + dual-dim descriptions; frontend `/admin/selection` 2-band chip; +12 contract tests; live smoke 3/3 PASS (method=disabled expected — cohort_n=6 < 10). **p233 #321 Gap A closed** (PR #326): migration `20260805000018` AFTER INSERT trigger on certificates + 30-row backfill + 12 contract tests + sanity DO block + live smoke 0 phantom + invariants 19/19=0. **p234 #322 Gap B closed (via PR #327, 2 migrations)**: initial migration `20260805000019` backfill 8 rows to `skipped` + forward guard in `approve_selection_application` + offboarding extension in `admin_offboard_member` + `get_my_onboarding` harmonizes `skipped`≡`completed`; close-review patch `20260805000020` mirrors guard in `get_my_onboarding` auto-seed (PM curator caught unguarded re-entry vector). 20 contract tests + sanity DO + live goal_metric=0 + invariants 19/19=0. **p235 #323 Gap C closed**: migration `20260805000021` applies PM per-kind ABCD decision on `engagement_kinds` catalog — `study_group_owner` keeps `requires_agreement=true` and gains placeholder `agreement_template='study_group_owner_agreement_v1'` (preserves ADR-0006/0008 termo intent; mirrors ADR-0078 D5 external_reviewer slug placeholder precedent; template body deferred to follow-up legal-counsel issue); `study_group_participant` flips `requires_agreement=false` (legal_basis stays `contract`; course enrollee model per ADR-0008 "termo de uso" read as platform-wide TOS). Sanity DO block enforces invariant (`requires_agreement=true` ⇒ `agreement_template IS NOT NULL` OR `slug IN ('volunteer')`). 12 contract tests (11 static + 1 DB-gated). Live smoke: 0 catalog offenders + Fernando participant row → `is_authoritative=true` with zero new capabilities (role=leader doesn't match role=participant write_board seed; role=participant seed applies prospectively as PM intended) + owner row stays pending termo + invariants 19/19=0. All 3 children of #230 shipped; PR #328 already closed the umbrella, and this handoff PR records the evidence summary. Next dispatch (post-#230 close): keep #243/#254 spec/read-only behind #221/#218 consent blockers; #300 deferred unless PM escalates |
| Curatorship p197 | #185-#196, #188, #190, #201 | Needs parent status board | Allow max 2 ready leaves concurrently; serialize DB changes |
| Volunteer lifecycle | #177, #179(closed), #180, #181, #182, #183, #205, #213, #230(closed) + #321(closed)/#322(closed)/#323(closed) | Foundation sequence | Start after #221 containment; #179 closed as IMPLEMENTED p230 (canonical contract gate shipped); #230 reframed into umbrella with 3 ready-leaf children — #321 sync trigger closed p233 (trigger + backfill live, invariants 19/19=0), #322 closed p234 (8-row backfill + forward guard + offboarding extension + UX harmonize, goal metric=0 live), #323 closed p235 (catalog config: study_group_owner placeholder slug + study_group_participant flip, 12 tests, 0 live offenders). All 3 children shipped; umbrella close was recorded by PR #328 and this handoff PR records the evidence summary |
| MCP/AI | #162, #163, #170, #183, #188, #206-#208 | High-risk | Pause new tools until #162 contract matrix and #221 consent gates are stable |
| Attendance/calendar | #104, #107, #116, #156, #157, #172, #210 | Stabilization wave | Treat as one semantic cohort/cadence program |
| Drive/identity/collaboration | #110, #204, #205, #209, #211, #212, #254 | Architecture first | Drive permission lifecycle must follow engagement source-of-truth decision; video file reconciliation must start with read-only evidence |
| Governance Documents v1 | #315 (Wave 1a Foundation shipped p256 PR #366 `f728a89a`) + #367(closed) (Wave 1b first leaf shipped p257 — V invariant ACTIVATED as 21st with 0 violations) + #310(closed) (Wave 2 admin intake shipped p258 PR #371 `da83327f`) + #314(closed) (Wave 3 member library + `/documents` alias + retired legacy tab shipped p258 PR #372 `ecb111c7`) + #312 (Wave 4 QA — next-in-line for v1), #96 (Frontiers launch — Wave 2 infra fulfilled; operational fixture pending), #311/#308/#181 (Wave 6 evidence/certificate children), #301 (Wave 5 Drive grants child) | Wave 0 closed 2026-05-24 (PR #364 `decb35a9`). **Wave 1a Foundation shipped p256 PR #366 merge `f728a89a`** (3 migrations + 2 RPCs + 72 contract assertions; V invariant DEFERRED → #367). **Wave 1b first leaf #367 shipped p257** (2 migrations 20260805000038+039 + 46 contract assertions; V_status_chain_coherence ACTIVE as 21st invariant with 0 violations live; 7 legacy pre-chain docs anchored via PM-ratified synthetic chains; NO carve-out — forward-defense in CI locks regression class). **Wave 2 #310 shipped p258 PR #371** (admin intake wizard `DocumentIntakeWizard.tsx` consuming `create_governance_document_intake`; 32 contract assertions with 5 forward-defense regressions F1-F5; 54 i18n keys × 3 langs). **Wave 3 #314 shipped p258 PR #372** (member library `GovernanceLibrary.tsx` consuming `list_governance_library`; new `/governance/documents/index.astro` (PT canonical + EN/ES redirects) + `/documents` alias 301 per P2-Q3; retired `?view=documents` tab in GovernancePage.tsx (type View narrowed 4→3) + deleted `DocumentsList.tsx` + cross-link card; 34 contract assertions with 7 forward-defense regressions; 45 i18n keys × 3 langs). Cumulative test baseline: `2348 → 2414/2328/0/86 offline` (+66 pass). Multi-wave roadmap with v1 scope = Waves 1a+2+3 (✓ all shipped), Wave 4 next; Waves 5/6/7 deferred post-v1 | **Waves 1a, 1b first leaf, 2 and 3 ✓ all shipped**. Wave 4 #312 (audit jornada review/comment/approval persona smoke + curadoria per instrumento) is **next-in-line for v1 close**. **Frontiers fixture #96 operationally smoked p259** — `create_governance_document_intake` returned `document_id=18ec4690-4f5a-4cab-904d-451e2c7245bf` with `status=pending_proposer_consent` (canonical A2 path), library payload includes it (16/16 PostgreSQL JSON return), invariants 21/21=0, 0 audit side-effect by design. SPEC §9 acceptance criteria honored end-to-end (PM-ratified A2 supersedes §9 status enumeration `draft OR under_review`). Wizard JSX not exercised (PM may walk browser modal separately for visual UX evidence). SPEC `docs/specs/SPEC_GOVERNANCE_DOCUMENTS_END_TO_END.md` §19 captures the ratified matrix + amendments A1/A2/A3 + Wave 1a/1b/2/3 footprint. **Do not pull Wave 5/6/7 into v1**; PM directive p256 explicit. **Carries post-Wave 2/3 (deferred per PM Recommended)**: (a) Wave 1a known regression — curator-draft-access mitigation Roberto Macêdo + Sarah Faria (`document_versions_read_published` swap removed `curate_content`/`manager`/`deputy_manager` bypass; only `manage_member` is admin bypass); Wave 1b separate leaf (NOT #367, NOT #310/#314) ships dedicated curator-draft-access policy or RPC; (b) `/governance/document/[id].astro` reader hardening (currently table-direct SELECT on `governance_documents` + `document_versions`, pre-Wave-1a OPP-153.1; behavior post-RLS-swap uncertain; separate leaf to investigate + harden via SECDEF RPC if needed); (c) `list_governance_library` payload extension for `depends_on` / templates (Wave 1b expansion deferred until concrete dependency-graph need surfaces). **NEW carry p259** (PM-ratified at session close 2026-05-25): (d) GAP-259.A — `list_governance_library` default unfiltered payload includes ALL statuses (incl. `pending_proposer_consent`/`draft`/`withdrawn`/`revoked`) while `STATUS_FILTER_OPTIONS` in `GovernanceLibrary.tsx:69` only exposes 4 operational statuses in the dropdown. **PM picked Option (a)**: server-side default exclusion in RPC body — member library MUST exclude `draft`/`pending_proposer_consent`/`withdrawn`/`revoked` by default; explicit `p_filters.status` override allowed only for admin/audit contexts. Rationale: biblioteca membro representa documentos consultáveis/vigentes, não fila de intake/revisão. Implementation tracked as separate Wave 1b leaf — minimum change = server-side WHERE clause adding `(v_filter_status IS NOT NULL OR gd.status IN ('active','approved','under_review','superseded'))` + forward-defense contract test asserting default exclusion. Out of scope for #312 Wave 4 audit (NOW tracked as `#312-W4c` leaf candidate). **p260 Wave 4 #312 audit jornada shipped + PM D1-D5 ratified + 7 children spawned (this session)**: `docs/audit/p260_wave_4_312_journey_audit.md` — read-only audit grounded in code+RPCs+live corpus produces persona × shell matrix (SPEC §11), instrument × review_mode matrix (SPEC §6.1), gate template coverage probe (5/11 doc_types supported), comment visibility drift analysis (4 in drawer / 3 in RPC / 5 legacy `public` rows), 5 PM decisions D1-D5 ratified at session close + appendix §15.5 captures verbatim. **PM dispatch sequence (7 children spawned)**: #377 (W4b sign_proposer_consent v1-must seq 1/7) · #378 (W4a gate templates v1-must seq 2/7, blocked by #377) · #379 (W4c GAP-259.A v1-must seq 3/7, blocked by #378) · #380 (W4d reader hardening v1-must seq 4/7, blocked by #379) · #381 (W4e curator draft-read v1-must seq 5/7, blocked by #380) · #382 (W4f blind-review primitives v1-follow-up seq 6/7, blocked by 5 v1-must) · #383 (W4g content_products ADR v1-follow-up seq 7/7, **PM D3 ratified `content_products` canonical artifact**, `board_items` continues operational tracking only). 8/8 personas smoke verdict: 4 ✓, 3 ⚠, 1 ✗ (Curador independente — addressed by #382 follow-up). **D5 disposition**: 5 legacy `visibility='public'` rows preserved as-is (PM ratified — não deletar, não reinterpretar). Wave 5/6/7 deferrals preserved per PM directive p256. **p261 Wave 1b first dispatch #377 SHIPPED + CLOSED**: PR #385 squash-merged `e8d7fe92`. Migration `20260805000040` (4 sections) + new SECDEF RPC `sign_proposer_consent(uuid, jsonb)` + Frontiers fixture backfilled with Fabrício Costa member.id + contract test `p261-312-w4b-sign-proposer-consent.test.mjs` (28 assertions, 26 static + 2 DB-gated). Live smoke 3/3 PASS: Frontiers `pending_proposer_consent → draft` via Fabrício impersonation (SEDIMENT-226.C JWT-claim DO block); idempotent already-draft branch returns `already_signed=true`; wrong-member rejected. Invariants 21/21=0 live. 2 hotfixes during CI: (a) Phase C body drift — apply_migration MCP stripped inline `--` comments inside function bodies, fixed per documented pattern (strip from file to match live); (b) SEDIMENT-254.A shadow row `20260525182624` deleted with exact `WHERE version` discipline (canonical 20260805000040 preserved). **#378 W4a gate templates** is now next dispatch in PM-ratified sequence (2/7 — unblocked). **p262 Wave 1b dispatch 2/7 #378 SHIPPED + CLOSED**: PR #387 squash-merged `15c7b3f0`. Migration `20260805000041` extends `resolve_default_gates(p_doc_type text)` CASE with 6 new branches (editorial_guide/governance_guideline/project_charter/manual/executive_summary/framework_reference) + sanity DO at migration-time forward-defense. 5 pre-existing branches preserved byte-identical. Live: all 11 doc_types now return non-NULL gates (count: 6+6+5+5+5+3+3+5+2+3+2 = 45 gates total). Frontiers Editorial Guide (in `draft` post-#377) can now advance `draft → under_review` via editor lock for the first time. Contract test `p262-312-w4a-gate-templates-6-doc-types.test.mjs` 41 assertions (38 static + 3 DB-gated). Follow-up commit fixed `preview-gate-eligibles-cache-equivalence.test.mjs` (executive_summary no longer NULL → uses fictional `nonexistent_doc_type_for_test`). Invariants 21/21=0 live. SEDIMENT-254.A applied (shadow row `20260525191127` deleted). **#379 W4c GAP-259.A** is now next dispatch in PM-ratified sequence (3/7 — unblocked). **p262.W4c Wave 1b dispatch 3/7 #379 SHIPPED + CLOSED**: PR #389 squash-merged `eb72a926`. Migration `20260805000042` extends `list_governance_library` body with ONE new WHERE clause line `AND (v_filter_status IS NOT NULL OR gd.status IN ('active','approved','under_review','superseded'))`. Effect: member library default call now excludes `draft`/`pending_proposer_consent`/`withdrawn`/`revoked`; explicit `p_filters.status` override preserved. All 5 visibility-class branches + GRANT/REVOKE + SECDEF/STABLE + payload P0-Q8 forward-defense preserved byte-identical. PM ratification chain: p259 PM Option (a) → p262 PM #379 prompt → p262 PM AskUserQuestion 4-status default include set. Live smoke 2/2 PASS (Vitor impersonation: default hides Frontiers + explicit draft filter shows Frontiers). Corpus split: 13/16 visible by default + 3 hidden. Contract test `p262-312-w4c-gap-259-a-default-status-exclusion.test.mjs` 20 assertions (17 static + 3 DB-gated). Invariants 21/21=0 live. SEDIMENT-254.A applied (shadow row `20260525193317` deleted). **#380 W4d reader hardening** is now next dispatch in PM-ratified sequence (4/7 — unblocked) — extends `/governance/document/[id].astro` to use new SECDEF RPC `get_member_governance_doc` instead of table-direct SELECT. |

---

## Dispatch Rules

1. Assign implementation agents only to `ready-leaf` issues.
2. `spec-only` issues may produce docs, ADRs, and child issues only.
3. `qa-window` issues are read-only unless smoke fails.
4. Foundation migrations serialize unless explicitly proven independent.
5. MCP/AI work must name data touched, consent basis, RPC/tool contract, and smoke evidence.
6. Any branch touching multiple lanes needs a documented exception or a split.
7. During the Selection reliability sprint, **#260 Workstream 2 ALL 7 leaves are SHIPPED end-to-end (p228)**: PR #305 (Leaf 1 catalog/helper parity) + PR #307 (Leaves 2 interview_overdue cron + 3 soft AI gate + 4 selection_cutoff_approved foundation + 5 selective replay RPC + 6 operational suppress_all bypass + 7 24h health signal) + PR #313 Leaf 5 hotfix + PR #316 P162 hotfix addendum. Replay executed live (`updated_count=2`; 15 manual_close), so #260 remains `qa-window` for 7d production smoke only. **p229 #116 closed**: live smoke PASS — 22 webhook-synced rows since p95 (2026-05-06), 92% cycle4 sync rate, idempotency + B3 reschedule-clearance both live, most-recent fire 2 days pre-close. P162 #195 carries WATCH-116.A (webhook service_role bypasses `schedule_interview` gate by design; `gate_attempts` audit visibility weaker than RPC path — non-blocking, documented). **#318 A3 invariant drift fixed p230**: PR #320 added `chk_a3_active_role_not_none`, repaired Herlon via canonical V4 ladder, and restored check-invariants to 19/19=0 with no bypass. **p230 #179 closed as IMPLEMENTED**: 5/5 ACs met live (canonical `approve_selection_application` exists; `admin_update_application` + `finalize_decisions` delegate with RAISE on failure; invariants R/S/A3 green; `canonical-approval-orchestration.test.mjs` covers contract); implementation continues under children. **p230 #230 reframed as umbrella**: live audit refuted Herlon-specific premise (his engagements don't require agreement); 16→4 pending_agreement (none `volunteer` kind); spawned 3 ready-leaf children #321 (phantom row sync trigger, ~30 rows) + #322 (classification leftovers + forward guard; re-audit post-#321 baseline first, latest P162 says 4 active + 4 inactive) + #323 (catalog config: `study_group_*` requires_agreement=true with null template). Do NOT mint Herlon term. **p232 #229 Phase 2 closed**: leader_extra cohort visibility completed via migration `20260805000017` (3 read RPCs extended) + MCP v2.79.1 (`score_column` z.enum adds `leader_extra_pert_score` + descriptions clarify dual-dim) + frontend `/admin/selection` 2-band chip + 12 new contract tests; live smoke 3/3 PASS; method=disabled expected because cohort_n=6 < 10 threshold (will turn dynamic once 10+ leader-track approvals exist historically). **p233 #321 Gap A closed** (PR #326 — migration `20260805000018` AFTER INSERT trigger on certificates + 30-row backfill via DISTINCT ON CTE + 12 contract tests + sanity DO block + live smoke 0 phantom + invariants 19/19=0). **p234 #322 Gap B closed (PR #327, 2 migrations)**: initial migration `20260805000019` (backfill 8 rows to `skipped` + 3 RPC changes: approve_selection_application guard + admin_offboard_member offboarding extension + get_my_onboarding harmonize) + close-review patch `20260805000020` (PM caught unguarded auto-seed in get_my_onboarding — patched to mirror approve_selection_application guard with EXISTS check on engagements+engagement_kinds for active + requires_agreement=true). 20 contract tests (19 static + 1 DB-gated) + sanity DO block + live smoke goal_metric=0 + invariants 19/19=0. **p235 #323 Gap C closed**: migration `20260805000021` applies PM per-kind ABCD decision — `study_group_owner` keeps `requires_agreement=true` + placeholder `agreement_template='study_group_owner_agreement_v1'` (ADR-0078 D5 external_reviewer pattern); `study_group_participant` flips `requires_agreement=false` (legal_basis stays `contract`; course enrollee model). Sanity DO enforces catalog invariant. 12 contract tests (11 static + 1 DB-gated; +2 forward-defense ratchets for both kinds). Live smoke: 0 catalog offenders + Fernando participant → is_authoritative=true with zero new capabilities (role=leader doesn't match the role=participant write_board seed; participant seed applies prospectively as PM intended) + owner row stays pending termo + invariants 19/19=0. **All 3 children of #230 umbrella shipped — #230 auto-closed via PR #328 merge (registry text matched GitHub auto-close regex); evidence summary posted at comment 4526837462**. p230 fast-follow carries: auto-trigger design for selection_cutoff_approved (cron schedule + threshold evaluation) + MCP tool registration for `get_selection_emails_pending_24h` + WATCH-116.A audit-visibility evaluation (if PM wants symmetric webhook logging). p226 carries: cycle4 committee seed (no longer urgent post-Leaf 3 soft gate) + 19 cycle4 screening apps status advance. #298 + #116 + #318 + #179 + #229 + #321 + #322 CLOSED. **p240 #251 closed**: PM reproduced 2026-05-24 — Luíse Quintana + William Junio (researcher) + 8 others in cycle4-2026 labelled `Aguardando Entrevista` despite completed interviews + submitted interview evals. Root cause: 3 silent gates fragmented status-advance — calendar webhook (#116) inserts `selection_interviews` direct without app status sync; `mark_interview_status('completed')` no-ops silently when source status is `interview_pending` (precondition `status IN ('interview_scheduled','interview_done')`); `submit_interview_scores` only advances when ALL `interviewer_ids` submitted (cycle4 has Vitor+Fabricio as evaluators with only Vitor submitting). Fix: migration `20260805000025` adds AFTER INSERT OR UPDATE OF (status, conducted_at) trigger on `selection_interviews` that derives target app status from canonical interview-row evidence + backfill of 14 rows (10 cycle4 + 4 cycle3-2026-b2) with audit reason `p240_251_interview_status_transition_backfill` + sanity DO block + terminal-status no-op guard (PM directive: trigger nunca toca terminal). 13 contract tests (11 static + 2 DB-gated). Live smoke 3/3 PASS (Luíse → `interview_done` + William researcher → `interview_done` + Rafael Bellotti → `interview_scheduled`); William *leader* preserved as `rejected` (terminal guard). Invariants 19/19=0. **p241 WATCH-240.A shipped**: migration `20260805000026` hoists `UPDATE selection_interviews SET conducted_at = now()` out of `submit_interview_scores`'s all-submitted branch (idempotency guard `IF v_interview.conducted_at IS NULL`) so partial-submission paths fire the p240 trigger reliably — closes the residual fragility for any NEW interview where 1-of-N evaluator submits and the other never does. Same-signature CREATE OR REPLACE, no consumer break. 15 contract tests (11 static + 2 forward-defense + 2 DB-gated; baseline 1938/1874/0/64 → 1953/1887/0/66). Forward-defense regex catches any future re-introduction of `conducted_at = now()` inside the all-submitted branch. Live body-hash drift check confirms `md5(normalized prosrc)='f9cf4bde13a3145f6702d5543661cf79'` matches local file. P162 entry #209 RESOLVED-WATCH-240.A anchors. **p242 WATCH-240.C closed (audit doc, PR #345)**: cycle4 + cycle3-b2 PERT cutoff calculation gap was simply "cron never had its first window yet" (jobid 47 installed p228 Sat, first fire Mon 2026-05-25 13:00 UTC). Manual `recompute_all_active_pert_cutoffs()` populated all 38+2 apps; 11 above-target identified (6 already in interview_* + 5 in screening). `docs/audit/CYCLE4_PERT_CUTOFF_P242_WATCH_240_C.md` + P162 #210 anchor. **Audit-corrected canonical workflow** for the 5 above-target screening apps: PM runs `notify_selection_cutoff_approved('<app_id>'::uuid)` per-app from authenticated MCP-Claude session (gate: committee lead OR `manage_member`); RPC dispatches `selection_cutoff_approved` email with `interview_booking_url` CTA via `campaign_send_one_off` → Resend; status advance happens *naturally* after candidate books via the calendar webhook (#116) → `schedule_interview` → p240 trigger advances `screening → interview_scheduled`. **NOT** `finalize_decisions` (that's the FINAL committee decision RPC `approved/rejected/waitlisted/converted` AFTER interviews — a different stage). `status='objective_done'` exists in code only as a variable name `v_objective_done int`, never written as an enum value — confirms there's no intermediate stage between `screening` and `interview_scheduled`. 2 below-band apps + 1 in-band borderline deferred to committee. Pre-dispatch check: `cycle4-2026.interview_booking_url` must be set (otherwise raises `CUTOFF_NO_BOOKING_URL` errcode P0020). **p236 disposition #221/#218 → 5 ready-leaf children**: Both umbrellas DECOMPOSED per PM Option A — #221 closed as IMPLEMENTED (Wave 1 engineering moat shipped p207); #218 closed as REFRAMED. 5 narrow children spawned: #331 (W2 UI + i18n, ready-leaf), #332 (W3 retroactive notification, ready-leaf with C4 interim), #333 (W4 invariant U, blocked on PM sequencing call), #334 (W5 Angeline legal-ops, blocked external dep), #335 (ADR-0094 spec, blocked on C1/C2/C3). Registry "consent blockers resolved or explicitly decomposed" clause satisfied → #243/#254 spec/audit work unblocked; PM dispatched #254 read-only audit + #243 spec-only child split as next p236 sequence after #221/#218 disposition. Keep #300 deferred unless PM explicitly escalates a narrow active-data-corruption child. **p238 #331 closed**: Wave 2 UI consent capture shipped — migration `20260805000022` extends `give_consent_via_token` to accept `voice_biometric` + REQUIRED `p_evidence` (version + lang + SHA-256 of displayed destacado label) + extends `consume_onboarding_token` payload; `PMIOnboardingPortal.tsx` gains amber destacado section + `sha256Hex` helper + `handleVoiceConsentToggle` handler + video upload gate on `hasVoiceConsent`; i18n `privacy.s4.openaiWhisper` + 7 voiceConsent* keys in 3 langs; 23 contract assertions; live smoke 3/3 PASS. **p238b #332 infra shipped (status `ready-to-dispatch`)**: Wave 3 retroactive notification + deletion audit-log infrastructure shipped — migration `20260805000023` adds `pii_access_log.deletion_artifacts jsonb` + 2 SECDEF RPCs (`lgpd_record_retroactive_notification` + `lgpd_execute_retroactive_deletion`) both gated on `manage_member`; PM-approved interim notification template at `docs/audit/lgpd-art11-remediation/notification_eduardo_luz_p238_interim.md` (pt-BR primary + en-US fallback); 20 contract assertions; live smoke 2/2 PASS (both RPCs gate correctly). **p239b #332 operator surface shipped + accessor_id FK hotfix + actual dispatch end-to-end CLOSED (PR #342)**: 3-commit PR — (1) +2 MCP tools wrap the p238b RPCs (tool count 299→301; nucleo-ia-hub v2.78.1→v2.79.0; ef_version v2.79.1→v2.80.0); (2) hotfix migration `20260805000024` for `accessor_id := auth.uid()` → `v_caller_member_id` FK violation (the p238b RPCs INSERT into `pii_access_log` whose `accessor_id` FK targets `members(id)` not `auth.users(id)`; service-role smoke missed it because gate ladder fires first); (3) ratchet doc updates + GH issue close. PM dispatched the interim_v1 email to Eduardo Luz 2026-05-24T00:29:00-03:00, then invoked `lgpd_record_retroactive_notification` → `pii_access_log.id = ee6a1541-bef6-4f7c-be69-c1e7efbc6568` (chain anchor). 9 forward-defense contract assertions in new `p239b-332-hotfix-accessor-id-fk.test.mjs` lock the FK-column-source regression class. **#332 CLOSED p239b via PR #342 squash-merge**. Next sprint dispatch: #333 (W4 invariant U — still blocked on PM sequencing call: (a) wait for Eduardo's deletion response within 30 days vs (b) immediate 1-row allowlist), then #334 (W5 Angeline legal-ops blocked external 3-5 business days). **p243 cycle4 cutoff dispatch operationally complete (PR #346 + post-merge PM intervention)**: 5/5 above-target screening apps dispatched live 2026-05-24 16:39-16:40 UTC via `notify_selection_cutoff_approved('<app_id>')` per-app (service-role + `SET LOCAL request.jwt.claims` SEDIMENT-226.C pattern simulating Vitor); booking URL `https://calendar.app.google/XPiGWLh9JaLVFKJc6` populated on cycle4-2026; idempotency LOCKED (`cutoff_approved_email_sent_at` populated 5x); Resend campaign send chain anchored with `rpc_version='p228_w2_leaf4'`. Apps now in booking-wait phase (natural advance via candidate-booking webhook #116 → schedule_interview → p240 trigger → `interview_scheduled`). Cron jobid 47 first fire Mon 2026-05-25 14:00 UTC observability armed via scheduled remote agent `trig_01GCFWXgzN1dWndKXRv34eVC`. Issue #348 captures PM's 4-step booking_url evolution roadmap (Step 1 ✅ done, Steps 2-4 backlog). **p244 WATCH-240.B closed (audit-only no remediation, PR #349)**: audit `docs/audit/CYCLE3_B2_LATE_EVAL_P244_WATCH_240_B.md` finds (1) p240 backfill touched 0 cycle3-b2 rows live (all 14 are cycle4-2026; P162 #208 propagation attribution error corrected); (2) cycle3-2026-b2 has 2 apps both already `approved` via `application.status_manual_advance` 2026-05-09 — Herlon (leader 252) + João U. (researcher 137) administrative pre-cycle4 onboarding cases; (3) "4 late-eval" reframed as Herlon's 4 non-interview eval rows submitted 2.4-3.2d post-interview, all legitimate PM/curator post-hoc in administrative onboarding window; (4) no structural remediation needed. Docs-only PR, 0 migrations, 0 schema changes. 1 NEW SEDIMENT-244.A (handoff narrative propagation drift — live-query before propagating count claims through subsequent handoffs). **p245 #229a per-candidate PERT classification SHIPPED (PR TBD)**: `src/pages/admin/selection.astro` gains 4 new helpers (`bandClassify`/`formatDelta`/`pertBandChip`/`pertFullTooltip`/`leaderExtraChip`) + 2 per-app TD refactors. research_score TD gains explicit band-position chip + enriched tooltip (target/banda/cohort/método). **leader_score TD strips `bandColorClass(r.leader_score, currentCycle?.pert_cutoff)` to honor PM rule "Não comparar líder contra a régua objetiva"** + appends leader_extra dimension chip wired to `leader_extra_cutoff` (with disabled-reason chip when cycle cutoff method=disabled per cohort_n<10 threshold). 5 new i18n keys × 3 langs (15). 30 new contract tests (29 helper/wiring + 2 forward-defense regressions locking the PM-rule-violating leader_score×pert_cutoff pattern). Baseline 1953/1887/0/66 → 1983/1917/0/66 offline (+30). 0 migrations / 0 RPC changes (data populated p242 recompute). Cycle4 state: 38/38 objective PERT populated, 3 apps with leader_extra_pert_score, cycle leader_extra_cutoff=disabled cohort_n=6<10 (expected). P162 entry #212 RESOLVED-#229a-READ-SURFACE. **p246 #229b Foundation SHIPPED**: Final-score PERT régua by track via migration `20260805000027` (6 new `final_score_pert_*` columns + 4-dim recompute + 2 cycle blocks scoped per track). **p247 #229b Frontend SHIPPED**: per-candidate Final + Interview score on /admin/selection (frontend-only, 0 migrations). **p248 OPP-246.A CLOSED**: `submit_evaluation` canonical final_score via migration `20260805000028` + Francisleila cleanup (drift 309→158.30 canonical). **p250 SPEC #348 Child #1 #354 SHIPPED via PR #358**: Foundation DDL — `members.interview_booking_url` + `selection_committee.interview_booking_url` + `selection_dispatch_url_log` table + V4 RLS pair (rejected `view_selection` non-existent action). **p251 SPEC #348 Child #2 #355 SHIPPED**: RPC body extension — `notify_selection_cutoff_approved` now routes track-aware via LRD (researcher) / cycle URL (leader) with `selection_dispatch_url_log` audit-before-send + 3 forward-defense regressions locking SPEC drift + leader-no-committee + audit-before-send. SPEC §5.1 + §8 Q3 amended for PM Option A (`role IN ('evaluator','lead')` filter, not `role='researcher'`). Next dispatch: **#356 Child #3 Admin UI** + **#357 Child #4 Cycle4 committee reseed** (both unblocked; parallel-shippable). Cron jobid 47 first natural fire still pending Mon 2026-05-25 13:00 UTC. **p260 Wave 4 #312 audit jornada shipped + PM D1-D5 ratified + 7 children spawned (this session)**: `docs/audit/p260_wave_4_312_journey_audit.md` — read-only audit + PM ratification appendix §15.5. 7 children created on GitHub in PM-ratified dispatch sequence: #377 → #378 → #379 → #380 → #381 → #382 → #383. **D1-D5 verbatim**: D1 v1-must Wave 4a (#378) · D2 v1-follow-up #312-W4f (#382) · D3 ADR-required `content_products` canonical (#383) · D4 v1-must Wave 4d (#380) · D5 preserve 5 legacy public rows as-is. **Next dispatch** = #377 (sign_proposer_consent — highest leverage, unblocks Frontiers + all future pending_proposer_consent intakes). After #377 + #378 ship, full Frontiers chain becomes exercisable end-to-end for the first time. **p261 #377 SHIPPED + CLOSED** via PR #385 squash `e8d7fe92` — sign_proposer_consent canonical A2 path live; Frontiers fixture transitioned `pending_proposer_consent → draft` via 3/3 live smoke (Fabrício impersonation + idempotency + wrong-member rejection); invariants 21/21=0 live. **Next dispatch** = #378 W4a gate templates (sequence 2/7, unblocked) — create minimum gate templates for 6 doc_types currently returning NULL from `resolve_default_gates(p_doc_type)` (editorial_guide + governance_guideline + project_charter + manual + executive_summary + framework_reference). After #378 ships, Frontiers editorial_guide can advance from draft to under_review via editor lock. **p262 #378 SHIPPED + CLOSED** via PR #387 squash `15c7b3f0` — 6 new gate templates added (editorial_guide/governance_guideline/manual/executive_summary/framework_reference/project_charter); all 11 doc_types now return non-NULL; invariants 21/21=0 live; Frontiers editorial_guide has 3 gates ready (curator/leader_awareness/submitter_acceptance) so editor lock unblocks `draft → under_review`. **Next dispatch** = #379 W4c GAP-259.A (sequence 3/7, unblocked) — extend `list_governance_library` body with default-status exclusion of `draft`/`pending_proposer_consent`/`withdrawn`/`revoked` per p259 PM-ratified Option (a) + p262 PM #379 prompt; explicit `p_filters.status` override remains available for admin/audit context. **p262.W4c #379 SHIPPED + CLOSED** via PR #389 squash `eb72a926` — GAP-259.A closed; member library default call (no filter) now excludes 4 non-consultable statuses; explicit status override preserved for admin/audit; corpus split 13/3 visible/hidden live; Frontiers remains hidden until status advances. **Next dispatch** = #380 W4d reader hardening (sequence 4/7, unblocked) — extends `/governance/document/[id].astro` to use new SECDEF RPC `get_member_governance_doc` instead of table-direct SELECT (carry from p259 reader hardening item; PM D4 ratified at p260 audit close).

---

## Related Audit

See `docs/audit/2026-05-21_ISSUES_REGISTRY_AND_PARALLEL_WORK_AUDIT.md` for the initial curator analysis behind this registry.
