# Generated / heavy artifacts — reproducible from the scripts, not versioned.
# *.pptx covers BOTH the 18MB PMI brand template (build/) and the rendered deck (skill guardrail).
*.pptx
*.pdf
preview*/
out/

# Personal photos (member headshots) — never commit to a PUBLIC repo (skill guardrail).
assets/people/

# Third-party report/standard covers — not versioned (copyright).
assets/covers/*.png
assets/covers/*.jpg
assets/covers/*.jpeg

# Round-1 feedback asset inbox (third-party screenshots/covers for triage) — not versioned.
assets/inbox_r1/
# Processed cert badges (white-bg -> transparent), derived from inbox_r1 — not versioned.
assets/certs_proc/
