# Claude Code — Project Rules

## Domain Model V4 (concluído 2026-04-13)
Refactor arquitetural completo: 6 ADRs (0004-0009), 30 migrations, 7 fases. Ver `docs/refactor/DOMAIN_MODEL_V4_MASTER.md` para decisões e histórico. Decisões-chave:
- `can()` / `can_by_member()` é a source of truth para autoridade (ADR-0007)
- `initiatives` é o primitivo de domínio; `tribes` é bridge via dual-write (ADR-0005)
- `persons` + `engagements` modelam identidade; `members` é bridge (ADR-0006)
- Novos tipos de iniciativa = config no admin, não código (ADR-0009)

## Platform
- **URL:** https://nucleoia.vitormr.dev
- **Supabase:** ldrfrvwhxsmgaabwmaik (sa-east-1)
- **Version:** v3.2.1 (Structural Quality) | **238 MCP tools (158R+80W) + 4 prompts + 3 resources v2.55.0** | **32 Edge Functions** + **1 Cloudflare worker (pmi-vep-sync) LIVE @ wrangler 4.x** | **1446** unit (**1408** pass DB-aware) + 40 e2e tests | **69 ADRs + 10 amendments** (+ADR-0066 Amendment 3 Bug #2 worker filter) (p93 single-context ~3h 2026-05-05 PM-collaborated: **6 commits (`cb63c52` chain recirc + `387db83` state line + `8a0403b` p93b diff/comments + `5fea8ee` p93c PDF parser+draft mode + `fd9fac0` p93d sanitize chars + hyphen + `c9044ac` p93e collapse spaces) + 2 migrations 520000-520001 + 4 worker prod deploys (29d4fb17/6b31b5d9/25ef067d/4d7b9b2b) + 0 invariant violations + 3 emails delivered**. **p93b** (diff "anterior↔atual" + comments herdados): get_previous_locked_version filtro `approved` substituído por `NOT EXISTS withdrawn` (era latente desde IP-3d, bloqueava diff entre rodadas pre-ratificação) + list_document_comments + p_include_prior_versions param + 3 cols (from_version_id/from_version_label/is_inherited) → 19 comments Round 4 (Roberto/Sarah/Fabricio em 21-27/04) agora visíveis no chain v6 com badge âmbar `↩ v1.0`. **p93c** (PDF parser refactor + draft mode): parseHtml emite Node[] com Segment[] tipados (text/bold/italic/href), preserva inline strong/em/a via Text spans (33 entradas glossário §13.4 Política IP renderizam termo em bold; 371 strongs mid-paragraph mantêm bold), blockquote agrupa filhos em View border-left + bg cinza, URLs sufixadas em 8pt cinza quando href != texto. ChainPDFExportIsland 2 PDFDownloadLinks (draft + oficial) com smart default (recommend draft se chain.review + 0 sigs); draft tem watermark fixed em cada página + skip de assinaturas + página final identificação chain_id/version_id; oficial mantém comportamento (sigs + evidence trail). **p93d** (sanitize + hyphen + nested tags): Font.registerHyphenationCallback(word=>[word]) global (hyphen library default → "Pro-priedade In-dustrial"); sanitizeText converte `→`/`←` em `›`/`‹` (WinAnsi U+203A/U+2039), `↔` em `<->`, emoji surrogate pairs droppados (📝 watermark renderizava "EFRASCUNHO" → fix removendo emoji do JSX literal); parseInlineSegments pre-strip tags `<p>`/`<div>`/`<span>`/`<section>`/`<article>` (§4.5.4 royalties mostrava `• p>(a) Percentual.../p>` por nested `<p>` em `<li>`). **p93e** (cosmetic): collapse `\s{2,}` em sanitizeText final + `↔` sem espaços hardcoded (deixa source HTML prover) → `Brasil <-> UE / EEE` single-space. **PDF rendering validado** com 4 iterações ChatGPT compartilhando PDFs renderizados (Política IP v6 38K chars, 12 páginas A4). 4 sediment patterns identified: governance migration must use chain workflow (p90/p90.b/p90.c anti-pattern retroativo) + first vs current ratified cache (PM insight 2 dimensões controle distintas) + react-pdf Helvetica WinAnsi gaps + react-pdf default hyphenation library inadequado p/ docs jurídicos PT-BR. **PM action items pendentes**: aguardar curadores assinarem 6 chains (Roberto = curator gate em todos 6 + 19 inherited comments para resolver; Sarah/Fabricio acompanham). Memory: feedback_governance_migration_must_use_chain_workflow + feedback_first_vs_current_ratified_cache_pattern + handoff_p94_artia_sync_audit. | p93 single-context ~1h 2026-05-05 PM-collaborated: **1 commit (`cb63c52`) + 1 migration 520000 + 0 invariant violations + 3 emails delivered**. **Bug surfaced PM**: 5/6 docs governance visíveis no `/admin/governance/documents` (Anexo Técnico criado em p90.c sem `approval_chain`) + chains apontando para v3 Round 5 ADR-0068 stale ao invés de v6 Round 6 atual (p90/p90.b/p90.c inseriram v4-v6 LOCKED direto sem propagar para approval_chains nem notificar curadores; orphan locked versions). **Fix p93** migration `20260516520000`: (a) **schema cache duplo** `first_ratified_*` (3 cols, idempotente, set once via COALESCE) + `current_ratified_*` (3 cols, overwrite a cada nova ratificação) em `governance_documents` + 4 FK constraints ON DELETE SET NULL + 2 indexes parciais; trigger `trg_sync_ratification_cache` AFTER INSERT/UPDATE OF status ON approval_chains (auto-supersede prior active chain `WHERE id<>NEW.id AND status='active'` + auto-promote `governance_documents.status` under_review→active na 1ª ratificação); (b) supersede 5 chains v3 stale (Política IP `955a4728` / Termo `d16d1241` / Adendo Retif `2e76f367` / Adendo PI Coop `d5291281` / Acordo Bilateral `cec9e6b8`) + criar 5 chains novas apontando para current_version_id (v6 Política IP/Termo/Adendo Retif `v2.6-p90c-material-fixes`; v5 Adendo PI Coop `v2.5-p90c-material-fixes` / Acordo Bilateral `v1.4-p90c-material-fixes`) com gates copiados verbatim; (c) criar chain v1 Anexo Técnico (`7537f6d2`) com 4 gates reduzidos (curator/all + submitter_acceptance/1 + president_go/1 + president_others/4 — sem leader_awareness/member_ratification por ser anexo subsidiário); (d) novo template `governance_recirculation_batch` (5 vars: first_name + batch_intro + doc_links_html + platform_url + sender_name) + 3 emails delivered consolidados (Sarah `141bd3d5` / Roberto `059ecbf6` / Fabricio `2d0da5d6` com 6 chain links cada). **Comparação `v oficial atual ↔ v oficial anterior` no futuro**: `SELECT version_id FROM approval_chains WHERE document_id=X AND activated_at IS NOT NULL ORDER BY activated_at DESC LIMIT 2` (coluna activated_at já existia). 2 sediment patterns: governance migration must use chain workflow (p90/p90.b/p90.c anti-pattern detectado retroativamente) + first vs current ratified cache (PM insight 2 dimensões controle distintas). 11/11 invariants 0 violations end-to-end. PM action items pendentes: aguardar curadores assinarem 6 chains (Roberto = 1ª curator gate em todos 6 + chain anterior tem comentários; Sarah/Fabricio acompanham). Memory: feedback_governance_migration_must_use_chain_workflow + feedback_first_vs_current_ratified_cache_pattern. | p92 single-context ~5h 2026-05-05 autônoma + followup PM-collaborated: **5 commits (`1f320fc` Phase A-F + `e7535a6` state line + `87b1c26` Apps Script bug + spec clarify + `1c77532` webhook lookup objective_eval + `65aff6b` RPC throttle pg_sleep) + 4 migrations 510000-510003 + 4 prod main worker deploys (78a1f1c8/fa00d6a3/61dd6cb4/[final]) + 1 worker pmi-vep-sync deploy (`0ac0d0d1`) + 0 invariant violations + 1446 tests pass + 31 emails dispatched live (12 peer review smoke + 18 bulk welcomes + 1 Ana Karina resend) + 1 Calendar webhook real validated end-to-end (Maria Araújo → selection_interviews `9d3802b5`)**. **Phase A-F shipped** endereçando 6 dos 7 bugs catalogados em p91 audit + **followup**: webhook lookup ampliada para objective_eval, Apps Script bug `e undefined` fix, PM autenticação Cloudflare recovery (`wrangler login` + dashboard caminho A para SUPABASE_SERVICE_ROLE_KEY secret), bulk dispatch 18 candidatos via RPC `dispatch_pending_welcomes(false)` + Ana Karina re-send com token refreshed. **Resend 429 5rps cap** caught durante bulk: round 1 13/18 fail; recovery 3 rounds throttled retry (0.3s→1s→2s) → 19/19 sent; RPC patched v2 com `PERFORM pg_sleep(0.3)` entre invocações. **Apps Script trigger ATIVO desde 1/maio** mas filtro `getDateCreated > since 5min` filtrava re-fires de eventos antigos — primeira execução end-to-end successful 03:35 BRT após webhook + secret + lookup fixes. **PM action items pendentes**: avaliar 6 candidatos pending peer review (Sarah/Fabricio/Vitor 12 emails inbox), monitor 19 candidatos onboarding 14d TTL via /admin/selection Pipeline column, observar reschedule cron primeira execução. 4 sediment patterns capturados (Astro v6 env, round-robin load metric, admin_audit_log columns, Resend 5rps cap). 11/11 invariants 0 violations end-to-end. Memory: handoff_p93 + project_session_p92_jornada_fix + 4 feedback. | p91 single-context ~3h 2026-05-05: **1 commit (`1f320fc`) + 4 migrations 510000-510003 + 2 prod deploys (worker `pmi-vep-sync 0ac0d0d1` + main worker `78a1f1c8`) + 0 invariant violations + 1446 tests pass + 14 emails dispatched live (12 peer review smoke + 0 bulk)**. **6 phases A-F shipped** endereçando 6 dos 7 bugs catalogados em p91 audit: **Phase A** worker `_bucket==='submitted' && statusId===2` guard antes dispatchWelcome (Bug #2 spurious welcomes para qualified/rejected leaders). **Phase B** webhook Astro v6 env-access fix (`locals.runtime.env` removido → `cloudflare:workers` env binding) — P0 bug latente desde p87 (webhook 500 baseline incorretamente interpretado como "live") + spec p87 patches B1+B2+B4+B5+B6+B8 (getEvents 90d window, EMAIL_ALIAS map Gmail→institucional, URL canonical, OAuth scopes nota, idempotência docs, smokeTest eventId param) + B3 webhook clear interview_status flags em UPDATE (Bug #6 partial). **Phase C** RPC `dispatch_peer_review_invitations(app_id, max_peers=2)` round-robin SECDEF + helper `_get_peer_review_eligibility` (load_count = submitted + pending invites para true rotation) + template `peer_review_request` (3 langs) + smoke 7 candidatos AI-complete (Danilo/JoãoCoelho/Luciana/LUIZ/Maria/THAYANNE → status='objective_eval' + 2 invites cada; João Uzejka skipped já 2 evals) → 12 emails delivered Sarah=6/Fabricio=5/Vitor=1. **Phase D** get_selection_dashboard +5 indicators (ai_analysis_done, peer_eval_count, peer_invites_pending, interview_scheduled, token_consumed, video_screening_done) +3 stats rollup + admin UI nova coluna "Pipeline" 5 mini-badges color-coded (IA/Pares N/2/EN/TK/VD) com tooltips (Bug #7). **Phase E** column `interview_reschedule_last_nudged_at` + template `interview_reschedule_nudge` (3 langs) + RPC `process_pending_reschedule_nudges()` cron-bypass ADR-0028 pattern + pg_cron `nudge-reschedule-pending-daily` 14:00 UTC daily com 3d cooldown (Bug #6). **Phase F** RPC `dispatch_pending_welcomes(dry_run=true, max_count, ttl_days=14)` SECDEF token via gen_random_bytes 32 base64url + sha256 hash metadata; dry_run identificou 18 candidatos corretos (João Uzejka filtrado por `ai_analysis IS NULL` guard) — RPC ready, **NÃO executou live** (PM action: invocar quando ready, recomendado após Apps Script setup) (Bug #3). **PM action items pendentes**: (1) `npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY` — webhook depende; (2) Apps Script project setup conforme spec corrigido p87; (3) avaliar 6 candidatos pending peer review; (4) opcional bulk dispatch live 18 welcomes. 3 sediment patterns capturados (Astro v6 env access, round-robin load metric inclui pending, admin_audit_log columns changes/metadata). 11/11 invariants 0 violations. Memory: handoff_p93 + project_session_p92_jornada_fix + 3 feedback. | p91 single-context ~3h 2026-05-05: **0 commits + 0 migrations + 7 admin_audit_log entries + 4 token annulments + 4 welcome resends (3 delivered + 1 Gmail draft pending PM) + 1 spec doc + 4 memory entries + 0 invariant violations**. **PMI Journey audit completo** (`docs/specs/p91-selection-journey-audit.md`): partiu de relato candidato William Junio thread Gmail "Link do email não funciona" (2026-04-29 19:30) → escavou 7 bugs estruturais. **Bug #1 rendering** (FIXED prod): migration 20260516220000 dropou `'variables', p_variables` no `audience_filter` JSONB; 230000 (slot seguinte) restaurou; 8 emails 04-29 saíram com `{{first_name}}` literal. **Bug #2 worker filtra**: extract_pmi_volunteer.js varre 3 buckets, worker `index.ts:268` chama dispatchWelcome sem checar `_bucket`/`statusId` → 4 welcomes inválidos para qualified active leaders (Hayala/AnaCarla/Marcos PMI-CE/MG) + 1 rejected anterior (Adalberto). **Bug #3 bulk import**: 19 candidatos submitted via SQL bulk em batches 04-01/04-15/04-21 nunca disparam welcome (worker só dispara em was_new=true via worker, bulk SQL bypassa); 18 candidatos legitimately pendentes alguns aguardando 60+ dias (incl Eduardo Luz já voluntário ativo PMI-RS). **Bug #4 Apps Script Calendar nunca configurado**: endpoint LIVE em prod mas spec p87 declara "Apps Script setup pending PM action"; 0 interviews scheduled batch p82+, 0 gate_attempts logged. **Bug #5 peer review sem trigger**: 7 candidatos AI complete, 0 peer evaluations; gate P0002 GATE_NO_PEER_REVIEW funciona mas upstream não tem RPC/notification para invitar peers. **Bug #6 reschedule incompleto**: RPC request_interview_reschedule envia email candidato, mas sem Apps Script (#4) re-booking não sincroniza. **Bug #7 admin /selection lista**: get_selection_dashboard retorna scores apenas; falta indicators ai_done/peer_count/interview_marked/token_consumed/video_done sem clicar candidato a candidato. **Recovery imediato (PM aprovou cada item)**: 4 tokens annulados (Hayala/AnaCarla/Marcos/Adalberto, expires_at=now()), Herlon welcome correto delivered (PMTEST era teste por PM+Claude criação jornada — Herlon nunca recebeu correto), Ana Pacheco + DJEIMIYS resend correto delivered, William Gmail draft `r-5580222886714244339` em nucleoia@pmigo.org.br Drafts (pending PM clicar enviar) + tokens estendidos +14d. PM decisão: dispatch 18 bulk-import BLOQUEADO até bugs #2/#4/#5 resolvidos próxima sessão p92 dedicada. 4 sediment patterns (audience_filter load-bearing, status filter gap, bulk import skip, Apps Script setup-PM-action drift). Memory: handoff_p92_jornada_fix + project_session_p91_jornada_audit + 3 feedback. p91 não tocou DDL — apenas DML recovery + audit log + spec. | p89 single-context ~5.5h 2026-05-02 morning: **7 commits + 4 migrations + 1 EF deploy + 1 Worker deploy + 2 GH closed + 1 routine scheduled + 0 invariant violations**. **Audit drift closure** (`1e044cf`): /audit revelou 2 HIGH drifts — (a) MCP version line desalinhada (code v2.53.0/236 vs CLAUDE.md v2.54.0/237) → bump version constant + health hardcoded para v2.54.0/237 + redeploy nucleo-mcp v2.54.0; (b) ADR README index com 43 ADRs faltantes (0024-0027 + 0030-0068) → flat-list inserida preservando estilo, audit script PASS. Drift MEDIUM longstanding (361 phantom schema_migrations rows) deferido (apply_migration MCP gap conhecido). 1 advisor ERROR (longstanding `public_members` SECDEF view) abaixo do baseline ~12 — improved. **Feature #122 governance recirculation workflow** (`6dc101d`): sediment p88 pattern recorrente automatizado. RPC `recirculate_governance_doc(chain_id, dry_run, recipient_emails)` migration `20260516480000` + template parametrizado `governance_recirculation_request` (8 vars: first_name + document_title + version_label + new_chain_url + old_chain_url + changelog + platform_url + sender_name) + MCP tool wrap (manage_member gated) + ReviewChainIsland button "🔄 Re-circular aos curadores" + preview modal (dry_run shows recipients/gates_to_copy/warnings + confirm executes + redirect new chain) + contract test 3 tests. Authority: manage_member (matches lock_document_version). Default recipients: members eligible para first gate via `_can_sign_gate` (ignora signoffs prévios — re-assinatura). Reuso: `lock_document_version` para atomic lock + chain creation + audit + in-platform notif. Email separado via `campaign_send_one_off`. Audit log: action='governance.recirculated' com full context. Validation paths verified end-to-end via DO impersonation + temp v99 fixture: 5 error paths (not authn, not found, status check, no draft, lock fail) + 2 dry_run success paths (auto + explicit recipients). MCP v2.54.0→v2.55.0 (237→238 tools, +1 tool 158R+80W). Smoke pós-deploy: MCP v2.55.0/238/initialize 200, domain 200, 11/11 invariants 0 violations, tests 1443→1446/1408 pass (Δ +3 skipped local). **Cron audit P0 fixes** (`ef314fd`, migrations 490000+490001): /audit em 23 pg_cron jobs surfaced 2 P0 bugs — (a) `send-weekly-leader-digest` 100% falhando há 30d (auth.uid() check em cron context sem JWT context → "Unauthorized" via `get_weekly_tribe_digest`) → fix com bypass robusto `auth.role()='service_role' OR jwt.claims IS NULL` (ADR-0028 amendment p89). Validation live: 7 tribe leaders pela primeira vez receberam in-platform digest em produção (delivery via send-notification-email cron 5min); (b) DUPLICATE Saturday 12:00 UTC cron `weekly-card-digest-saturday` + `send-weekly-member-digest` ambos chamando `generate_weekly_member_digest_cron()` → members estavam recebendo dupla notificação digest cada sábado. Drop `weekly-card-digest-saturday` (jobid 23) preservando EF wrapper para observability. Cron count 23→22. Audit findings deferidos MEDIUM/LOW: 3am UTC cluster (4 jobs simultaneous), hourly :00 cluster (2 jobs), Sunday 4-5am cluster (4 jobs incl Cloudflare pmi-vep-sync), anonymize unification potencial (LGPD legacy ↔ V4 by_kind). **Routine agendada** `trig_014b4VSJ3DfESMFA38j33iWH` fires 2026-05-09T14:00Z (Saturday 11h BRT, 1 week) — read-only check curador response status 5 v3 chains via `get_chain_audit_report`, output `docs/curator-followup-2026-05-09.md` com per-chain status + curator latency table + recommendation (nudge/recirculate/escalate). **CI #16 fix** (`ac181a7`): regex `\$\$` em test contracts/adr-0022-delivery-mode não casava com `$function$` delimiter usado nas migrations 490000+490001 → replace por `$$` (idempotente, função em produção idêntica). Tests 1446/1408 pass. **#117 closed** (workflow gate ecosystem 100% per p87 — Phase 2 ecosystem A.1+A.2+A.3+A.3.b+A.3.c + C raise-the-bar + C.b backfill + C.c UI prominent + Sprint 4 + Sprint 3.b ROBUST n=53). **Cron audit MEDIUM stagger** (`03024d9`, migration 490002): stagger 2 collisions — v4_engagement_expiration `0 3 * * *` → `5 3 * * *` (avoid drive-discover-atas 3:00 collision) + retry-pending-ai-analyses `0 * * * *` → `7 * * * *` (avoid hourly :00 spike com expire-stale-invitations). Anonymize unification investigation: KEEP BOTH (lgpd-anonymize-inactive legacy member-level vs v4-anonymize-by-kind person-level engagement_kinds — different abstractions). Cloudflare↔Supabase 4:00 UTC collision accepted (independent infra). LOW deferred via audit_log entry. **#120 _can_sign_gate V4 BLOQUEADO p90** — pre-migration check exige active chains review=0; 5 v3 chains em review state; esperar curadores assinarem antes. Pendente p90: aguardar curadores re-revisarem v3 + assinarem (Roberto/Sarah/Fabricio); advogado humano licenciado revisa Track B + Standby + cláusula plataforma + cláusula continuidade ANTES circular para 15 capítulos. Memory updated: handoff_p90 + project_session_p89_audit_122 + project_session_p89_cron_audit (cron MEDIUM stagger + #117 closure + #120 deferred apêndice). | p88 single-context ~6h 2026-05-01 evening: **4 commits + 2 migrations + 2 Worker prod deploys + 5 v3 governance docs redraftados Round 5 + 5 chains transitioned + 3 emails enfileirados curadores + 1 GH closed + 1 GH opened + 0 invariant violations**. **CI #121 fix** (`6743076`): TDZ TribeAttendanceTab — useState declared after useCallback que dependia de excuseReasons → ReferenceError no SSR /tribe/[id]. 7+/7- ordering move. Sediment: workerd DNS error red herring pattern (filter `grep -v "DNS lookup\|workerd@"` antes de buscar real error). **Draft panel UI** (`2af06a7`): RPC `get_next_draft_version` SECDEF + ReviewChainIsland banner âmbar "Próxima versão (draft pendente)" + 4 tabs (Documento + Diff anterior↔atual + Draft + Diff atual↔Draft) + changelog notes expandable. **Glossário URL canônica** (`e5c57f4`): RPC `get_governance_glossary` SECDEF anon-grant + Astro page `/governance/glossario` espelho dinâmico §13 Política + history versões + Política §13.5 amend referenciando URL. **Default-to-draft UX fix** (`b353703`): viewMode default = 'draft' quando há draft pendente — curador entra direto na versão nova (PM feedback fricção real). **5 docs governance Round 5 redraft** (DML in-place, drafts ainda v3 quando começou, agora locked + current_version_id): Política (16k → 34.6k) + Acordo Coop (22k → 28.1k) + Adendo PI (6k → 16.9k) + Termo (10k → 16.8k) + Retificativo (10k → 16.9k). 5 rounds consolidados: R1-R3 ADR-0068 framework (Path A/B Track B + Material/Editorial change + atribuição canônica + INPI 12m + Glossário §13 + Policy Owner) + R4 PM (PMI® primeira citação + EN name "AI & PM Study and Research Hub" + future-proof §1.1 sem listar capítulos atuais + PMI Brasil ≠ entidade jurídica → PMI-GO única responsável CNPJ 06.224.773/0001-90 + atribuição dual PT-BR + EN) + R5 PM (glossário URL canônica + §15 Continuidade do Programa amaciada com disclaimer §15.3 não-premeditação + Art. 8 Adendo PI Plataforma como projeto pessoal independente do GP autor com CoI declarado). **Lock + promote** 5 v3 drafts → current_version_id (PM Vitor 880f736c). **Chain transition**: 5 v2 chains superseded com closed_at/notes + 5 novas chains v3 review com gates copiadas. **Template `governance_curator_review_request`** criado (category=operational; constraint check: apenas operational/onboarding/announcement/newsletter). **3 emails enfileirados** via `campaign_send_one_off`: Roberto Macêdo (boblmacedo@gmail.com, send 8b5b6b22) + Sarah Rodovalho (sarah.famr@gmail.com, send 917e8ea6) + Fabricio Costa (fabriciorcc@gmail.com, send a56b9f43). **GH issue #121 closed** (CI Validate green) + **#122 opened** (feat: workflow re-circulação curadores pattern recorrente, RPC + MCP + UI button + audit, ~4-5h estimated). 11/11 invariants 0 violations end-to-end. Memory: handoff_p89 + feedback_pmi_brand_canonical + feedback_governance_recirculation_workflow + feedback_workerd_dns_error_red_herring (+ 4 entries total). Red flag preservado: advogado humano licenciado revisa Track B + Standby + cláusula plataforma + cláusula continuidade ANTES de circular para 15 capítulos. Pendente p89: aguardar 3 curadores re-revisarem v3 + assinarem nas chains novas (Roberto = 1ª signature em todos 5; Sarah = 1; Fabricio = 4); curadores também marcam comments como resolvido nas chains anteriores (audit trail PM-controlled). | p87 EPIC ULTRA-EXTENDED marathon 2026-05-01 ~12h: **30+ commits + 10+ migrations + 7+ EF deploys + 4 Worker prod deploys + 7 GH items + 0 dependabot vulns + 6 memory entries**. **Tema 1 (Bug Ana Carla) closed**: 3 surfaces fix (AttendanceGridTab admin grid + MobileCardList mobile + TribeAttendanceTab dentro tribo) + cycle modificado pular excused + long-press 300ms modal + reason input opcional + help banner azul + bulk override checkbox + asterisco* reason persistida + a11y onKeyDown + WCAG touch target. Worker prod v2ea9f5ba live. **Tema 2 (Curadoria 5 docs governance)**: 18 comments dos 3 curadores extraídos + 3 rounds deferring legal-counsel + accountability-advisor (6 invocations) + 11 ressalvas absorvidas + reliable sources validation + ADR-0068 canonical framework shipped (Path A/B Track B-Periódico + Material/Editorial change framework + atribuição contratual + período graça INPI + glossário centralizado + Policy Owner cargo+nome+vigência + sponsor PMI Goiás assimetria definida). PM strategic A6 rejection (organic growth — Núcleo será procurado pelo PMI Global, não vice-versa). Migration redraft texto 5 docs DEFERRED próxima sessão laptop dedicada (~2h, ADR-0068 canonical guides). Red flag preservado: advogado humano licenciado revisar Track B + Standby antes de circular para 15 capítulos. **Sprint 4 + Sprint E** (`b5106f0`): pmi-ai-analyze + pmi-ai-analyze-research SYSTEM_PROMPT evolution — raises_the_bar agora considera DOIS paths convergentes: (a) track record explicito + (b) potencial convergente (formação sólida + commitment + fit≥4 + sem red flags); calibração explicita do MVP findings n=14. + Calendar webhook endpoint LIVE `/api/calendar-webhook` (#116 closure infra) com secret auth + idempotência via calendar_event_id + bypass gate justificado (post-mark_interview_status legítimo flow). Apps Script setup doc `docs/specs/p87-calendar-webhook-apps-script.md`. **Sprint 3.b ROBUST n=53** (`2fb343b`): Tier 1 Gemini paid + AI Studio prepayment credits → 53/53 candidatos cycle3-2026 completaram. Precision YES 64.5%, recall YES 80% (vs n=14 33%), F1 0.71 (vs 0.46) — Sprint 4 prompt evolution = SUCCESS. Worker production deployed v022e9617 (webhook live). #120 spinoff: _can_sign_gate V4 conversion plan deferred. **Spinoffs**: #117 workflow gate ecosystem 100% (A.1 RPC gate + A.2 token endpoint + A.3 audit + A.3.b admin UI tab + A.3.c MCP wrap + C raise-the-bar AI rubric + C.b backfill + C.c UI prominent). #118 chapter normalization (worker mapper + backfill 4 candidates incl João Uzejka PMI-RS resolved) — closed. #119 raises_the_bar validation FULL n=53 robust (precision 64.5% / recall 80% / F1 0.71). **PM ratify** (`16b1f79`): UI hint operacional inline em ai-runs tab. ADR-0066 Amendment 2 documenta decisão. **Bug fix p87** (`e1eded0`): migration 420000 corrige get_application_ai_analysis_runs + get_application_gate_attempts (RETURNS TABLE id ambiguity). **Sprint C.b+C.c** (`53b0500`): admin_retry RPC + maxOutputTokens 4096 + 6/6 candidatos backfilled raises_the_bar. Phase 2 #117 ecosystem 100% complete. **Sprint A.3.b+c** (`cc0d4be`): admin UI Auditoria tab + MCP tool wrap + nucleo-mcp redeploy. **Phase A #117 substrate** (3 sprints `bdd4dc7`+`e8607b2`+`d6453f6`). **Phase C #117** (`3d9bb9a`): EF pmi-ai-analyze raises_the_bar rubric. **Phase B'' explorada**: easy-batch V3→V4 esgotado, único strict V3 remanescente é `_can_sign_gate` (signing chain — high risk, requer engagement_kind_permissions seed via #120). **p87 hotfix anterior** (3 commits): `dbdd1c9` ADR-0066 Amendment 2026-05-01 + `1fc1125` chore deps bump pmi-vep-sync wrangler 3→4 + vitest 2→3 (4 dependabot moderates closed) + `20260516370000` p87 hotfix migration: campaign_templates `pre_eval_pause` + Danilo status revert. Issue #117 spinoff + comment #116. Operacional: emails Thayanne+Danilo Resend ack delivered (Danilo opened 16:10). Smoke 11/11 invariants = 0 violations. **Sprint 4 + Sprint E** (`b5106f0`): pmi-ai-analyze + pmi-ai-analyze-research SYSTEM_PROMPT evolution — raises_the_bar agora considera DOIS paths convergentes: (a) track record explicito + (b) potencial convergente (formação sólida + commitment + fit≥4 + sem red flags); calibração explicita do MVP findings n=14. + Calendar webhook endpoint LIVE `/api/calendar-webhook` (#116 closure infra) com secret auth + idempotência via calendar_event_id + bypass gate justificado (post-mark_interview_status legítimo flow). Apps Script setup doc `docs/specs/p87-calendar-webhook-apps-script.md`. **Sprint 3.b BLOCKED** (`bae1464`+aborted): expansion paid Gemini Tier 1 falhou 55/55 com 429 — GCP billing project ainda em Free Tier; PM precisa habilitar manualmente. Worker production deployed v022e9617 (webhook live). #120 spinoff: _can_sign_gate V4 conversion plan (Path A engagement_kind_permissions seed preferred, deferred to dedicated session — high risk signing chain). **Spinoffs**: #117 workflow gate ecosystem 100% (A.1 RPC gate + A.2 token endpoint + A.3 audit + A.3.b admin UI tab + A.3.c MCP wrap + C raise-the-bar AI rubric + C.b backfill + C.c UI prominent). #118 chapter normalization (worker mapper + backfill 4 candidates incl João Uzejka PMI-RS resolved) — closed. #119 raises_the_bar validation MVP (n=14 cycle3-2026, precision YES 75%, recall YES 33%, F1 0.46 — rubric conservadora; LGPD Option B PII-stripped via anonymize RPC + pmi-ai-analyze-research EF; 3.b aborted Gemini Free Tier 429 cap, deferred Tier 1 paid OR distributed-days). **PM ratify** (`16b1f79`): UI hint operacional inline em ai-runs tab — YES+fit>=4 → ⭐ skip-to-interview shortlist, NO → ⚠️ soft signal NÃO auto-reject, uncertain → ↺ par-revisão prioritária. ADR-0066 Amendment 2 documenta decisão. **Bug fix p87** (`e1eded0`): migration 420000 corrige get_application_ai_analysis_runs (tv.ip → tv.ip_address, tv.ua → tv.user_agent — colunas reais selection_topic_views) + get_application_gate_attempts (RETURNS TABLE id → attempt_id, resolve PL/pgSQL OUT variable ambiguity). Worker production redeployed via npx wrangler deploy version `01213cf5-363f-4844-a13f-150655a3436b` — admin UI Auditoria tab + raise-the-bar verdict prominent agora visíveis em produção. Pattern recurring sediment: frontend changes precisam wrangler deploy separado (esquecido durante marathon — caught quando PM testou production). #118 spinoff (chapter_affiliation→chapter normalization bug, João Uzejka case, P0 backfill+worker mapper fix). #119 spinoff (training data validation raises_the_bar vs decisões humanas cycle3-2026, 180 apps com final outcome, 61 com human evals — propõe Option B PII-stripped training como LGPD-defensible path). **Sprint C.b+C.c** (`53b0500`): admin_retry_application_ai_analysis RPC migration 410000 + EF maxOutputTokens 2048→4096 (3 SyntaxError fails resolvidos) + 6/6 candidatos cycle3-2026-b2 backfilled com raises_the_bar (5 NO + 1 UNCERTAIN — INSIGHT: João Uzejka fit=4 também NO, confirma PM mindset 'fit ≠ raise the bar') + admin UI ai-runs tab agora exibe verdict prominent (badge color-coded + rationale border-left destaque). Phase 2 #117 ecosystem 100% complete. **Sprint A.3.b+c** (`cc0d4be`): admin UI nova aba "Auditoria" no modal selection (loadGateAuditLog renderiza timeline de gate_attempts com badges P0001-P0004 + bypass status + payload snapshot expandível) + MCP tool wrap `get_application_gate_attempts` (manage_member gated) + nucleo-mcp redeploy (smoke 406 OK = native transport behavior, NOT 500 crash). MCP tool count 236→237. **Phase A #117 substrate completo** (3 sprints): `bdd4dc7` Sprint A.1 schedule_interview workflow gate (3-layer GATE_NO_AI/GATE_NO_PEER_REVIEW/GATE_NO_SCORE + bypass via manage_member, migration 20260516380000); `e8607b2` Sprint A.2 token-gated booking page + 2 RPCs (issue_interview_booking_token committee+gate, validate_interview_booking_token anon-grant) + frontend `/interview-booking/[token].astro` 3 langs, migration 20260516390000; `d6453f6` Sprint A.3 gate_attempts audit table (RLS V4) + _log_gate_attempt SECDEF helper + get_application_gate_attempts RPC + RPC rewrites com logging completo, migration 20260516400000. **Phase C #117** (`3d9bb9a`): EF pmi-ai-analyze redeploy com raises_the_bar rubric (verdict yes|no|uncertain + rationale) + SYSTEM_PROMPT documenta PM mindset (Vitor 2026-05-01 'does that person raise the bar?') — INDEPENDENT de fit_for_role. **Phase B'' explorada**: easy-batch V3→V4 esgotado, único strict V3 remanescente é `_can_sign_gate` (signing chain — high risk, requer engagement_kind_permissions seed). **p87 hotfix anterior** (3 commits): `dbdd1c9` ADR-0066 Amendment 2026-05-01 (Phase 2 trigger + workflow gate gap surfacing + raise-the-bar mindset + Fabricio incident trace). `1fc1125` chore deps bump pmi-vep-sync wrangler 3→4 + vitest 2→3 (4 dependabot moderates closed: vite/undici×2/esbuild dev-time transitives — 0 prod runtime impact). `20260516370000` p87 hotfix migration: campaign_templates `pre_eval_pause` (3 langs) + Danilo status revert idempotente. Issue #117 spinoff (workflow gate enforcement: RPC precondition + Calendar token + audit log) + comment #116 (Calendar sync gap) com sweep findings. Operacional: emails Thayanne+Danilo Resend ack delivered (`5effc0ba` + `d4976563`, Danilo opened 16:10:02), Danilo `interview_pending → submitted`. Smoke 11/11 invariants = 0 violations + 8/8 Phase A substrate checks. Routine `trig_01DYnnv5...` armed 2026-05-02T14:00Z verifica reschedule outcome. | p86 MEGA-marathon 2026-05-01: 10 commits autônomos + 6 deploys + 5 migrations + 2 issues fechadas + **Wave 5b chain 100% COMPLETE end-to-end**. `0b62229` Wave 5b-1b ai_analysis_runs separate table (RLS committee+manage_member SELECT, anon DENY) + backfill 6 apps run_index=1 + EF refactor pmi-ai-analyze (INSERT row up-front + UPDATE on completion/fail, derives triggered_by from enrichment_count) + EF redeploy. `b3846d6` Wave 5b-3 admin diff UI: nova aba "Análises IA" no modal admin/selection com runs timeline expansível (run_index DESC, badges triggered_by + status, snapshot expand: sumário/strengths/areas_to_probe/red_flags/fields_changed/tokens/duration) + topics-viewed audit count (informational). RPC `get_application_ai_analysis_runs` V4 committee/manage_member/view_internal_analytics. Migrations 20260516350000+360000. #115 CI closed (raiz: 8 commits unpushed); #113 Mayanna closed (audit revelou 7/7 items já implementados desde p77/p79). | p86 marathon 2026-05-01: 5 commits autônomos + 4 deploys + 4 migrations. `8d1de5d` Wave 5d core (RPC `request_interview_reschedule` V4 manage_member-or-committee-lead + admin UI amber block + email via campaign_send_one_off template `interview_reschedule_request` 3 langs + 14 contract tests + migrations 20260516320000+330000). `1ae7522` corrigi `.claude/rules/database.md` apply_migration MCP gap (NÃO escreve file local nem registra schema_migrations — manual repair mandatório). `d0f2acd` admin UX fixes (Data Aplicação column em pipeline list + filtros category/source no histórico de campanhas). `2b4c8cc` #5 architectural: Comunicação tab no perfil candidato (RPC `get_application_communications` lookup por external_email cobre PMI welcomes + reschedule + futuras sem backfill, migration 20260516340000). `343dc60` Wave 5b-2 portal frontend (Cards B + A): InterviewTopicsOptIn opt-in reveal + log_topic_view audit + EnrichmentCard 5 textareas com cap 2/cooldown 5min/cap-reached message + 28 i18n × 3 langs + onEnriched re-poll 8s. Wave 5b chain: 5b-0+5b-1+5b-2 ✅ ; 5b-1b/5b-3 deferred. Wave 5d ✅ E2E. Worker version `3ddd12ad-6516-48ad-a8dc-225cf9eeb9a1`. 8 sediment patterns (apply_migration MCP gap, campaign_send_one_off canonical, comms↔candidate via external_email, frontend changes need wrangler deploy separado, etc). Routine `trig_01DYnnv5...` armed 2026-05-02T14:00Z (Thayanne verify). | p84 EPIC 2026-04-30: 20 commits autônomos em 7 phases. Phase 1 chapter sweep 4 commits (admin/report+ReportPage data-driven, frontpage 3 sections + i18n + title 15, cycle-report HSL chart colors, certificate text neutralized 7 surfaces). Phase 2 Council UX/Product Wave 1+2+3 reorganização 6 commits (reorder 14 sections + drop dup stats, Hero "15 capítulos" pill, KPI goal-beat 8→15 superada, footer/ResourcesSection/chapters.cta neutralized, TribesSection a11y WCAG, CpmaiSection PT-BR announcement). Phase 3 PM-flagged content 1 commit. Phase 4 dynamic Dream Team + iniciativas + migration 20260430144154 (1 commit + 1 migration — `get_homepage_stats`/`get_public_platform_stats` extended com initiatives + active_leaders). Phase 5 SEO/RSS baseline 1 commit (sitemap.xml + /blog/feed.xml + /publications/feed.xml). Phase 6 MCP catalog + framework 3 commits (`scripts/generate-mcp-manifest.mjs` + `/docs/mcp` Astro page Path B curado + `docs/drafts/blog-mcp-framework-outline.md`). Phase 7 Wave 5b spec + council Tier 2 + 5b-0 legal blockers 4 commits (`docs/specs/p84-wave5-ai-augmented-self-improvement.md` + bug fix /blog anon read migration 20260430223830 + privacy.s4.googleAi 3 langs B1 + ADR-0067 N1 Art.20 safeguards + R3-C3 cláusula 9-A draft N2). 8 council agents convocados em 3 sets paralelos. Wave 5b-1+ destravada (schema migration + EF redeploy + frontend Card B antes A + admin diff). Memory updated: handoff_p85 + reference_key_people (Roberto Macedo correto, Macioni hallucination corrigida `d56b36d`). p83 chapter expansion foundation 2026-04-29: 3 commits autonomous data-driven `chapter_registry`. Sprint 1 backend: schema +country/display_order/logo_url cols + backfill 5 founding chapters + RPC `get_active_chapters()` SECDEF anon-grant LGPD-safe (cnpj não exposto). Sprint 2 frontend: novo `src/lib/chapters.ts` helper module-cached + 7 hot-spots refactored (3 React islands via useEffect+loadChapters; 3 Astro components SSR-fetch ou JSON injection; 1 admin page inline-script). Removed CHAPTER_FULL constant. Sprint 4 i18n: meta.description (3 langs) drops "5"; privacy.s1.chapters template `{list}` placeholder; footer.chapters semantic label + BaseLayout SSR-renders dynamic line; about.astro descriptions drop "5"; certificate footer text deferred Sprint 5. **15 chapters expansion now data-only** — quando Ivan mandar lista oficial: INSERTs em `chapter_registry` + adicionar 10 PNG logos = footer/privacy/team/admin atualizam automaticamente. p82 CBGPL launch 2026-04-29 18h: Ricardo Vargas QR + 15 chapters PMI Brasil união anunciada Ivan. Phase B portal completa: 6 UX fixes R1-R6 + interview opt-out all-or-nothing + Calendar booking `gh9WjefjcmisVLoh7` + Apps Script auto-add guests + profile self-service (LinkedIn/Credly/WhatsApp). Phase C parcial: EF `pmi-ai-analyze` gemini-2.5-flash + responseSchema PT-BR + retry 1s/4s/9s + anti-bias, dispatch via give_consent_via_token. N1 Resend daily throttle 100/day + 4/s burst + cron */30min. N2 AI retry cron 0 * * *. RPC `get_pmi_launch_health()` real-time observability. p82: 9 commits + 8 migrations 250000-310000 + 2 crons + 2 EFs (1 nova). DEFERRED V2: CV PDF extraction + LinkedIn post scraping. ADR-0066 PMI Journey v4 Phase 1 LIVE com Amendment 2026-04-29 pivot to /ingest pattern: 4 new tables + 22 RPCs (14 token-auth + 8 service helpers) + 2 views + 3 triggers. Worker pmi-vep-sync `dfaee1d6` + browser script. | p80 marathon: Phase B'' batches 15-20 (18 V3→V4 conversions across attendance/tribe/privacy/interview/showcase/admin/onboarding/selection-committee/eval-readers/dashboard tracks) — Phase B'' 131/246 (~53.3%) | p79: Mayanna feedback rounds 2+3 + #113 frontend 5/5 closed + Phase B'' batches 11-14 | p78: ADR-0065 Drive Phase 4 auto-discovery atas via cron + filename date heuristic + auto-promote + Pattern 43 4th reuse health RPC; ADR-0064 Drive Phase 3 LIVE com Path F OAuth refresh; 5 EFs Drive, 8 MCP tools Drive total | p77 ULTRA-EXTREME-marathon: Mayanna 6/7 items closed, ADR-0063 WhatsApp, drift v4 catched 3 LGPD broken | Phase B'' V3→V4: **131/246 (~53.3%)**)
- **AI Model:** Claude Opus 4.7 (`claude-opus-4-7`) — released 2026-04-16. xhigh effort level available. Updated tokenizer (1.0-1.35x token mapping). /ultrareview for code review.
- **Wiki:** GitHub org `nucleo-ia-gp` — repos `wiki` (private, Obsidian vault) + `frameworks` (public, CC-BY-SA/MIT). Synced to `wiki_pages` table via FTS. Scope: narrative knowledge only (ADR-0010) — operational data stays in SQL.
- **LGPD:** Art. 18 cycle complete (consent gate + export + delete + anonymize cron 5y)

## Build & Test
```bash
npx astro build          # MUST pass before commit
npm test                 # 1383 pass / 0 fail / 35 skip locally (with SUPABASE_SERVICE_ROLE_KEY: 1418 total)
npx wrangler deploy      # Deploy Worker
supabase functions deploy <name> --no-verify-jwt  # Deploy EF
```

## GC-097: Pre-Commit Validation (MANDATORY)

### If you touched SQL/RPC:
1. Check FK constraints: `SELECT constraint_name, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'TABLE'::regclass AND contype = 'f';`
2. Verify `auth.uid()` vs `members.id` — events.created_by FK → auth.users(id), NOT members(id)
3. Test the RPC with real data via MCP execute_sql
4. Check for column name mismatches: members uses `name` (not `full_name`), `credly_url` (not `credly_username`), publication_submissions uses `submission_date` (not `submitted_at`)
5. Check array types: members.designations is `text[]` (not jsonb). Use `&&` not `?|`, use `array_length()` not `jsonb_array_length()`

### If you touched i18n:
1. Every new key MUST exist in ALL 3 dictionaries (pt-BR.ts, en-US.ts, es-LATAM.ts)
2. Grep for raw keys in components: any `t('key.name')` must have a corresponding entry
3. Check the key name matches exactly (e.g., `modal.advanced` vs `modal.advancedFields`)

### If you created/modified routes:
1. If a PT-BR page exists, /en/ and /es/ redirect pages must also exist
2. Check: `ls src/pages/en/X.astro src/pages/es/X.astro`

### If you modified an RPC signature:
1. Use DROP + CREATE (not CREATE OR REPLACE) when changing parameter types or count
2. Check for overloaded functions: `SELECT count(*) FROM pg_proc WHERE proname = 'X' AND pronamespace = 'public'::regnamespace`
3. After applying to DB, ALWAYS run: `NOTIFY pgrst, 'reload schema'`
4. Mark migration as applied: `supabase migration repair --status applied TIMESTAMP`

### ALWAYS:
1. Run `npx astro build` — must pass with 0 new errors
2. `npm test` — 0 failures
3. No hardcoded legacy URLs (grep for `platform.ai-pm-research-hub.workers.dev`)

## Key Architecture Decisions (do NOT re-litigate)
1. `checkOrigin: false` + manual CSRF in middleware (Astro's check blocks OAuth/MCP POSTs)
2. Custom domain `nucleoia.vitormr.dev` (`.workers.dev` has Bot Fight Mode blocking datacenter IPs)
3. `@modelcontextprotocol/sdk@1.29.0` + WebStandardStreamableHTTPServerTransport + Zod 4 schemas for MCP
4. Webinars table is source of truth (not events filtered by type)
5. Board items read-all for Tier 1+ members (curators need cross-board access)
6. LGPD: anon/ghost gets nothing from PII tables; public data via SECURITY DEFINER RPCs only
7. V4 Authority: `can()` is the canonical gate (ADR-0007). RLS uses `rls_can(action)` helpers. `operational_role` is a cache maintained by `sync_operational_role_cache` trigger.

## Detailed Rules
- Database: `.claude/rules/database.md`
- i18n: `.claude/rules/i18n.md`
- MCP: `.claude/rules/mcp.md`
- Deploy: `.claude/rules/deploy.md`

## Council (multi-agent review structure)
**Active since 2026-04-18.** 12 especialized sub-agents em `.claude/agents/` (product-leader, ux-leader, c-level-advisor, stakeholder-persona, senior-software-engineer, ai-engineer, data-architect, security-engineer, startup-advisor, vc-angel-lens, legal-counsel, accountability-advisor) operando em 3 tiers:

- **Tier 1 (always)**: `platform-guardian` + `code-reviewer` em início/fim de sessão e mudanças estruturais
- **Tier 2 (domain-triggered)**: invocar agent específico conforme domínio (ver `docs/council/README.md` tabela)
- **Tier 3 (strategic)**: `/council-review [topic]` em milestones — output em `docs/council/`

Todos são **consultivos** (não modificam código). PM/main loop decide ação. Decision log em `docs/council/decisions/`.
