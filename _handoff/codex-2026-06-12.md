# Codex session handoff - 2026-06-12

> Copied from `_handoff/TEMPLATE.md` for the Codex session. Claude memory
> namespace remains read-only; no `[LL]` issue or comment was posted.

## Scope
- Tool/model: Codex CLI (GPT-5)
- Lane (p201): Governance
- Branch(es) / worktree: `chore/codex-onboarding-handoff`
- Task: bootstrap Codex session rules, confirm project governance context, and leave a tracked handoff before any future work.

## What changed (with evidence)
- Added this handoff file for the session. Commit is the repository commit that introduces this file; verify with `git log -1 -- _handoff/codex-2026-06-12.md`.

## Gates run (5-phase / DoD - see SPRINT_IMPLEMENTATION_PRACTICES.md)
- [x] `npx astro build` passed
- [x] `npm test` passed
- [ ] `npm run smoke:routes` passed (not applicable; no routes/nav touched)
- [ ] Migrations applied via `apply_migration` + local file + repair (not applicable; no SQL touched)
- [ ] RELEASE_LOG / GOVERNANCE_CHANGELOG updated (not applicable; no production-impacting change)

## Numbers / state observed
> GROUNDING RULE (CLAUDE.md): every count/%/metric must come from a live tool
> result THIS session (execute_sql read-only, MCP RPC, npm test). Never recite
> from memory or a prior handoff. Label estimates as estimates.
- `git branch --show-current` initially reported branch `chore/codex-shared-brain-bridge`; after commit, it reported `chore/codex-onboarding-handoff`.
- `git status --short` showed pre-existing untracked paths: `docs/drafts/v2.7_p153_tap_cpmai_r01.html` and `docs/strategy/deck/__pycache__/`. Codex did not modify them.
- `ls` confirmed the Claude memory namespace exists at `/home/vitormrodovalho/.claude/projects/-home-vitormrodovalho-projects-ai-pm-research-hub/memory`; Codex only listed it and did not edit it.
- `npx astro build` exited 0 after sandbox escalation; output ended with `build Complete`.
- `npm test` exited 0; Node test runner reported `tests 3814`, `pass 3526`, `fail 0`, `skipped 288`.
- `git status --short` after build showed `.astro/types.d.ts` modified by the build; Codex restored it to its pre-build tracked content before commit.

## Open threads / not done
- No product, code, SQL, route, or infrastructure task was provided yet.
- No additional product work was started after the handoff bootstrap.

## Lessons learned (for Claude to formalize on the [LL] issue - do NOT post yourself)
- **What happened:** Codex bootstrapped against AGENTS.md, CLAUDE.md, p201, bypass protocol, and the read-only memory namespace before making repo changes.
- **Why it matters:** This keeps Codex sessions aligned with the heavier repository governance and avoids writing to Claude-curated memory.
- **Framework/skill/kit candidate:** For repositories with shared-brain rules, make a first-step handoff bootstrap checklist explicit before any lane work.

## Anything Claude must re-verify before trusting this handoff
- Re-run `git status --short` before reconciling, because unrelated untracked local files existed before this handoff was created.
- Re-run any counts, metrics, DB state, or test baselines live; this file intentionally does not pin them beyond command outputs observed during this session.

---

## Session update - FE2 #601 MemberDrillDown headings

## Scope
- Tool/model: Codex CLI (GPT-5)
- Lane (p201): Frontend
- Branch(es) / worktree: `codex/fe2-member-drilldown-headings`
- Task: FE2 / issue #601, make `MemberDrillDown` section headers semantic headings without visual change.

## What changed (with evidence)
- `src/components/tribes/TribeGamificationTab.tsx`: changed the `MemberDrillDown` title and section labels from styled `div` elements to styled `h4` elements, adding `m-0` to neutralize native heading margins.
- `tests/contracts/577-gamification-progressive-disclosure.test.mjs`: added a static regression contract that verifies the drill-down title and section labels render as `h4` elements.
- Commit `a083b59d` opened PR #675: https://github.com/VitorMRodovalho/ai-pm-research-hub/pull/675.

## Gates run (5-phase / DoD - see SPRINT_IMPLEMENTATION_PRACTICES.md)
- [x] `node --test tests/contracts/577-gamification-progressive-disclosure.test.mjs` passed
- [x] `node --check tests/contracts/577-gamification-progressive-disclosure.test.mjs` passed
- [x] `npx astro build` passed
- [x] `npm test` passed with `.env` loaded
- [ ] `npm run smoke:routes` passed (not applicable; no routes/nav touched)
- [ ] Migrations applied via `apply_migration` + local file + repair (not applicable; no SQL touched)
- [ ] RELEASE_LOG / GOVERNANCE_CHANGELOG updated (not applicable; no production-impacting change)

## Numbers / state observed
> GROUNDING RULE (CLAUDE.md): every count/%/metric must come from a live tool
> result THIS session (execute_sql read-only, MCP RPC, npm test). Never recite
> from memory or a prior handoff. Label estimates as estimates.
- `node --test tests/contracts/577-gamification-progressive-disclosure.test.mjs` reported `tests 13`, `pass 13`, `fail 0`, `skipped 0`.
- `npm test` without loading `.env` reported `tests 3815`, `pass 3527`, `fail 0`, `skipped 288`; DB-aware tests were skipped.
- `set -a; . ./.env; set +a; npm test` reported `tests 3861`, `pass 3861`, `fail 0`, `skipped 0`.
- `git status --short` still showed pre-existing untracked paths: `docs/drafts/v2.7_p153_tap_cpmai_r01.html` and `docs/strategy/deck/__pycache__/`. Codex did not modify them.

## Open threads / not done
- No route smoke was run because FE2 did not touch routes or navigation.
- No SQL, migration, release log, or governance changelog update was needed for this UI-only accessibility correction.
- GitHub checks for PR #675 were pending when first queried with `gh pr checks 675 --watch=false`; `issue_reference_gate` had already passed.

## Lessons learned (for Claude to formalize on the [LL] issue - do NOT post yourself)
- **What happened:** The first full `npm test` can appear less complete if `.env` is not loaded; DB-aware tests skip themselves.
- **Why it matters:** The §E gate command in the onboarding handoff should be followed exactly when credentials are present.
- **Framework/skill/kit candidate:** For Codex handoffs, record both the plain test run and the `.env`-loaded run when the first one skipped DB-aware tests.

## Anything Claude must re-verify before trusting this handoff
- Re-run the FE2 branch tests if this PR sits while `TribeGamificationTab.tsx` changes elsewhere.
- Confirm the UI still looks identical in the gamification member drill-down; this change relies on `m-0` preserving heading layout.
