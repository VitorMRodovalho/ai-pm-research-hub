---
interface Props {
  title: string;
  subtitle?: string;
  cardId?: string;
  canvasId?: string;
  heightClass?: string;
  bodyClass?: string;
}

const {
  title,
  subtitle = '',
  cardId,
  canvasId,
  heightClass = 'h-[320px]',
  bodyClass = 'p-4',
} = Astro.props;
---

<section id={cardId} class="bg-[var(--surface-card)] border border-[var(--border-default)] rounded-xl overflow-hidden">
  <div class="px-4 py-3 border-b border-[var(--border-default)]">
    <h2 class="text-sm font-bold text-navy">{title}</h2>
    {subtitle ? <p class="text-[.68rem] text-[var(--text-muted)]">{subtitle}</p> : null}
  </div>
  <div class={bodyClass}>
    {canvasId ? (
      <div class={heightClass}>
        <canvas id={canvasId}></canvas>
      </div>
    ) : null}
    <slot />
  </div>
</section>
