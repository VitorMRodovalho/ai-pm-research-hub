---
// A2 #740 — /en/onboarding consolidated into the single /en/workspace cockpit.
// See src/pages/onboarding.astro for rationale. Redirect (302, qs preserved).
const qs = Astro.url.search || '';
return Astro.redirect('/en/workspace' + qs, 302);
---
