---
// A2 #740 — /es/onboarding consolidated into the single /es/workspace cockpit.
// See src/pages/onboarding.astro for rationale. Redirect (302, qs preserved).
const qs = Astro.url.search || '';
return Astro.redirect('/es/workspace' + qs, 302);
---
