---
const url = new URL(Astro.request.url);
const lang = url.searchParams.get('lang');
const target = lang ? `/publications?lang=${encodeURIComponent(lang)}` : '/publications';
return Astro.redirect(target, 301);
---
