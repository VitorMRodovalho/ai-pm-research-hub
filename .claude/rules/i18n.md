---
description: Internationalization rules for trilingual support
globs: src/i18n/**/*.ts, src/pages/**/*.astro
---

---
description: i18n rules — 3-dictionary parity (pt-BR/en-US/es-LATAM), key naming, per-locale page redirects
paths:
  - "src/i18n/**"
  - "src/pages/**"
  - "src/components/**"
---

# i18n Rules

1. Every new key MUST exist in ALL 3 dictionaries: pt-BR.ts, en-US.ts, es-LATAM.ts
2. Grep for raw keys in components: any `t('key.name')` must have a corresponding entry
3. Check the key name matches exactly (e.g., `modal.advanced` vs `modal.advancedFields`)
4. If a PT-BR page exists, /en/ and /es/ redirect pages must also exist
5. Verify: `ls src/pages/en/X.astro src/pages/es/X.astro`
