---
description: Deployment rules and verification
globs: wrangler.toml, astro.config.mjs
---

# Deployment Rules

## Pre-Deploy Checklist

1. `npx astro build` — must pass with 0 errors

2. `npm test` baselines (last updated **p240 — 2026-05-24** via #251 selection interview status transition gap close — 13 new contract assertions (11 static + 2 DB-gated) for the AFTER INSERT OR UPDATE trigger on `selection_interviews` + 14-row backfill scoping cycle4-2026 + cycle3-2026-b2. SEDIMENT-186.C reminder triggered: initial `npm test` reported 1925/1863 instead of 1938/1874 because the new file was not added to the `package.json` `"test"` + `"test:contracts"` whitelist — both scripts use hardcoded file lists, not glob. **Updated pins below absorb p239b actual baseline drift (was pinned 1916/1854 but actual was 1925/1863 — +9 from `p239b-332-hotfix-accessor-id-fk.test.mjs` operator+hotfix tests that landed but didn't bump the pin)**:
   - **Offline CI** (no DB env): **1938 total / 1874 pass / 0 fail / 64 skip** *(p240 #251 — +13 from new contract test `tests/contracts/p240-251-interview-status-transition-trigger.test.mjs`: 11 static migration body assertions + 2 DB-gated. Static (11): migration file existence + trigger fn SECDEF/pinned search_path + AFTER INSERT OR UPDATE OF (status, conducted_at) narrow-scope declaration + all 7 terminal-status guards present in early-return + conducted_at OR status=completed branch UPDATEs app to `interview_done` with broad source-status precondition + scheduled/rescheduled branch UPDATEs to `interview_scheduled` with narrower source-status precondition (excludes `interview_scheduled`/`interview_done` to prevent regression) + REVOKE EXECUTE FROM PUBLIC + backfill scope cycle4-2026 + cycle3-2026-b2 (CTE evidence ladder mirrors trigger body) + audit canonical action `p240_251_backfill_interview_status` + `admin_audit_log` shape (actor_id/target_type — NOT actor_member_id/target_kind which don't exist; SEDIMENT-240.A NEW lesson, caught at first apply_migration atomic-rollback) + sanity DO RAISES on residual drift + NOTIFY pgrst. DB-gated (2): migration 20260805000025 registered in supabase_migrations.schema_migrations (proxy for trigger deploy) + post-backfill 0 apps in cycle4+cycle3-b2 with `status='interview_pending'` AND a submitted interview eval. **p239b absorbed silent drift was 1925/1863/0/62; p238b was 1899/1837/0/62; p238 was 1879/1820/0/59; p234 was 1844/1789/0/55.**)* *(p239b #332 operator surface — +17 from new contract test `tests/contracts/mcp-lgpd-retroactive-operator-tools.test.mjs`: 2 tool-registration existence + 2 canV4(manage_member) gate + 2 Zod schema completeness (5 params each) + 1 ADR-0018 W1 confirm gate behavior on deletion + 1 forward-defense notification-no-confirm + 1 JS reason length guard + 1 UUID validate via isUUID + 3 version bump consistency (nucleo-ia-hub 2.79.0 + ef_version 2.80.0 + /mcp tools 301) + 1 header changelog mentions v2.80.0+p239b#332 + 2 matrix coverage (json + MD H1) + 1 forward-defense both names present. Plus ratchet updates in `mcp-semantic-gateway-bridge.test.mjs` (v2.79.\d+ → v2.(79|80).\d+ marker + /health tools 299 → 301) + `member-emails-write-surface.test.mjs` (/health /mcp tools 299 → 301). **p238b was 1899/1837/0/62 (p238b #332 infrastructure shipped 20 contract tests above p238 #331's 1879/1820/0/59). p234 was 1844/1789/0/55.**)* *(p234 #322 Gap B close + close-review — +20 from new contract test `p234-322-volunteer-term-gap-b-classification.test.mjs`: 19 static migration body assertions + 1 DB-gated. Static (15 from initial patch + 4 from close-review): migration file existence + backfill scope (no cert + no active requires_agreement engagement) + backfill metadata fields (reason=no_requires_agreement_engagement) + backfill audit action + approve_selection_application forward guard inline clause + approve_selection_application SECDEF/search_path preserved + admin_offboard_member offboarding extension UPDATE + offboarding extension metadata reason (offboarded_pre_signing) + offboarding extension audit conditional on rows_affected + admin_offboard_member SECDEF/search_path + ARM-9 G3 preserved + get_my_onboarding completed_steps harmonized (IN ('completed','skipped')) + get_my_onboarding all_complete harmonized (NOT IN ('completed','skipped')) + get_my_onboarding SECDEF/search_path + step rendering verbatim + sanity DO block RAISES EXCEPTION on goal-metric violations + NOTIFY pgrst + **close-review (4 new): migration 20260805000020 present + v_has_req_agreement_engagement boolean declared via EXISTS on engagements+engagement_kinds (status=active + requires_agreement=true) + auto-seed INSERT includes guard `AND NOT (s.id='volunteer_term' AND NOT v_has_req_agreement_engagement)` + skipped≡completed harmonization + SECDEF + per-step rendering verbatim + NOTIFY pgrst preserved**. **p233 was 1824/1770/0/54**.)* *(p233 #321 Gap A close — +12 from new contract test `p233-321-complete-volunteer-term-on-cert.test.mjs`: 11 static migration body assertions + 1 DB-gated (skips offline). Static covers trigger fn SECDEF/search_path + AFTER INSERT trigger WHEN clause + body NULL member_id guard + idempotent UPDATE + metadata enrichment + audit gated on rows_affected + backfill DISTINCT ON CTE + backfill audit + sanity RAISE EXCEPTION + NOTIFY pgrst. **p232 was 1812/1759/0/53**.)* *(p228 W2 + hotfix — full delta vs p227 baseline (1759): +34 total across 7 leaves + hotfix. Hotfix #313 = +3 explicit forward-defense assertions in `adr-0022-delivery-mode.test.mjs` (no RETURNING ... INTO + scalar v_caller_id + audit gate); the remaining +6 are from the `findLatestFunctionMatch`-derived sub-asserts that count when migration body covers extra branches. **Total Amendment D suite: 33 forward-defense assertions across 7 leaves + hotfix locking selection notification policy matrix + replay safety.** Pre-hotfix offline was 1784 (post-PR-307 baseline).)*
   - **With-DB** (SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY): **~1846 total / ~1846 pass / 0 fail / 0 skip** *(p234 with-DB delta post-close-review: p233 baseline ~1826 + +20 contract test → ~1846 expected. CI evidence to be confirmed post-merge.)* *(p228 with-DB CI evidence post-#307 was 1817/1817; +9 hotfix increment expected to bring it to ~1826. CI evidence post-#313 merge to be confirmed.)* *(p227 #298 — +8 static assertions in `get-my-pending-evaluations-cycle-deterministic.test.mjs` for Option A+ fix: migration file existence + cycle picker uses ORDER BY created_at DESC + gate cycle-scoped (sc.cycle_id = v_cycle.id) + legacy permissive JOIN gate absence + Unauthorized cycle-scoped message + can_by_member admin bypass retained + empty-cycle precedes gate ordering + NOTIFY pgrst issued)* ; *(p225 vault refactor — adds 7 new certificate-pdf-autogen-trigger.test.mjs assertions covering: vault refactor migration 20260805000006 exists + reads from vault.decrypted_secrets + preserves fn signature for trigger binding + best-effort exception semantics + sanity DO + SEDIMENT-225.B doc + ADR-0098 vault path. Total 22+7=29 assertions on cert PDF pipeline.)* ; *(p225 initial — adds 22 new certificate-pdf-autogen-trigger.test.mjs static assertions covering: migration trigger fn SECDEF + secret read + pg_net call + AFTER INSERT WHEN(pdf_url IS NULL) gate + EXCEPTION handler + sanity DO + REVOKE/GRANT; endpoint Bearer auth + service-role client + pdf.ts reuse + CF Browser Rendering + idempotency guard + storage upload + POST/GET method gate; wrangler.toml [browser] binding; middleware.ts CSRF bypass for /api/internal/; ADR-0098 + README entry; @cloudflare/puppeteer dep; @page CSS parity with backfill script)* ; *(p224 — 9 new ADR-0097 ratchet tests in `rpc-migration-coverage.test.mjs`: 3 SIZE assertions offline + 6 DB-gated. Baseline files at `docs/audit/MIGRATION_*_BASELINE_P224.txt` 694+15+41=750. Helper RPC `_audit_list_schema_migrations()` returns jsonb. **Sediment §4**: TABLE-returning RPCs silently truncated at 1000 rows by PostgREST — use `RETURNS jsonb` + `jsonb_agg(...)`.)* ; *(p223 — fix TAP diff view leaks `<style>body{max-width:920px}</style>` globally via dangerouslySetInnerHTML; +7 contract tests in `version-diff-viewer-style-isolation.test.mjs`)* ; *(p222 — +25 mcp-semantic-gateway-bridge assertions; corrected chronic drift pin 1681 actual `1681 total = 1639 pass + 42 skip`)*
   - **With DB env** (SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY): **~1773 total / ~1731 pass / 0 fail / ~5 skip** *(p227 — empirical: p225 with-DB ~1765 + p227 +8 static (no DB-gated) = 1773 expected. CI evidence to be confirmed post-merge.)*
   - Coverage delta vs prior baselines: Phase C 3 tests + Q-C/p65/Pacote M unlocks + p185 admin_audit_log allowlist fix + 4 `detect_inactive_members` contract tests (dry_run shape + tx=rollback runtime + p186 INSERT-path hermetic helper + p187 misuse-path defensive restore) + 2 p188 `member_cycle_history` self-read static tests (GAP-181.B closure) + 1 p192 `exec_cross_initiative_comparison` p_kind dispatch test (OPP-191.A close inline LOW) + 2 p194 contract tests (GAP-192.C total_hours + GAP-194.A members_inactive_30d strict scope) + 9 p212 #224 admin-selection-import-error-rendering contract tests + 4 p212 #237 onboarding-token-organization-id contract tests + 11 p214 GAP-205.A/B member-emails RLS multi-tenant + 2 p215 GAP-205.C verified_at absence forward-defense + **22 p216 GAP-205.D write-surface (3 RPCs × ~7 assertions each: existence + SECDEF/search_path + auth gate + primary-rejection + GRANT EXECUTE + MCP registration + version/health label parity) + 7 p216 council amendments (HIGH LGPD generic msg + MED #1 FOR UPDATE + MED #2 org boundary anchor × 3 RPCs + LOW #1 generic remove primary msg)**.
   - p196/p197/p198 added 0 tests despite 10 migrations + new RPCs (GAP-197.G: 0 contract tests pros 2 RPCs novos `complete_peer_review` + `complete_leader_review` — backlog).
   - Drift history (newest → oldest): **p239b +17** (mcp-lgpd-retroactive-operator-tools for #332 operator-path unblock — 17 static, no DB-gated: 2 tool-registration existence + 2 canV4('manage_member') gate + 2 Zod schema completeness × 5 params each (notification = uuid+string+string+enum+ISO; deletion = uuid+uuid+string+string+boolean) + 1 ADR-0018 W1 confirm gate behavior (deletion branches on `confirm !== true` → preview envelope with `preview:true` + `will_clear_transcription` + `cross_app_check` + `next_call` hint with `confirm:true`; logs `result_kind="preview"` to mcp_usage_log) + 1 forward-defense notification-no-confirm (audit-row only, must not include `confirm:` Zod field to keep ADR-0018 scope clean) + 1 JS layer reason length guard (`>=8` mirrors RPC sanity) + 1 UUID validate via `isUUID()` for both tools + 3 version bump consistency (`name:"nucleo-ia-hub", version:"2.79.0"` + `ef_version:"2.80.0"` + `/mcp: {server:"nucleo-ia-hub", version:"2.79.0", tools:301}` in /health surface report) + 1 header changelog mentions `v2.80.0` + `p239b #332 W3 LGPD` provenance + names both tool ids + 2 matrix coverage (`mcp-tool-matrix.json` lists both names; `MCP_TOOL_MATRIX.md` H1 reflects new "304-Tool" total = 301 /mcp + 3 /semantic) + 1 forward-defense both names must remain present. Plus 2 ratchet updates on existing tests: `mcp-semantic-gateway-bridge.test.mjs` (bridge era marker loosened from `v2.79.\\d+` to `v2.(79|80)\\.\\d+`; /health tools count 299 → 301) + `member-emails-write-surface.test.mjs` (/health /mcp tools count 299 → 301 with history comment 296 p215 → 299 GAP-205.D → 301 p239b). Live smoke 3/3 PASS (/health 301 + v2.79.0; tools/list 301 with both names; tool/call bogus JWT → "Not authenticated"). Invariants 19/19=0.); **p238b +20** (lgpd-art-18-retroactive-deletion for #332 W3 audit-log infrastructure — 11 static migration + 4 static docs + 2 forward-defense + 3 DB-gated; baselines bumped 1879→1899); **p238 +23** (consent-voice-biometric-ui for #331 W2 destacado consent UI — 17 static + 3 forward-defense + 3 DB-gated; baselines bumped 1856→1879); **p234 +20** (p234-322-volunteer-term-gap-b-classification for #322 / Gap B of #230 reframe close + close-review patch via migration 20260805000020 — 19 static + 1 DB-gated; close-review added +4 assertions covering get_my_onboarding auto-seed forward guard that mirrors approve_selection_application guard. Caught by PM curator review of PR #327 before merge. Static: file existence + backfill scope (step_key=volunteer_term + status=pending + NOT EXISTS cert WHERE c.type=volunteer_agreement AND c.status=issued + NOT EXISTS engagements WHERE e.status=active AND ek.requires_agreement=true) + backfill writes status=skipped + metadata.completed_via=p234_322_backfill_no_agreement_path + metadata.reason=no_requires_agreement_engagement + metadata.migration=20260805000019 + COALESCE(metadata,'{}'::jsonb)||jsonb_build_object preserves existing keys + backfill audit action=p234_322_backfill_volunteer_term_no_agreement + approve_selection_application has guard `AND NOT (s.id = 'volunteer_term' AND NOT COALESCE(v_requires_agreement, FALSE))` in Path A onboarding_steps catalog INSERT + approve_selection_application preserves SECURITY DEFINER + pinned search_path TO 'public', 'pg_temp' + admin_offboard_member UPDATE onboarding_progress SET status=skipped WHERE step_key=volunteer_term AND status=pending + GET DIAGNOSTICS v_vol_terms_skipped = ROW_COUNT + extension metadata.completed_via=p234_322_offboarding_extension + metadata.reason=offboarded_pre_signing + metadata.offboarded_to_status captured + extension audit conditional IF v_vol_terms_skipped > 0 with canonical action=onboarding.volunteer_term_skipped_on_offboard + admin_offboard_member preserves SECURITY DEFINER + pinned search_path + ARM-9 G3 alumni_recognition logic intact + get_my_onboarding completed_steps uses status IN ('completed', 'skipped') + all_complete uses status NOT IN ('completed', 'skipped') + get_my_onboarding preserves SECDEF + pinned search_path + per-step COALESCE(op.status, 'pending') rendering verbatim (UI can render skipped distinctly) + sanity DO RAISES EXCEPTION on remaining goal-metric violations (active members with pending vol_term AND no active requires_agreement engagement) + NOTIFY pgrst. DB-gated: live goal metric = 0 active members with pending vol_term and no active requires_agreement engagement.); **p233 +12** (p233-321-complete-volunteer-term-on-cert for #321 / Gap A of #230 reframe close — 11 static + 1 DB-gated. Static: file existence + trigger fn `_trg_complete_volunteer_term_on_cert` SECURITY DEFINER + pinned search_path + AFTER INSERT trigger ON public.certificates with `WHEN (NEW.type = 'volunteer_agreement')` + DROP TRIGGER IF EXISTS + body NULL member_id guard + UPDATE idempotency via `status != 'completed'` filter on unique `(member_id, step_key)` index + completed_at COALESCE(NEW.issued_at, now()) + metadata enrichment with cert_id + verification_code + migration tag + audit insert only when `v_rows_affected > 0` + canonical action `onboarding.volunteer_term_completed_on_cert` + backfill DISTINCT ON latest cert per member CTE + backfill audit action `p233_321_backfill_volunteer_term_phantom` + sanity DO block RAISES EXCEPTION on remaining phantom + NOTIFY pgrst. DB-gated: post-backfill 0 phantom vol_term rows where matching cert exists.); **p232 +12** (p232-229-phase2-leader-extra-visibility for #229 Phase 2 close — 8 static migration body assertions + 4 DB-gated. Static: file existence + 2-arg overload signature + CHECK whitelist leader_extra_pert_score + dual-track LE math (target/band/calc_at/cohort_n + distribution comparisons) + get_application_score_breakdown leader_extra_cutoff block + leader_extra_score_position + top-level leader_extra_pert_score in core + get_selection_dashboard.cycle leader_extra_cutoff sibling + per-row leader_extra_pert_score in application rows + DROP 1-arg overload + NOTIFY pgrst + WHAT/WHY/ROLLBACK header. DB-gated: 1-arg overload absence + leader_extra_pert_score acceptance + leader_extra_cutoff key present + 6 phase-1 columns selectable. Plus 1 ratchet in mcp-semantic-gateway-bridge.test.mjs to allow v2.79.\\d+ patch bumps.); **p230 +7** (a3-active-role-not-none-constraint for #318 / RESOLVED A3 invariant drift — 6 static + 1 DB-gated: migration file existence + chk_a3_active_role_not_none constraint name + CHECK predicate exact tuple `NOT (active + 'none')` + ALTER TABLE public.members target + repair audit row action `member.operational_role_a3_repair` + V4 cache ladder branches in repair query + live `check_schema_invariants() A3=0` gate); **p227 +8** (get-my-pending-evaluations-cycle-deterministic for PR #298 / RESOLVED Code Bug A from p226 audit — Option A+ fix locks ORDER BY created_at DESC picker + cycle-scoped gate sc.cycle_id=v_cycle.id; forward-defense against legacy permissive JOIN gate re-introduction + empty-cycle-precedes-gate ordering); **p224 +9** (rpc-migration-coverage ratchet extension for ADR-0097 / WATCH-185 close — 3 NEW-drift assertions + 3 STALE assertions + 3 SIZE assertions against 3 baselines: MIGRATION_FILE_DRIFT_BASELINE_P224 (694 missing files), MIGRATION_ORPHAN_LOCAL_BASELINE_P224 (15 orphan local files), MIGRATION_EMPTY_STATEMENTS_BASELINE_P224 (41 empty-statements rows). Helper RPC `_audit_list_schema_migrations()` (migrations 20260805000003 TABLE + 20260805000004 jsonb_return). 3 SIZE always-run + 6 DB-gated skip-without-env); **p223 +7** (version-diff-viewer-style-isolation for BUG-CPMAI.C TAP diff view CSS scaffold leak); **p222 +25** (mcp-semantic-gateway-bridge for PR #285 / #280 alpha — 24 core + 1 Council HIGH-1 forward-defense: registerSemanticTools function + buildSemanticError helper + exactly 3 mcp.tool() calls in semantic block + tool names exact + Zod input schemas + envelope keys ok/data/summary/warnings/next_actions/audit + audit fields semantic_domain/permission/pii_level/source_tools/generated_at + /semantic route registered + nucleo-ia-semantic v0.1.0 + only registerSemanticTools called in /semantic handler + stateless transport + /health both surfaces + /mcp regression-safe + worker proxy file exists + UPSTREAM /nucleo-mcp/semantic + OAuth gate + auto-refresh + rate limiting + execution strip + CORS + legacy mcp.ts untouched + pii_level per tool + members no email/phone/address selection + notifications no `payload` column); **p221 +9** (certificates-bucket-and-backfill-script for PR #282 / #267 alpha — migration file existence + bucket attrs private/10MB/PDF + ON CONFLICT idempotency + RLS-deliberately-absent forward-defense against future drift adding policies via wrong path + canonical pdf.ts template import preserves visual 1:1 vs browser-print + storage path convention `<member_id>/<verification_code>.pdf` for future Option C member-owned RLS + idempotency filter (pdf_url IS NULL default) + content type application/pdf matches bucket allowed_mime + A4 margins 15/12/18/12mm parity with @page CSS); **p220 +8** (mcp-tools-list-execution-strip for PR #276 — regex detection, both strip variants, content-length delete, order check, kvLog, comment rationale, universal apply); **p220 +9** (oauth-redirect-uri-allowlist for PR #275 — root hosts, subdomains incl Perplexity variants, attacker rejected via dot anchor, custom schemes, localhost, HTTP downgrade, garbage, case-insensitivity, query/path ignored); **p220 +11** (chain-pdf-table-rendering for PR #274 — TableCell/Row types + Node union table variant + parseTable signature + header dual-path detection + masterRegex + dispatch via match[1] + renderTable styles + wrap=false rows + all 6 styles declared + renderNode table dispatch + empty rows filtered); **p220 +15** (external-reviewer for PR #272 — 7 RPC carve-out scope guards + 8 UX externalReviewMode prop / canComment widening / banner / link toggle / route existence / i18n redirects); **p219 +9** (leader-extra-cohort-separation.test.mjs for #229 Phase 1 — schema + RPC extension + cron extension + backfill scope + audit shape + sanity + file registration + NOTIFY + Phase 2 deferred-items documented); **p219 +7** (normalize-legacy-contract-volunteer.test.mjs for WATCH-257.B — backfill UPDATE + 2 RPC body changes + sanity DO scope guards + file registration + NOTIFY); **p219 +8** (auto-link-volunteer-engagement-to-cycle-cert.test.mjs for BUG-218.A — BEFORE INSERT trigger + scope guard kind=volunteer + member_id resolution via persons.legacy_member_id + backfill targets only orphan vol engagements + audit log + sanity DO + file registration + NOTIFY); **p218 +5** (certificates-page-download-wire.test.mjs for RESOLVED-217.B / #258 — 5 assertions: import + download button + click handler + record_certificate_download tracking + content_snapshot PII discipline); **p218 +2** (engagements-legal-basis-lgpd-canonical.test.mjs for RESOLVED-WATCH-257.A — migration content + filename canonical; behavioural test removed pre-merge after CI caught `.catch()` anti-pattern); **p217 +2** (certificates-page-include-volunteer-agreements.test.mjs for RESOLVED-217.A bug fix — /certificates page was filtering out volunteer_agreement certs due to missing p_include flag); **p217 +9** (engagement-kinds-catalog-invariants.test.mjs for Issue #160 path A' catalog reconciliation; covers UPDATE + VALUES-tuple INSERT + ON CONFLICT DO UPDATE re-flip patterns); p216 +29 (22 GAP-205.D + 7 council amendments); p215 +2 (column-absence assertions #12 + #13); p214 +11 (RLS multi-tenant suite); p212 +13 (1490→1503 admin-selection + onboarding-token); p198 update (1447→1449 silent drift since p194); p192 +1 pass (p_kind dispatch static); p188 +2 pass (static mch_self_read tests); p187 +1 skip (misuse-path); p186 +1 skip (INSERT-path hermetic); p185 +1 pass admin_audit_log allowlist + +2 skip detect-inactive non-dry-run; p181 pin = 1443/0/42 offline (corrected silent drift from p175 pin of 1442).
   - Update protocol: each session that adds/removes a test MUST bump the offline + with-env counts inline (manual ratchet — see WATCH-186.C for automation backlog).

3. No legacy URLs in code (`grep` for `platform.ai-pm-research-hub.workers.dev`)

4. **Local QA workflow**: ver `docs/operations/LOCAL_QA.md` (adopted p202, issue #164 close). Default = remote-linked; local stack opcional após `supabase db pull --linked` bootstrap. Migration `20260723000000_baseline_rpcs_after_schema.sql` resolve o ordering bug que travava `supabase start`.

## Deploy Commands
- **Worker:** `npx wrangler deploy`
- **Edge Functions:** `supabase functions deploy <name> --no-verify-jwt`
- **Migrations:** Apply via Supabase MCP, then `supabase migration repair --status applied TIMESTAMP`

## Domain Architecture
- Canonical: `nucleoia.vitormr.dev` (custom domain on Cloudflare zone vitormr.dev)
- Legacy: `platform.ai-pm-research-hub.workers.dev` (301 redirect via middleware)
- MCP endpoint: `nucleoia.vitormr.dev/mcp`
- Supabase: `ldrfrvwhxsmgaabwmaik.supabase.co`

## CSRF Middleware
- `checkOrigin: false` in astro.config (Astro's check runs before middleware, blocks OAuth/MCP)
- Manual CSRF in `src/middleware.ts`: bypasses /oauth/, /mcp, /.well-known/; checks origin for all other POSTs
